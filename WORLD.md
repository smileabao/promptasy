# WORLD.md — Promptasy 世界觀聖經

> 這片土地上曾經有一群人學會了跟神諭說話。
> 他們留下的東西還在。你是後來的旅人，沿著他們走出來的路，把這件事重新學一遍。

這份文件是**世界的單一真相來源**。CLAUDE.md 管「做什麼」，這一份管「做出來的東西長什麼樣、說什麼話、怎麼跟玩家互動」。
任何一次新增場景、道具、互動、文案之前先讀這裡；做完之後，照最後一節的**維護檢查表**逐條過一遍。

CLAUDE.md 的六條開發護欄仍然優先於本文件的任何美學主張。兩者衝突時，護欄贏。

---

## 一、世界觀（Lore）

### 1.1 神諭（The Oracle）

**神諭**是這個世界的中心。它不是神、不是機器、不是角色——它是**一種會回答的東西**。
你把話說給它聽，它照著你說的做。它不猜、不補、不替你想；你沒說出口的事，它不會知道。

> 這就是整個遊戲的教學核心：**神諭＝大型語言模型**。
> 世界裡的每一條規矩，對應的都是一條真實、有官方出處的 prompt engineering 技巧。
> **換皮，但不說謊**——凡是教學內容，一定接得回真正的官方文件（見 §3.4）。

神諭從不現身。玩家只會遇到它的**痕跡**：刻文、碑、石座、面具、旋鈕。

### 1.2 回聲（The Echo）

**回聲**是唯一會直接對玩家說話的存在。序章「喚醒神諭」裡把你叫醒的就是它。

- 它是神諭留在世界上的一點餘響，不是嚮導、不是助手、不是任務發布員。
- **一次只說一句話，最多兩句，每句 ≤ 31 字。** 它不解釋規則、不催促、不誇張。
- 它記得每個人的第一句話——所以誰都沒有真的走丟過（見「回聲的小祠」）。
- 它從不使用系統術語。世界裡沒有「送出評分」「按鈕」「面板」，只有「呈給神諭」「石碑」「刻印」。
- **它也記得這裡發生過什麼**（v1.2 · P20a）：每片土地有一處回聲坐在一處小景旁邊——
  一團坐著的光。走過去按 `E`，4–6 秒的殘影會**在那一處小景的範圍內**把當年的事重演一次，
  演完就散掉。它不解說、不評論；重演之前一句、之後一句，各不超過 31 字（見 §4.17）。
- **它會回應你剛剛做的事**（v1.2 · P08）：安撫一隻濁靈、撿到一頁殘頁、讀了一塊碑、找到祕密、
  動了器物、拿到 S、升等、開了新的一片土地、第一次走進某一區、精通、收滿一種收集、久沒動作——
  各有自己的一句（分支表 `ECHO_LINES`，≥12 條）。事情多半發生在面板還開著的時候，
  所以它**先記著、等面板收起來那一拍才說**，而且 20 秒內只講最近的那一件事（回聲不排隊、不嘮叨）。

### 1.3 抄寫人（The Scribes）

那群學會說話的人叫**抄寫人**。他們已經不在了，但整片土地是他們的工作現場：

- 刻到一半的碑、沒收的茶具、拆到一半的機器、掛在架上的面具、走不到盡頭的階梯。
- 他們的失敗比成功更常見，而且**失敗被留在原地沒有清理**——那才是最好的教材。
- 工作留下來的紙也還在：**殘頁**（工單／信／筆記，§4.9）散在路邊；有些碑上不只一種筆跡——
  原句、後人補寫的一句、再後面的人劃掉的一句（**回信碑**，`lines` 支援 `{ text, hand }`）。
- 玩家在遊戲裡看到的每一句白話翻譯、每一份中文示範，設定上都是「抄寫人寫下來的」。
  這一層在資料上永遠標記 `authored: "game"`，結構上不可能被誤認為官方引文。

### 1.4 十二片土地

地圖是「中央高原 ＋ 七條橋 ＋ 七片各有調性的土地 ＋ 四座加建的院落／建物」。
每一片土地對應課程的一個大分群，也對應抄寫人的一種工作方式：

| 區域 id | 名字 | 方位 | 調性 | 抄寫人在這裡做什麼 | 地標 |
|---|---|---|---|---|---|
| `foundations` | 撰寫基本功 | 中央高原 | 開闊、平靜、冷藍 | 學會把話說完整 | 斷環 |
| `reasoning` | 示範與推理 | 西北 · 階梯迴廊 | 稀薄、看得遠、冷紫 | 一步一步想 | 無盡階梯塔 |
| `grounding` | 脈絡與長文 | 東北 · 沉書檔案庫 | 霧最濃、最暗、暖褐紙塵 | 先讀再答 | 藏書之樹 |
| `orchestration` | 流程與代理 | 西南 · 齒輪工坊 | 金屬、有節奏、偏綠 | 把大事拆小 | 巨臂吊車 |
| `config` | 角色與參數 | 東南 · 面具劇場 | 舞台光、暖、偏紅 | 換上面具再開口 | 面具拱門 |
| `forms` | 量器坊 | 正南 · 鑄場台階 | 熄了火、冷錫色、最安靜 | 把話倒進模子裡定形 | 刻度之柱 |
| `toolcraft` | 契約鍛冶場 | 正西 · 鍛台與工具溝 | 爐子還溫著、暖褐、火星最多 | 替神諭打造它的手 | 未命名的工具 |
| `wards` | 護欄崗 | 東北外緣 · 檔案庫外的哨所 | 最冷、看得最遠、螢火最少 | 守著外面來的字 | 不會關上的門 |
| `refinery` | 校驗場 | 西南外緣 · 齒輪工坊旁的院子 | 銀灰、光被折過一次 | 改自己寫過的字 | 會回頭照自己的鏡 |
| `frugality` | 減法之庭 | 正北 · 高原北緣的院落 | 最空、最平、霧最淡、螢火最少 | 學會拿掉 | 空的基座 |
| `divergence` | 分歧之廳 | 東偏南 · 高原上的建物 | 廳裡沒有暗處、曝光最高、青灰 | 把兩份相反的守則並排讀 | 兩面的柱 |
| `sight` | 觀象臺 | 正東偏北 · 仰起來的高地 | 空氣最乾淨、看得最遠、冷藍紫 | 不只讀字：看與聽 | 朝天的鏡 |

> **觀象臺（課程 v2 · Phase I）**：一片**斜著抬起來的高地**，自己一條橋 ——
> 它刻意**不接在任何一區後面**（多模態跟文字技巧沒有依賴關係，玩家隨時可以岔出去看一眼）。
> 中心 `(174.2, -23.4)`、半徑 44.2（同樣壓在 ±221 的地形網格邊界內：174.2 + 44.2 = 218.4）。
> **設計寫的是「東北高地」，實際落在正東偏北** —— 東北那一角已經被沉書檔案庫（半徑 59.8）
> 與它北緣的護欄崗佔滿，任何離檔案庫中心 < 98.8 公尺的點都會把兩片土地黏在一起。
> 整片坡由西南（橋頭）往東北緩緩升上去，走上來這件事本身就是「往上看」；
> 巨鏡斜插在最高的那一側，映著整片星空。造景全部朝著同一個方向仰起來
> （觀測架的環一律往東北抬 52 度），地標「朝天的鏡」**零實體光源**。
> **這一區教的是「怎麼寫多模態的 prompt」，遊戲並沒有真的看圖或生圖** ——
> 素材一律是抄寫人寫下來的文字，整段玩下來不會多要一張圖、一段影片、一個音檔（見 §3.3c）。

> **減法之庭（課程 v2 · Phase H）**：高原北緣的**加建**（沒有自己的橋，走出高原正北就到，
> 閘門立在 `regionAt()` 的正規化距離分界上）。它是整張地圖上**最平**的一片土地 ——
> 因為東西都被搬走了：只留中央那塊放基座的台、一道被踩平的門檻、地上幾道很淺的印子。
> 造景規則跟其他九片相反：**東西要少**（空托座 10 個、印子 12 片，別區的一半）。
> 地標「空的基座」上什麼都沒有，被拿走的那件東西只剩一圈懸在半空的光輪廓。
> 這一區的配樂是**減法**：音階只有空心音、pad 只有兩個聲部、鐘聲全場最稀。

> **量器坊（課程 v2 · Phase E）**：地形是「一把躺著的尺」——由北（橋頭）往南一階一階降下去的
> 長平台，每一階是一格刻度，中央再挖低一階當鑄槽。中心 `(0, 161.2)`、半徑 57.2
> （**半徑不是美術決定的，是地形網格決定的**：`buildTerrain()` 的平面是
> `TERRAIN_SIZE` ＝ 442 公尺見方（±221），再往南就會有一角掉出網格外）。
> 這裡的抄寫人量過所有東西，最後量到自己頭上；柱子斷在還沒刻完的那一格，
> 那把尺卻還停在半空，指著一個沒有人再讀得到的刻度。
> **它曾經是第一片沒有配樂音檔的土地**——issue #3 補上《Foundry of Measures》之前，
> 聽到的是專屬的合成 pad；那組 `REGION_MOODS` 留著當備援（見 §6.5）。

> **契約鍛冶場（課程 v2 · Phase F）**：正西的新地形，量器坊的鏡像。
> 中心 `(-161.2, 0)`、半徑 57.2（同樣壓在 ±221 的地形網格邊界內）。
> 地貌是「一張攤開的工作檯」——中央一塊抬高的鍛台，四周是放射狀的工具溝槽，
> 一格一把。抄寫人在這裡替神諭打造它的手；可是那一圈懸在半空的鑰匙，
> **每一把都沒有刻名字**——沒有人寫得出「什麼時候該用我」。
> 這一區的配樂《Contract Forge》在 issue #3 補齊；合成 pad 退回備援（見 §6.5）。

> **護欄崗（課程 v2 · Phase F）**：它**不是新大陸，是加建**（`REGION_SITES` 上的
> `annexOf: 'grounding'`）。中心 `(140.4, -185.9)`、半徑 35.1，**刻意與沉書檔案庫重疊**——
> 所以它**沒有自己的橋**：走出檔案庫北緣，跨過那道門檻就到了，中間沒有虛空。
> 重疊處誰說了算？比的是**正規化距離** `d / radius`（`regionAt()`）——離自己中心
> 越近（相對於自己的大小）的那一片贏；閘門就立在那條分界上，所以人被擋下來的
> 那一步正好在拱門底下，而母土地一寸都沒有被吃掉（測試逐關驗過檔案庫的 13 座石座）。
> 中心往東偏（換算成放大前是 7 公尺），是為了讓檔案庫西北角那座「抄書人的桌」不會被算進哨所的地界。
> 這一區的配樂《The Unclosing Door》在 2026-08-03 補齊；合成 pad 退回備援（見 §6.5）。

> **校驗場（`refinery`，Phase G）也是加建**——齒輪工坊西南外緣的院子。
> 同一套機制（`annexOf: 'orchestration'`、`ANNEX_LINKS` 的頸口、正規化距離裁定重疊處），
> 所以它一樣**沒有橋**：走出工坊西南邊的桁架就到了。半徑 52（比哨所大得多，
> 因為它要站得下 11 座神廟），中心 (-167.7, 167.7) 壓在 `buildTerrain()` 的 ±221 網格內。
> 地貌是「兩面互相照著的鏡」：一條淺谷把院子分成幾乎一樣高的兩半。
> 這一區的配樂《Proving Yard》在 issue #3 補齊；合成 pad 退回備援（見 §6.5）。
> **它的地標刻意不站在區域中心**（其他八座都站中心）：52 公尺半徑要站下 11 座石座，
> 中心讓給石座，鏡子推到院子那一角——這是「地標可以偏心」的第一個先例。

橋中央各有一道**閘門**。解鎖條件有兩種寫法：
既有四條橋是「等級 ＋ 前一區通關數」；
**量器坊、契約鍛冶場、護欄崗與校驗場走的是「知識式軟門檻」**——條件寫的是「你已經會了哪幾條」，
不看等級、不看通關數：量器坊＝`clear-specific` ＋ 角色與參數任一座；
契約鍛冶場＝`agent-approval-bounds` ＋ 流程與代理三座；護欄崗＝脈絡與長文三座 ＋ 契約鍛冶場一座；
**校驗場＝流程與代理兩座 ＋ 任一區精通**（`masteredAny`，Phase G 新開的一種條件——
精通的定義完全沿用 `regionMastery()`，不會有第二套判準）。
**護欄崗、校驗場、減法之庭與分歧之廳的門不在橋上**（它們沒有橋），而是立在加建的頸口上，其餘行為完全一樣。
**加建的地界不得吃掉別人的橋**（Phase J1 的硬規則）：加建的院落刻意與母土地重疊，
但它的圓盤有可能擦到另一片土地的橋 —— 這時候**橋說了算**（`regionAt()` 只對加建的院落套這條規則）。
不這樣做的後果不是難看而是走不過去：`isWalkable()` 擋的是「屬於這座院子的點」，
院子的地界一旦蓋到別人的橋上，那條橋在院子解鎖之前就整條斷了。
**兩種寫法的門面完全一樣。**
條件沒到時門是關的，但**它會問你一句**——走到門前（或按 `E`）會出現「這道門的考驗還沒完成（還差…）。想先過去看看嗎？前方的試煉不會因此變簡單」。
選「直接前往」門就開了（記進存檔的 `skippedGates`），選「先留下修行」門留著、走遠再回來才會再問一次。
**先行前往開的是門，不是進度**：不給 XP、不收技巧、不寫任何一關的評價——所有數字照實計；之後真的補滿條件時也不會再慶祝一次。

> **2026-08-03 站長裁決：全世界一道硬門檻都沒有。**
> `divergence`（分歧之廳）曾經是唯一一道不能先行前往的門（`hard: true` ＋ `masteredAny: 4`），
> 現在**比照其他區域**：`hard` 旗標拿掉、門檻降到 `knowledge.masteredAny: 2`（規格仍逐字取自
> `regions-v2.json`），走的是上面那條「想先過去看看嗎」的路，也照樣誠實記進 `skippedGates`。
> 課程設計的理由（curriculum-v2 §5.4：這一區的每一關都建立在「你已經知道通則」之上）
> 改用**門上那句話**傳達，而不是用一道關死的門：玩家自己決定要不要先進去看看。
>
> 機制本身留著：`gate.js` 仍然讀 `status.hard`，真的要鎖哪一道門時不必重寫 UI
> （那條版面只留「先留下修行」與 `Esc`、文案不寫成失敗）；`skipGate()` 的攔截也還在。
> **要再開一道硬門檻之前，先問一次「這一區真的沒有通則就學不會嗎」。**

過一座橋是一個**事件**——霧色、色偏、環境光、配樂全部平滑漂移，外加一記光湧。

### 1.5 NPC 與「誰在說話」

這個世界**沒有會走動的 NPC**。所有的「人」都以痕跡出現：

- **石座上的委託人**：每一關各有一個 `npc` 名字（守門人、郵箱精靈、印刷工、派工的管事、量器坊的門房…）。他們只在關卡文字裡出現，不在場景裡站著。
- **回聲**：序章、導航提示、解鎖宣告。
- **守望的小獸**（Phase 22）：唯一會動的活物。沒有名字、沒有台詞、不能互動——它只是遠遠看著你，太近就竄開。它的存在是為了讓這片土地不像博物館。
- **濁靈**（v1.2）：留在原地的東西——會轉頭看你，但不移動、不靠近、不跟隨（見 §1.6）。
- **守夜人**（v1.2 · P16c）：每片土地一位、提著一盞燈**站著不動的人**——這個世界第一個**會回答你的人**。
  「沒有會走動的 NPC」那條鐵則沒有被改掉：**他本來就不走**。他會動的只有頭、光、呼吸（同濁靈的規矩）；
  不移動、不靠近、不跟隨、不離開崗位，`prefers-reduced-motion` 之下連呼吸都停掉，只留終態。
  走近按 `E` 開一扇小窗，**用選的、不打字**，一次一句，1–3 拍；他給四種情報（見 §3.2 的第 ⑧ 層）。

- **守門者**（v1.2 · P18）：護欄崗那道「不會關上的門」旁邊**站著不動的人**——這個世界第一個**身上帶著一份 system prompt 的人**。
  同守夜人：他本來就不走，會動的只有頭、胸前那塊板上的字、呼吸；`prefers-reduced-motion` 之下只留終態。
  他胸前掛著一塊寫滿字的板 ＝ 他的**交辦**（玩家走近按 `E` 讀得到整份七行）。
  那份交辦就是教材本身：指令階層、規矩排在資料前面、界定符、挑罕見的界定符、
  把會出事的那一步換個形狀、動作前確認、先替他試過假扮的委託。
  說服他要**用選的、不打字**，每一個選項都是真的送進評分引擎的一句話；對上一行，板上就亮一行。
  **沒有失敗態**：他只有一種狀態沒到——**還沒被說服**。已經對上的行**永不暗回去**（存檔累積），
  分兩次各說一半也說得完（同濁靈的契約）。他不擋路、不拿走任何東西、不影響任何解鎖。

新增「角色」時的原則：**先問這個角色能不能改成一件留在原地的東西。** 通常可以，而且通常更好。
守夜人是這條原則的極端解：他是「人」，但他是一件**站在那裡的東西**。

### 1.6 濁靈與清燈（v1.2「濁靈之夜」）

回聲在序章說過：「這座高原上沒說清楚的話，都會落到我這裡。」接不住、又被一再重複的那些，落進霧裡結成一團——抄寫人叫它**濁言**，後人叫它**濁靈**。

- 濁靈**不是怪物**：牠是一句還沒說完的請求，卡在原地等人把它說完。每一隻＝一段寫壞的 prompt（含糊、只說「不要」、沒給範例、憑印象、一口氣做完…），身上一層層濁氣＝那句話缺的每一件事。
- **安撫＝代它把話說完。** 你寫的好 prompt 不是攻擊牠，是替牠把那句話重說一遍。每說清楚一件事，牠就少一層濁氣；說到牠聽懂，牠安靜下來變成一盞**清燈**——留在原位，回頭看得到自己安撫過的痕跡。世界的光語言一致：石座的光柱＝還沒解開的問題，清燈＝已經解開的問題。
- **沒聽懂不是失敗**：殼還在、眼光還盯著你，回聲不催。已經聽懂的部分牠**記著、永不忘**（存檔累積），下次接著說就好。牠不會靠近、不會拿走任何東西——牠自己就是被拿走了意思的話，沒有能力再拿走別的。
- **牠會動的只有頭、光、霧、殼。** 安撫那一下有一撮光屑繞你一圈再回到燈位，那是光，不是實體；沒有任何東西跟著玩家走。
- 用語：畫面上叫牠**濁靈**、牠那句話叫**濁言**、安撫後叫**清燈**；不說「怪物」「敵人」「打敗」「傷害」「血量」。
- 資料：`src/data/murks.json`（`authored: "game"`）；每一隻的檢查器與出處都沿用同一區已有神廟的官方文件，不新增技巧、不改課程資料。

**兩個尺寸（v1.2 · P17）**：同一種東西、同一份契約，差別只在量體與層數。

| | 小濁靈 | **大濁靈** |
|---|---|---|
| 數量 | 8 隻（前四區各 2） | **12 隻（一片土地一隻）** |
| 資料 | `kind` 缺省 | `kind: "great"` |
| 殼／段數 | 3 | **6–8** |
| 底座碰撞半徑 | 0.9 | **1.5** |
| 互動半徑 | 5.5 | **6.0**（最外層的殼撐到 3.99 公尺，5.5 幾乎貼著殼面） |
| 圖鑑 | 濁言 ＋ 正言 ＋ 出處 | 再加**兩層**：A 開抄寫人的眉批、S 開一句來歷 |

- **規則疊加**：大濁靈的每一段都寫著「這一層要什麼」，**走到才看得見**（沒走到的只有一個「？」）。
  與 Password Game 最大的差別是**已經散掉的層不會回來**：存檔記著的那幾層，重開面板時直接刻好、不再問一次——
  這就是「進度只累積」在畫面上的樣子。
- **分兩次各說一半也安撫得了**：安撫看的是跨次**聯集**（`state.murks[id].hits`），不是「同一次全部到齊」。
  門檻是總權重的 75%（小濁靈 3/4）。

---

## 二、視覺與調性

### 2.1 一句話

**平靜的夜間探索。** 不是恐怖、不是荒涼、不是奇觀——是「你半夜一個人走在一片有人來過的地方」。

### 2.2 硬規則

- **一律夜景。** 沒有白天、沒有日出。曝光 1.02（低畫質補到相同亮度）。
- **一夜的時辰（v1.2 · P05）。** 天空是進度的外顯：時辰 = f(精通區數 0.5、技能數 0.3、清燈數 0.2)，四態
  **入夜 → 深夜 → 月落 → 星最亮之夜**（p < .25 / < .5 / < 1 / = 1）。**永遠是夜**：終態是月落之後、
  星最亮的那一夜——**沒有黎明、沒有魚肚白、沒有東方發白**（roadmap 鐵則 3）。時辰**只乘因子、不換色系**
  （霧亮度 ±10%、hemi ±0.08、曝光 ±10%、月高／月相、星密度、極光強度／色相；區域色盤一個位元組不動），
  入夜的因子逐值等於「沒有時辰之前」的畫面。**`engine.setMood()` 是唯一入口**：區域色盤 × 時辰因子
  在 `main.js` 的 `applyMood()` 用 `composeMood()` 組好再送進去；P06 的區域色彩腳本、任何之後想動霧色／
  月亮／星星／極光的東西，都走這同一個入口，不另外寫 uniform。時辰改變是低頻事件（進區／進程變化），
  平滑交給引擎既有的 `moodNow` lerp；`engine.forceHour(n|null)` 只給測試與截圖（`scripts/shots-hours.mjs`）。
- **地面壓暗、天空是主角。** 星空、月盤與月暈、兩層極光帶、貼地霧氣。抬頭（`↑`）看得到完整的天。
- **有限色盤。** 每區一個主色（`kitFor()` 產生 accent / light / mid / dark 四階），冷星光是介面主色，**暖金只留給成就熱點**（過關、精通、找到東西、可點的出處）。
- **區域色彩腳本（v1.2 · P06）。** 每區一組色 `{ skyTop, skyLow, fog, tint, key, rim, particle }`，寫在
  `src/data/color-script.json`（`authored:"game"`、純視覺、無出處），由 `src/world/color-script.js` 讀＋驗、
  `main.js applyMood()` 用 `colorScriptFor(region)` 當 `composeMood()` 的第一個參數 —— **仍是 `setMood()` 那一個入口**。
  `fog／tint` **就是** `REGION_ATMOSPHERE` 現值（json 只是可讀複本，rubric 逐值比對）；`skyTop／skyLow` 相對全域
  `PALETTE.sky／skyLow` 只做**色相 ±≤12°、亮度 ±≤0.08** 的微偏，HSL 亮度 ≤ 0.35（永遠是夜）—— 穹頂材質用「目標 ÷ 基準」
  的乘數套在原本的漸層貼圖上，中央高原乘數 ＝ 1，畫面逐值不變；`key`＝那一盞主色補光、`rim`＝道具自發光補色（覆寫
  `kitFor().light`）、`particle`＝螢火色，三者建構時就套、不平滑。時辰因子只乘不換色（P05）。走進新的一片土地，
  「顏色變了」就是進度感。**地面的兩色基底（v1.2 · P12）**：`groundLow`／`groundHigh` 取代全域的 `PALETTE.ground`／`groundHigh`，
  低處一色、高處一色（沿用原本的高度階）；區界 **6 公尺**內漸變（判準與 `regionAt()` 一樣是正規化距離）；
  再疊一層週期 20／8 公尺的低頻碎紋（只改亮度 ±6%，**低畫質只留基底**——頂點色是**建構時**烤好的，
  所以這一層跟地形段數一樣是「開新世界時決定」，遊戲中途切畫質不會重烤）。
  **橋面**（兩片土地的半徑之間那一段）不屬於任何一片土地，照 `BRIDGE_SPANS` 沿著橋從這頭漸變到那頭、
  再用離橋中線 14 公尺的橫向斜坡與腳下的土地互相讓位 —— 兩端與土地接得上，穿過別片土地的那幾條也不跳色
  （P12 審查前橋頭有一條 0.098 的硬邊；現在沿線最大跳色 <0.002，`test:rubric` 拿它當硬斷言）。
  全部住在 `src/world/ground.js`，逐頂點呼叫、純頂點色 —— 不改高度場、不影響可行走判定與碰撞、零貼圖檔。
  夜色門檻：`groundLow` 亮度 ≤ 0.30、`groundHigh` ≤ 0.40、飽和 ≤ 0.35，而且高處一定比低處亮。
  **兩兩要分得出來**：`toneDistance()`（HSL、色相差用飽和度加權）66 對全部 ≥ `TONE_DISTANCE_MIN`（0.05）
  （實測最小 0.064 校驗場／分歧之廳、中位數 0.203、最大 0.514）；`test:rubric` 拿它當硬斷言。

  下表由 `node scripts/color-script-table.mjs` 從 json 產生，**改 json 就重跑貼上，不手抄**：

| 區域 | skyTop | skyLow | 天空偏移（色相°／亮度） | fog | tint | key（補光） | rim（補色） | particle（螢火） | groundLow | groundHigh |
|---|---|---|---|---|---|---|---|---|---|---|
| 中央高原 `foundations` | `#101a28` | `#33465c` | 0° ／ 0.00·0.00 | `#1e2c40` | `#bcd6e6` | `#a9c9d8` | `#b6c3ca` | `#c4dce9` | `#2a3947` | `#415a69` |
| 階梯迴廊 `reasoning` | `#11172c` | `#334061` | +12° ／ +0.01·+0.01 | `#232a48` | `#c6c2ee` | `#b7b0d8` | `#bcb9ca` | `#cad0e9` | `#2f3350` | `#4a5078` |
| 沉書檔案庫 `grounding` | `#10141e` | `#373f4d` | +8° ／ -0.02·-0.02 | `#2a2b33` | `#e4d3b8` | `#9eccbe` | `#b2c4be` | `#bfdddc` | `#3b3527` | `#5d5340` |
| 齒輪工坊 `orchestration` | `#0f1b24` | `#314a59` | -9° ／ -0.01·-0.01 | `#1a2b2e` | `#a7d2cd` | `#dcc79c` | `#ccc2b1` | `#dddbce` | `#243830` | `#3a5a4b` |
| 面具劇場 `config` | `#141c33` | `#384566` | +10° ／ +0.03·+0.03 | `#33253a` | `#f0c8c0` | `#adb6c6` | `#b8bbc2` | `#c6d3e0` | `#472f39` | `#6d4956` |
| 量器坊 `forms` | `#151e29` | `#3c4b58` | -2° ／ +0.01·+0.01 | `#263139` | `#d6dcc4` | `#c2c79f` | `#c0c2b2` | `#d0dbcf` | `#353d33` | `#525f4e` |
| 契約鍛冶場 `toolcraft` | `#151b2d` | `#3b465e` | +10° ／ +0.02·+0.02 | `#35262a` | `#e8c3ac` | `#c8977c` | `#c3b0a7` | `#d3c6c3` | `#45332a` | `#674d3f` |
| 校驗場 `refinery` | `#17202b` | `#404d59` | -2° ／ +0.02·+0.02 | `#24303a` | `#cdd8dc` | `#9fb8c4` | `#b2bcc1` | `#bfd4df` | `#3a4348` | `#5b676d` |
| 減法之庭 `frugality` | `#16212d` | `#3d4f5c` | -4° ／ +0.02·+0.02 | `#1d2a33` | `#c9d4d2` | `#c3ccc7` | `#c1c4c2` | `#d0dde0` | `#2d3d37` | `#485f56` |
| 觀象臺 `sight` | `#0c1527` | `#2a3e60` | +5° ／ -0.01·-0.01 | `#1c2440` | `#c0cdf2` | `#b0c0e6` | `#b9bfd0` | `#c7d7f0` | `#2a3052` | `#404678` |
| 分歧之廳 `divergence` | `#19212e` | `#444d5b` | +2° ／ +0.03·+0.03 | `#2b2f3c` | `#d8d2e4` | `#d8b8c6` | `#cabcc2` | `#dbd4e0` | `#3a3849` | `#585670` |
| 護欄崗 `wards` | `#0d1821` | `#2f4655` | -8° ／ -0.02·-0.02 | `#1b2733` | `#b4c6dc` | `#8f9fb8` | `#adb2bc` | `#b9c9da` | `#222d3f` | `#344961` |

- **軟門檻三態（v1.2 · P06）。** 閘門與石座依 `progression.gateStatus()` 呈三態，從高原遠看就讀得出：
  閘門 **已解鎖 → 主色亮**（柱 emissive 0.6×、拱 1.3）；**未解鎖但這道門的條件指向的區已有一片解鎖（可以先行前往）→ 邀請琥珀
  （`PALETTE.invite` `#a8865c` 0.35×，不是成就暖金）**；**條件指向的區一片都沒解鎖 → 暗**（0.12×）；硬門未解鎖一律暗。
  「條件指向的區」：鏈式門看 `requires.region`；知識式門看 `knowledgeGaps` 指到的區（技能所在區、`regionSkills`／`mastered` 的
  `regionId`；`masteredAny: N` 沒指名 → 已解鎖的區夠 N 片才算前路已開）；什麼都沒指到 → 母土地／中央高原。
  石座 **所在區未解鎖 → 暗**（光柱／腳下圈底亮度 ×0.4）；**先行前往開的區 → 腳下圈邀請琥珀微亮**；正常解鎖 → 現狀。
  純函式 `gateVisualState()`／`gatePrevUnlocked()`／`markerVisualState()` 在 `world.js`；`world.refreshGates()` 在解鎖／跳門／進區／
  重置時呼叫、變化走 `update()` 的 lerp 不硬切（逐通道離目標 < 1e-3 就貼上，`visualSettled` 為真後每幀零工作）。
  **0 新光源**：三態只改 emissive 與透明度；**暖金仍只給成就熱點**（過關、精通的石座）。
- **靠打光與後製，不靠資產數量。** bloom 0.58/0.82/0.55、filmic 色彩分級（只抬暗部）、霧、暗角。程序化幾何體 ＋ flatShading，不下載任何外部模型。
- **高的東西旁邊要矮。** 每區一個地標，周圍強制留白 14–16 公尺。地標是導航工具，不是裝飾。
- **對比引導，不畫引導線。** 玩家看的是亮度／顏色／動態的對比。沒有小地圖上的箭頭，只有光柱、指南針的金針、走出來的路。

### 2.3 介面：夜間檔案館 ＋ 刻印牌

介面不是 HUD，是**器物**。設計語言寫在 `src/styles.css` 的檔頭，重點：

- **髮絲線取代陰影**當層級（`--rule` / `--rule-gold`）；明度階 `--ink-0…4` / `--paper…4` 取代玻璃感。
- **editorial 排版**：oversized 襯線大標、tracked-out 的全大寫 meta label（**0.22em 字距只給 Latin，漢字不加**）、非對稱分欄、首字放大。
- **Latin 字型一律排在 CJK 之前**，Latin 面加 `size-adjust` 對齊思源宋體的 cap-height。使用者自己打的字走系統字型（`--font-input`）。
- **一條招牌曲線**：`--e-out: cubic-bezier(.19,1,.22,1)`。
- **行內 SVG `feTurbulence` 顆粒**，零外部圖檔。
- 字型全部 SIL OFL、自架子集、零 CDN。**改任何中文字串都要重跑 `npm run fonts`。**

### 2.4 動態

- 角色是 rigless 程序化動畫（沒有骨架、沒有 GLTF、沒有動畫檔），動作全是相位的函式。
- 世界會動的東西：齒輪、飄浮階梯、書頁、刻度盤指針、吊車載重、霧、螢火、**每區專屬的空中粒子**（§4.11）、極光、星空閃爍。
- **`prefers-reduced-motion` 關掉的是「動」，不是「回應」**——光還是會亮，聲音還是會響，只是不再甩動／噴散。
- **逐項的讓步表（v1.2 · P25a 逐條量過，`test:rubric` 逐項守著）**：
  · **停住**（世界層 `kineticWorld` ＝ 0、旅人 `calm` ＝ 0、天空 `auroraDrift` ＝ 0）——
    石座的浮片／腳下那圈／光柱／光環、刻文石板與殘頁的光環、器物與祕密的自轉、
    起始祭壇的火心、閘門的拱、小景的浮階與刻度盤指針、地標的齒輪與吊載、
    空中的塵、貼地霧氣、工坊的齒輪、極光的漂，以及旅人**站著**的時候：
    呼吸、重心左右移、圍巾與提燈的閒置擺盪。
  · **照舊**（那是資訊，不是動）—— 每一處亮度／顏色／不透明度：星星的明滅、
    `engine.pulse()` 那一下亮度湧升、石座三態的濃淡、走近就亮、濁靈的眼光閃、
    披肩光點的等級與升等那一下、提燈的呼吸；以及旅人**走路**的整套步態、
    過關的舉手、跳躍本身 —— 那些是玩家自己按出來的動作，關掉會少掉資訊。
  · **為什麼氛圍層是 0 而不是 0.12**：反應場／器物／濁靈那三層用的是 0.12，
    因為它們是「走近才動一下」的一次性回應；氛圍層那一批是**永遠不停的迴圈**，
    打一折仍然是永遠在動，那不叫讓步。

### 2.5 開場（Phase 34）

- **按下開始之前，3D 世界一眼都不准被看到。** 黑幕 `#bootcover` 寫在 `index.html` 裡、
  用**行內樣式**（`styles.css` 是 `main.js` import 進來的，那之前的每一幀都保護不到），
  第一幀就蓋滿視窗（`position: fixed; inset: 0`，實色不透光，`z-index: 35`
  ——世界／HUD 之上、標題卡 40 與入場門 45 之下）。引擎照樣在底下暖機，只是沒人看得到。
- **掀開只有一個時機**：標題卡按下開始。1.4 秒淡出（`is-lifting`），淡完把節點移除。
  推開入場門**不**掀（門後面是標題卡，不是世界）。
- **開場的節奏是一件一件來，不是一起浮上來**：
  入場門＝光（0.35s）→ 字（1.5s）→ 提示（2.9s）；
  標題卡＝名字整個從模糊裡對焦（0.15s）→ 髮絲線（1.0s）→ 定位句**打字**（1.35s）
  → 中文句打字 → 開始鍵浮出（`.is-ready`）。
- **打字機不准擋輸入**：任何時候按下去都直接進場，兩句話在同一拍被補完
  （`title.finishTyping()`），不會有半句話淡出去；`prefers-reduced-motion` 下不打字，直接顯示。
  完整句子另外放一份在 `.sr-only` 裡——螢幕閱讀器不會念到打一半的字。

---

## 三、互動文法（Interaction Grammar）

### 3.1 按鍵

> **鐵則（Phase 23）：整趟旅程不碰滑鼠也走得完。** 新增任何互動之前，先問「純鍵盤怎麼做到這件事」。
> 滑鼠永遠保持可用，但它不能是唯一的路。

**世界層**（沒有任何一層開著的時候）：

| 鍵 | 做什麼 |
|---|---|
| `W A S D` | 走（只管走路） |
| `Shift` | 跑 |
| `←` `→` / 滑鼠拖曳 | 轉鏡頭（只管視角） |
| `↑` `↓` | 抬頭 / 低頭 |
| 空白鍵 | **跳（v1.2 · P16a 起：有高台的八片土地 ＋ 開了缺口的那一座橋跳得起來）** |
| `-` `=` / 滾輪 | 鏡頭拉遠 / 拉近（別名：`PageDown` `PageUp`、數字鍵盤 `−` `＋`） |
| **`E`** | **唯一的互動鍵**（含 v1.2 · P19 捷徑那道門的絞盤：按三下，不發明「按住」） |
| `C` / `O` / `F3` | 圖鑑 / 設定 / 效能監視器 |
| `?` | 操作一覽（疊在最上面，讀題讀到一半也叫得出來） |
| `Esc` | 收起最上面那一層 |

> **跳躍鍵為什麼是空白鍵（v1.2 · P13 先定 `J`，站長實玩後改成空白鍵）。**
> 第一版把跳躍放在 `J`，理由是「`Space` 與 `Shift` 都已經有主人了」——
> `Space` 是「一口氣抬頭看天空」、`Shift` 是跑。實玩之後那個理由不成立：
> **跳躍在所有遊戲裡都是空白鍵**，肌肉記憶不在我們這邊，玩家的手會自己按下去；
> 而「一口氣抬頭」本來就只是 `↑` 的捷徑（序章同時教的也是 `↑`），
> 為了一個捷徑把最順手的鍵讓出去，換來的是每一個新玩家第一次都跳不起來。
> 所以：**空白鍵 ＝ 跳**，抬頭看天空留給 `↑`（滑鼠上下拖曳也還在）。
> `Shift` 仍然是跑 —— 「跑著跑著想跳」是合理的組合，「跑著跑著想抬頭」不是。
> 焦點停在按鈕／勾勾上時空白鍵一律不跳（那時候它的意思是「按下它」，
> 設定頁的靜音勾勾就是這樣切的）；面板開著時世界層本來就收不到鍵。
> 手把（`A` / ○）與觸控的跳躍鍵留給 **P24**（行動裝置那一格）一起決定。

**跳躍的規則（v1.2 · P14；常數住在 `src/player/jump.js`，`test:rubric` 逐條比對這一段）：**

| 事 | 值 | 為什麼 |
|---|---|---|
| 跳得起來的土地 | **八片**（`JUMP_REGIONS`：中央高原、沉書檔案庫、面具劇場、量器坊、示範與推理、齒輪工坊、減法之庭、校驗場） | 一次打開 12 片土地＝一次押上 177 顆可站立體。P14 先用一片土地、一座高台證明整條路；P15 加到四片；**v1.2 · P16a 先量再放**——12 片土地各跑一次 `screen-fit`（表在 §4.12），蓋得出高台的長到八片。**開的一定是真的蓋了高台的那幾片**：在跳不起來的土地上蓋高台等於蓋裝飾，而「按了 `J` 卻沒有任何東西跳得上去」比不能跳更糟。量出來擺不下的四片（契約鍛冶場、觀象臺、分歧之廳、護欄崗）跳躍速度仍然是 **0**，每一幀與 P14 之前完全相同 |
| 跳得起來的橋 | **只有開了缺口的那一座**（`JUMP_BRIDGES`，v1.2 · P15） | 橋是唯一的必經之路，在上面亂跳只會製造「跳出去」的機會。這張表與 `BRIDGE_GAPS` 的 `region` **一一對應**（`test:rubric` 逐條比對）：有缺口才跳得起來，沒缺口一律 0 |
| 跳得多高 | 起跳 10.4 m/s、重力 26 m/s² → 頂點 **2.08 公尺** | 契約是「**跳得上 1.6、跳不上 3.0**」：1.6 是第一階的高度，3.0 是 `STAND_MAX_H`（§6.3「再高就不該跳得上去」） |
| coyote time | **100 ms** | 走出邊緣之後還能跳 —— 眼睛看到的「我明明還在上面」與程式判定的「你已經離開了」差的就是這一段 |
| input buffer | **150 ms** | 落地前先按的跳，落地那一刻補發 |
| 鬆手提前下落 | 上升中放開空白鍵 → 速度 **砍半** | 輕點是小跳、按到底才是滿跳；同一個鍵兩種高度 |
| 積分 | 垂直方向切成 **1/120 秒**的小步 | 這台機器一幀真的會有 0.2 秒（軟體渲染）。整幀積分會讓同一次跳躍在「升到 1.04」與「衝到 3.12」之間跳來跳去 —— 兩種錯法都跟機器速度有關 |

**三條不可妥協的護欄：**

1. **不按 `J` 就與 P14 之前逐幀相同。** 玩家只有跳躍才會離地，只有離地才可能站到東西上；
   兩者都沒發生時 `updateVertical()` 回的就是 `terrainHeight()` —— 是同一行，不是「很接近」。
2. **永遠不會掉進虛空、永遠不會被傳送。** 空中的每一步水平位移照樣走 `clampPosition()`，
   `isWalkable()`（覆蓋率 ＋ 閘門）一寸都沒放寬；起跳那一刻若腳下不合格（卡在石頭裡、站在虛空邊緣）
   **就地夾住，不准離地**。
3. **`prefers-reduced-motion` 拿掉的是擠壓，不是跳躍**（§2.4）：位移一寸不減，
   拿掉的只有 squash-stretch 與塵埃往外飛的那一段；塵照樣亮一下再淡掉。

**開著一層的時候**（主控台、序章練習台、圖鑑、設定…）：

| 鍵 | 做什麼 |
|---|---|
| `Tab` / `Shift`+`Tab` | 在這一層裡移動焦點（焦點鎖，不會跑到 3D 畫布上） |
| `↑` `↓` `←` `→` | 在**同一組**東西之間移動焦點：石碑選項 / 快速填入 / 技巧積木 / 圖鑑的 68 條（`rovingList()`） |
| `Home` / `End` | 跳到那一組的頭 / 尾 |
| `Enter` | 第一、二幕 → 往下一幕；停在可按的東西上就是按下去 |
| `1` `2` `3` | 挑一段刻上石碑 |
| 按住 `Enter` | 把手掌按上石碑（＝按住手掌印） |
| `Alt`+`1…4` | 直接回到某一幕（`1 2 3` 已經給石碑用了，所以走 Alt） |
| `L` | 翻開線索（第二幕）／ 神諭刻文側頁籤（第三幕） |
| `H` | 叫出提示球（只在自由書寫時存在） |
| `M` | 換一種答題方式 |
| `S` | 分享（結果卡 / 圖鑑） |
| `Esc` | 收起這一層 |

三條配套規則：

1. **所有單鍵快捷在文字欄位有焦點時一律失效**（`isTypingIn()`）—— 玩家打的 prompt 裡本來就會有這些字母。
   反過來，`Esc` 與 `Tab` **不准**被輸入框 `stopPropagation` 攔下來：那是「走出去」的路。
2. **有快捷鍵的東西就把鍵帽戴在身上**（`<kbd>` 小石籤），不要只寫在操作一覽裡。
3. **開一層的時候，焦點落在內容區第一個可按的東西上**（`createOverlay` 預設），不是標頭那顆關閉鍵 ——
   內容區沒東西可按（石碑、刻文那種小窗）才退回關閉鍵。開子層（提示框、分享卡）焦點要進去，關掉要還回來。
4. **焦點只准停在真的畫得出來的東西上。** 收起來的 `<details>` 裡面的東西在新版 Chrome 仍然有 `offsetParent`，
   `focus()` 對它是空包彈 —— 判定要用 `getClientRects().length > 0`（`rovingList()` 已經這樣做）。

**已知且刻意保留的滑鼠專屬操作**：效能監視器（`F3`）的**拖曳位置**。它是診斷工具、不影響遊玩；
開關與摺疊都有鍵盤路徑（`F3` ／ Tab 到摺疊鍵）。

**`E` 是唯一的互動鍵，永遠不要再加第二個。** 走近會有提示（`hud.setInteract`），提示一定長這樣：
標題 ＋ 一句狀態 ＋ `<kbd>E</kbd>` ＋ 一個**動詞**（互動 / 閱讀 / 看一眼）。

### 3.2 四層互動（新增互動前先確認你要的是哪一層）

| 層 | 東西 | 互動半徑 | 開什麼 | 教不教技巧 | 存檔欄位 |
|---|---|---|---|---|---|
| ① 反應 | 風鈴、音石、光菇、水紋、小獸、螢火 | 1.75–4.4（自動） | 什麼都不開 | 否 | 無 |
| ② 風味 | 世界觀石碑（12 塊）／祕密（**12 處**，v1.2 · P15） | 4.6 ／ 5.0（自動） | 一塊石板、兩三行字 | **否**（護欄） | `loreRead` / `secretsFound` |
| ③ 小語 | 刻文小語（13 則） | 3.8 | 很小的對話窗 | **是**（附出處） | `inscriptionsFound` |
| ④ 關卡 | 石座（Phase E 起 75 座） | 6.5 | 四幕分鏡主控台 | **是**（完整評分） | `bestGrades` |
| ⑤ 器物 | 動得了的東西（22 件、8 種，見 §4.6） | 3.2 | 多半什麼都不開（只說一句話） | **否**（護欄） | `handlesUsed` |
| ⑥ 遭遇 | 濁靈（v1.2；**20 隻 ＝ 8 小 ＋ 12 大**，見 §1.6／§4.8） | 小 5.5 ／ **大 6.0** | 同一座主控台（預設用選的，第一幕是牠的濁言） | **是**（rubric 沿用神廟的檢查器＋出處） | `murks` |
| ⑦ 殘頁 | 抄寫人的殘頁（v1.2；24 頁，見 §4.9） | 3.8 | 一頁紙的小窗（幾行字，一半附教學） | **一半是**（附出處）／**一半否**（純風味） | `lettersFound` |
| ⑧ 守夜人 | 站著不動的人（v1.2 · P16c；12 位，見 §1.5／§4.14） | **4.6** | 選項式的對話小窗（用選的，不打字） | **引用**（技巧那一項只引既有技能＋它自己的官方連結） | `watchmen` |
| ⑨ 守門者 | 帶著一份看得見的交辦站在門邊的人（v1.2 · P18；1 位，見 §1.5／§4.15） | **3.2** | 選項式的對話小窗（交辦 ＋ 你可以說的幾句） | **是**（分支只綁既有檢查器；出處引用那一關自己的官方連結） | `guardians` |
| ⑩ 機關 | 捷徑那道門的絞盤（v1.2 · P19；2 座，見 §4.16） | **3.2** | 什麼都不開（只說一句話） | **否**（護欄） | `shortcuts` |
| ⑪ 回聲重演 | 坐在一處小景旁邊的一團光（v1.2 · P20a；12 處，見 §1.2／§4.17） | **3.2** | 什麼都不開（4–6 秒的殘影在世界裡重演） | **否**（護欄） | **無（不加存檔欄）** |
| ⑫ 檔案廊 | 每片土地一座小展館（v1.2 · P20b；12 座、24 則小知識，見 §4.18） | **3.2**（走近浮出，**不搶 `E`**） | 什麼都不開（畫面側邊浮出一張讀得到、按不到的紙片） | **是**（「為什麼」那一層，附出處） | **無（不加存檔欄）** |
| ⑬ 終局 | 回聲的小祠 ＋ 母碑（v1.2 · P22；2 件，見 §4.20） | 小祠 **4.6** ／ 母碑 **7.0** | 儀式那一扇窗（還你第一句 → 牠聽懂了 → 刻不刻）／讀碑 | **否**（護欄；判定只做加法，沒有失敗態） | `flags` ×3 ＋ `motherStele` |

搶 `E` 的優先序：**終局 > 石座 > 濁靈 > 守夜人 > 守門者 > 石碑 > 刻文小語 > 殘頁 > 器物 > 機關 > 回聲 > 閘門**。
**終局排在最前面**（v1.2 · P22）：它是整條故事線的終點，沒有任何一層該蓋掉它；
而它的兩個圈**與每一層都不重疊**（量出來的：小祠還剩 4.45、母碑還剩 0.32），所以「排第一」在實務上不會與誰相爭。
它也是「半徑遞減」那條慣例的第二個往嚴方向的例外（同守門者）：母碑的 7.0 比石座還大 ——
它站在斷環臺座正中央，人最近只走得到 6.02 公尺，小於這個半徑就按不到（量法在 §4.20）。
**那一層絕大部分時間交不出東西**：沒到門檻的小祠與沒立起來的母碑一律回 `null`，所以底下每一層的行為與 P21 之前逐字相同。
**檔案廊不在這條序列上**（v1.2 · P20b）：它連 `E` 都不搶——走近就浮出來，而且只要這條序列上任何一層在範圍內，它就自己讓開並把亮度熄掉。所以「`E` 是唯一的互動鍵」這條鐵則在它身上是**用不加鍵**的方式成立的。互動半徑依這個順序遞減，讓小東西不會蓋掉大東西。
**回聲重演排在最後一位**（v1.2 · P20a），所以它的半徑就是最小的那一階（3.2）：誰要用 `E` 都讓，讓完才輪到它。
它是純氛圍層 —— 不開面板、不給 XP、不進圖鑑、**一個存檔欄都沒有新增**（那 12 處都是資料，看過沒看過不記帳）。
**守門者是這條慣例唯一的例外，而且是往嚴的方向例外**（v1.2 · P18）：他的半徑 3.2 比排在他後面的石碑（4.6）小，
但他的擺位規則要求**互動圈與每一層都不重疊**（連石座也是，`test:rubric` 逐層量）——
那條「半徑遞減」的慣例要守的事情（不要互相蓋掉）在他身上是用更嚴的方式成立的，所以他永遠不會蓋掉誰、也不會被誰蓋掉。
3.2 是**量出來**的：護欄崗是 12 片土地裡最擠的一片，照守夜人那一階（4.6）全區**一個落點都不剩**
（4.0、3.8 也是）；收到 3.2 才有 6 個，而且 6 個全部在門邊（見 §4.15 那張表）。
**守夜人（4.6）與石碑同一階**，由仲裁順序分先後（同刻文小語與殘頁那一對）——
他插在濁靈與石碑之間，是因為「一個會回答你的人」比「一塊碑」更該先被按到，
但他永遠不該蓋掉一座石座或一隻濁靈（那才是這一區的正事）。
殘頁與刻文小語同半徑（3.8）—— 它們是同一種尺度的東西，所以由**仲裁順序**分先後（刻文先），而不是靠半徑；
兩層之間的擺位規則要求互動圈不重疊（≥ 7.6，見 §4.9），實際上不會同時在範圍內。
濁靈（小 5.5／大 6.0）夾在石座（6.5）與石碑（4.6）之間，所以牠的擺放規則要求**互動圈不重疊**（§4.8），不靠仲裁補救。
**大濁靈對石座是唯一的例外**：那一條改成「不准站進人家的地盤」（≥ 9.0），理由與守夜人同一個（142 座石座把 12 片土地填滿了），
而且另外用「按得到牠的方向」逐點量——見 §4.8。

**同一層裡有兩件同時在範圍內時，用「面向」排名，不是用距離。**
器物擺得比其他層密，站在兩件中間是會發生的事；純比距離的話，會由零點幾公尺的差距
決定按到哪一個，但玩家看的是他**面向哪裡**。作法是把鏡頭的水平前方向丟進排名式
（`分數 = 距離 × (1 − 0.35 × 面向點積)`），沒有給面向時退回純距離。

### 3.3 分幕揭示（staged reveal）

**一次只有一件事擁有畫面。** 這是整個專案最重要的介面規則。

- 主控台是**四幕**：①委託（只有題目，零連結）②指引（神諭刻文＋出處）③刻印（石碑＋即時對照）④手印（儀式）。
- 走過的幕隨時回得去，沒走過的按不下去。轉場像「鏡頭切換」：切掉舊的，新的用 `.reveal .d1/.d2/.d3` 分批浮出。
- 小對話窗（石碑、刻文小語）也照同一個節拍：一行一行浮現，最後才出現「記下了 +N XP」。
- 序章一拍最多兩句。

### 3.3b 十一種答題方式（作答文法，Phase 27 起）

第三幕的舞台上一次只有**一種**題型。是哪一種由 `flows.json` 的 `kind` 決定，
**沒寫就是 `choice`** —— 換句話說，新增題型永遠不會動到舊關卡（護欄 7）。

| `kind` | 世界裡叫什麼 | 玩家在做什麼 | 教得了什麼是選擇題教不了的 | 用在哪 |
|---|---|---|---|---|
| `choice` | 石碑刻印 | 一段一段從 2–3 個選項裡挑，選對就刻上去 | 「這一段該寫什麼」 | 24 關（預設） |
| `order` | 排序刻印 | 石版已經刻好，用拖曳或鍵盤把它們**排順** | 「這幾段該照什麼次序」 | 長卷之塔（資料在前、問題在後）／優先序階梯（規則區最上面） |
| `workshop` | 神諭工坊 | 挑工具 → 把值石填進參數格 → 排呼叫順序 → 立規矩 | 工具使用 / function calling：工具是**宣告**出來的、參數是**填**進去的、呼叫**有相依順序** | 派工型神廟（稱呼可由 `workshop.labels` 覆寫） |
| `fix` | 改碑 | 預填的弱稿上，畫線的句子攤開替代寫法 | 「這一句該怎麼改」——包含「整句拿掉」 | 撰寫基本功起的多座 |
| `spot` | 點碑 | 點出有問題的句子（多選、不扣分） | 「哪幾句其實是病灶」 | 找碴型神廟 |
| `induct` | 推規碑 | 牆上的對照一組一組浮出來，先猜規律再刻 | 範例的**規律**本身（第四例真的在驗證） | 示範與推理 |
| `tradeoff` | 雙面碑 | 一張卡兩面，兩面都前進、兩面都收到誠實判詞 | 取捨：**換一張卡就翻面**，不是「哪一面比較好」 | 取捨型神廟 |
| `constraint` | 合尺 | 把石片放上檯，尺當場亮或暗（亮的依據是真的檢查器） | 「湊得齊這幾把尺嗎」——完全資訊，而且全選必有一把暗 | 約束滿足型神廟 |
| `multi` | 兩輪刻印 | 刻完第一輪 → 看一段**遊戲自撰**的神諭回話 → 第二輪修它 | 迭代：草稿→審查→改寫、換句話說再跑、把壞輸出連同 prompt 交回去 | 6 座（Phase G） |
| `sim` | 轉鈕 | 轉一格三檔的旋鈕，神諭的回話跟著換（**離線預寫樣本**） | 旋鈕類技巧唯一有體感的形狀：思考火力、亂度、動作預算「轉下去會發生什麼」 | 4 座（Phase H 三座 ＋ Phase J1 一座） |
| `reverse` | 拆碑 | 一份**已經寫好的**委託被拆成幾塊，一塊一塊說出「它為什麼在這裡」 | 複習的形狀：把圖鑑從被動的收集冊翻成一次**主動回憶** | 1 座（Phase J1） |

三種共用的硬規則（新增第四種題型時逐條照做）：

1. **結尾一模一樣**：都住在第三幕，做完自動切到第四幕，都是同一隻手掌印按住 600ms
   （`src/prompt/palm.js`，計時器判定不看影格）。
2. **送出的是同一段文字、走同一支離線引擎**（護欄 3）。做對組出來的那段字
   ＝該關 `challenges.json` 的 `sample`，丟進真的 rubric **每一條檢查都滿分**（測試強制驗證）。
3. **不會失敗**（§3.5）：挑錯工具 / 放錯值石 / 立錯規矩，一律「工坊不收」＋ 就地長出一句白話教學，
   不扣分、不前進、不跳失敗面板；石版排錯時**手掌印根本不出現** ——
   「送出之後才發現排錯」這件事在設計上不存在。
4. **鍵盤走得完**（§3.1 鐵則）。兩種新題型共用同一套「拿起 → 移動 → 放下」文法：

   | 鍵 | 排序刻印 | 神諭工坊 |
   |---|---|---|
   | `↑` `↓` `←` `→` | 沒拿東西＝移動焦點；拿著石版＝搬它 | 在工具牌 / 值石 / 參數格 / 規矩之間移動焦點 |
   | `Enter`（在石版 / 值石上） | 拿起來 · 再按一次放下 | 拿起值石（焦點自動跳到下一個空格）／收下工具／立規矩 |
   | `Enter`（在參數格上） | — | 手上有值石就放下；沒有就把格子裡的收回托盤 |
   | `Home` / `End` | 搬到最前 / 最後 | — |
   | `1` `2` `3` | — | 選規矩 |
   | `Esc` | 拿著東西時＝放回原地（第二次才收起面板） | 同左 |
   | 按住 `Enter` | 手掌印 | 手掌印 |

   每一次拿起 / 搬動 / 放下都用 `aria-live` 講出來（「拿起：問題那一行。第 1 片，共 3 片。」）。
5. **滑鼠是加分不是唯一**：石版與值石都拖得動（pointer 事件，不用 HTML5 drag），
   `prefers-reduced-motion` 下拖曳與搬動變成瞬間就位（關掉的是「動」，不是「回應」）。
6. **回饋沿用同一組顏色**：排對 / 收下的東西才轉暖金（成就熱點），沒排對的安靜留著，
   不用紅字、不用叉叉。

**兩輪刻印（`multi`）另外三條，違反其中任何一條就是說謊或會串輪**（Phase G）：

7. **中間那一段輸出是遊戲自己寫的，不是模型跑出來的。**
   資料層強制 `authored: "game"`，畫面上一定掛一顆 ⓘ 明講這件事（`AUTHORED_NOTE`）。
   絕不做成「看起來像真的模型回話」——那是騙人（§3.4 的界線一樣適用）。
   回話卡本身**不放連結**（教學與出處仍然只在第二幕與圖鑑）。
8. **輪次是 `flow.slots` 的一個切法，不是另一份資料。**
   每一輪只宣告「我吃幾段」（`count`），段落全部住在 `flow.slots` 裡，
   而且 `sum(count) === slots.length`。這條契約買到三件事：退回石碑刻印時玩家刻出來的字
   **一模一樣**；**不可能串錯輪次**（輪次只是同一條序列上的區間）；測試可以不知道題型就驗「全部選對＝S」。
9. **輪次狀態只活在記憶體裡，而且誠實。**
   幕與幕之間切換、引導式 ↔ 自由書寫切換 → 輪次原封不動；
   重開這一關（`load()`）→ 一律回到第一輪；**重新整理頁面 → 這一關從第一輪重來**
   （這是「重新開始」，不是「丟失」——其他八種題型也一樣，不為了 multi 破例做落地存檔）。
   另外：**第二輪一定要真的在加分**——只刻完第一輪不得已經滿分（playtest 逐關守）。

**轉鈕（`sim`）另外三條**（Phase H）：

10. **每一段回話都是遊戲自己寫的。** 樣本住在 `src/data/sim-samples.json`（`authored: "game"`），
    畫面上永遠掛一顆 ⓘ 明講「這些輸出是遊戲預先寫好的示範，不是真的模型跑出來的結果」。
    **這個題型絕不呼叫任何服務** —— 斷網照樣轉得動（護欄 3；`sim.js` 裡連一個網址都沒有，測試會掃）。
11. **旋鈕的行為不是普遍真理。** 每一個旋鈕都要寫得出 `condition`（在哪一台機器、哪一個時間點成立），
    而且那句話永遠跟樣本一起顯示在旋鈕面上。「這台機器不讓你設 temperature」是那一台在那個版本的事實。
12. **轉過每一檔才刻得了字。** 觀察是這一關的內容，不是可以跳過的過場（與推規碑「想通才給刻」同一個文法）；
    而且**轉旋鈕不會改變被評分的那段文字** —— 送出去的永遠只有刻在碑上的字。
    一個旋鈕**剛好三檔**（schema 硬性約束：太少看不出趨勢，太多變成翻頁）。

**拆碑（`reverse`）另外三條**（Phase J1）：

13. **牆上那一份是「別人寫好的」，不是題目。** 它本來就是一段好 prompt ——
    這一關的內容是**讀懂它**，不是修它。所以標名字這件事**不會進到被評分的文字裡**：
    `reverse.js` 的 `text` 只回刻寫台的內容，拆完之後玩家還是要自己刻一段（護欄 3 未動）。
14. **一定要有一片誘餌名牌。** 至少一片名牌從頭到尾都不是正解 —— 那就是這一關的「轉」
    （看起來很像，其實不是）。而且**每一片名牌都寫得出貼錯時要教什麼**（資料層強制）。
15. **`Esc` 是「拆回來」不是「關面板」**（與改碑同一個文法）：
    先把最後貼上去的那一塊拆回來、焦點回到它身上並用 `aria-live` 講出來；
    沒有東西可以拆了才冒泡出去收面板。

### 3.3d 應用關（試煉）的作答文法（課程 v2 · Phase J2）

每片土地的地標腳下站著一座**試煉**（12 座）。它與教學神廟是兩種東西：

1. **它不教任何新技巧 —— 所以第二幕整幕不存在。**
   幕的序列是 **① 委託 → ③ 刻印 → ④ 手印**（`console.js` 的 `actOrder()` 是單一真相）。
   指示器上**不畫**第二幕那一塊封印石（它不是「被鎖住」，是這一關根本沒有那一幕），
   `Alt + 2` 也不會有反應；第一幕的出口寫的是「接下試煉 →」不是「聆聽指引 →」。
2. **畫面上不放官方連結。** 刻痕對照、結果面板、幕與幕之間一個 `http` 都不准出現。
   資料層仍然留著 `source`（沿用既有兩座應用關的做法），但它只是溯源用，不上畫面。
3. **rubric 是開關卡的當下組出來的。** 資料層宣告一份**候選列**（標 `candidate: true`、
   每一列掛一條**這一片土地的** v2 技能 `skillId`），開關卡時用 `knowsSkill()` 過濾 ——
   **沒學過的不列**（P9：畫面上列出來的每一條，你都已經被教過了）。
   地基那一列（`assignsTask`，0.5 分）永遠都在，它是每一份委託的地板，不是技巧。
4. **門檻公式**（`src/challenges/trial.js` 的 `trialPass()`，runtime 與資料層用同一支）：

   ```
   pass = max(2, round(入選權重總和 × 0.5 × 2) / 2)
   ```

   也就是「入選權重總和的 50%，四捨五入到 0.5 分，下限 2 分」。
   資料層存的 `pass` 是「全部候選都入選」時的值（檔案層的測試看它）。
5. **絕不軟鎖。** 已學的候選少於 2 條時，照資料層順序補到 2 條，並在對照表底下
   誠實寫出「其中 N 條你還沒學過，先看一眼再寫」。
   「打得開卻永遠過不了」與「打不開」這兩種死路都不存在（測試逐條驗四種情境）。
6. **鷹架撤除到底。** 試煉**一片快速填入都沒有**（C5 的最後一格）；
   其中三座（議會信使 / 檔案庫封印 / 只剩一句話）連石碑都沒有 ——
   它們在 `flows.json` 裡**沒有條目**，走的是既有的自由書寫路徑。
7. **通過 → 那片土地的印記入袋**（`seals[]`，12 枚，冪等）。
   印記不給 XP 以外的東西、**不是任何解鎖條件**，只是「這片土地你走完了」的憑證。

### 3.3e 大師層印記（課程 v2 · Phase J2 · C9：可選、永不擋路）

| 名稱 | 條件 | 存檔欄位 |
|---|---|---|
| 無筆之印 ✒ | 一座**教學神廟**：沒用快速填入／技巧積木、沒開提示球、**這一關的範例從來沒被翻開過**、刻印一次都沒被退回，而且**開關卡以來的第一次呈遞**就拿到 S | `penlessSeals[]` |
| 默寫之印 ✍ | 一座**教學神廟**：用**自由書寫模式**拿到 S（同樣要求範例從沒被翻開過） | `scribeSeals[]` |
| 一區純手 | 一區的教學神廟全部拿到無筆之印（12 枚） | 由 `penlessSeals` 推算 |
| 分歧之證 | 分歧之廳 9 座全通 ＋ 終章試煉 S | 由 `bestGrades` 推算 |

三條硬規則：

- **應用關不發大師層印記** —— 它本來就是「把學過的用出來」。
- **作弊面在「範例」**：翻開過就寫進 `samplesSeen[]`，**永久**記著；
  關掉重開再拿 S 也不算「沒看範例」。判定寫在 `progression.js` 的 `masterSealFor()`，
  console 只負責誠實回報「這一次用了什麼輔助」。沒給判定材料時**一律不發**（寧可漏發不可誤發）。
- **它們不是任何東西的解鎖條件**，也不影響 XP、圖鑑、徽章與既有 finale
  （68→130 全收集 ＋ **四廠**徽章，一格未改；新廠只能是支線 —— 護欄 7）。

### 3.3c 多模態的誠實界線（課程 v2 · Phase I）

觀象臺教的是「怎麼寫看圖、生圖、生影片、說話的 prompt」。
**遊戲並沒有真的看過那張圖，也沒有真的生成任何東西** —— 它評的仍然只有一件事：
你寫的那段文字有沒有把該講的講出來（指到哪一塊、保留前一步、點名分鏡要素、
用標點做停頓、指名框架與範圍）。這條界線寫成三條規則：

1. **素材是抄寫人寫下來的文字**，不是圖檔。世界裡沒有相機，只有紙 ——
   「左下角有一塊木牌，牌上三行小字」這種寫法既誠實又便宜，而且玩家讀得懂。
2. **這一區不引進任何媒體資產**：關卡與流程的資料層不得出現 `.png` / `.mp4` / `.m4a`
   之類的檔名，畫面上也不得出現 `<img>` / `<video>` / `<audio>`（rubric ＋ e2e 各守一次）。
   之後若真的要放圖，就要逐檔登記 `public/LICENSE.md`，而且**不准走外部 CDN**。
3. **零外部請求**：整段玩下來不會向任何外部網域要東西（e2e 用
   `performance.getEntriesByType('resource')` 量過）。護欄 3 在這一區沒有例外。

### 3.4 換皮但不說謊（護欄 2 的實作方式）

世界觀的說法可以蓋在教學上面，**但每一次都要接得回真的東西**：

| 世界的說法 | 實際是什麼 | 規則 |
|---|---|---|
| 神諭 | 大型語言模型 | — |
| 神諭刻文 | 遊戲自撰的白話教學（coach.json） | 一定同時顯示「這一條對應的技巧名」 |
| **神諭原典** | 各廠官方文件 | **後面一定接得出真實文件名 ＋ 可點的 https 連結，而且要直接跳到被引用的那一節**（見下） |
| 抄寫人寫的 | 中文譯寫層（curriculum-zh / builder-zh / flows） | 資料檔標 `authored: "game"`，與官方欄位分開存放 |
| 石碑刻印 | 選擇建構式的 prompt 組裝 | 送出的是同一段文字、走同一支離線引擎 |
| **四部原典／四宿**（v1.2 · P08） | 四家公開的官方文件（`curriculum.json` 的 `vendors`） | 星圖只畫程序化星點與連線：**不畫任何標誌、不用任何品牌色**；世界層文字零公司名；真名只在**星圖下方那一行與成就頁**做出處性使用，且一定伴隨免責句 |

**可點的官方出處連結永遠留在畫面上**，不准收進 ⓘ、不准收進摺頁。
解釋世界觀說法的旁白可以收進 ⓘ（`infoTip()`）。

#### 世界層零公司名（v1.2 · P08）

世界裡的「話」——濁靈、殘頁、刻文小語、祕密、器物、石碑（含回信碑）、故事小景、HUD、回聲——
**一個公司名都不准出現**（`OpenAI`／`Anthropic`／`Google`／`xAI`／`GPT`／`Claude`／`Gemini`／`Grok`）。
准出現的地方只有三處，而且都是**出處性使用**：圖鑑的出處列、四宿星圖下方那一行、隱藏成就頁。
星圖那一行與成就頁都要帶著同一句免責句：**「本遊戲與這四家沒有隸屬或背書關係。」**
`scripts/test-rubric.mjs` 有一張很短的白名單守著這條線——要在新檔案用真名，得先在那張表上簽名。
（依據：OpenAI Brand Guidelines、Anthropic Trademark Guidelines、Google Brand Resource Center。）

#### 出處要深連結到「被引用的那一節」

點「神諭原典」落在頁面最上面，等於把找那一句的工作丟回給玩家 —— 附出處只做了一半。
所以每一個顯示得到的官方連結都要帶著片段（`#章節 id` 或 W3C 文字片段 `#:~:text=`）。三條硬規則：

1. **不准臆造 anchor**。每一個片段都要在稽核當天實際抓下來的頁面上驗證過：
   `heading` 型的 id 真的在 HTML 裡；`fragment` 型的文字片段在頁面文字裡**唯一**命中
   （片段 ≤ 8 個詞、取頁面上的原字，不是正規化過的版本）。找不到唯一落點就**誠實留在頁面層**。
2. **只准動片段**。`curriculum.json` 的官方網址一個位元組都不能動（護欄 2），
   舊 68 條走顯示層疊加 `src/data/source-anchors.json`（`anchored` 與 `url` 只准差一個片段，測試強制驗證）；
   課程 v2 的 130 條技能是遊戲自撰資料層，`skill-codex-v2.json` 就地升級並在每一列標 `anchor`。
3. **每一列都要表態**。有深連結標定位方式，沒有的標 `anchor: "none"` ＋ `anchorNote` 寫理由；
   覆蓋率（含誠實的 `none` 數）記在 `scripts/expected-counts.json` 的 `sourceAnchors`，退步要有人簽名。

新增或搬動出處時照同一套走，稽核方法與逐網址結果在 [`docs/design/source-anchor-audit.md`](./docs/design/source-anchor-audit.md)。

### 3.5 不會失敗

- 石碑刻印選錯 → 石碑不收、就地長出一句白話教學、**不扣分、不失敗、不前進**。重選就好。
- 排序刻印排錯 → 手掌印不出現（送不出去），石版隨便你搬到對為止；神諭工坊挑錯 / 放錯 → 同一套「不收 ＋ 教你」。
- 關卡沒過 → 柔和的音效、逐條指出缺什麼、附出處。不是懲罰。
- 祕密與刻文小語 → 錯過了就是錯過了，但沒有任何懲罰，回頭永遠找得到。
- 濁靈沒聽懂 → 殼還在、牠還盯著你；已經說清楚的那幾層**記在存檔裡、永不清零**，下次接著補。沒有血量、沒有追逐、不掉東西、不傳送、不倒數。這一次的結果卡照舊講這一次（印章、量表、缺什麼）；牠「累積聽懂了多少」另外一行說。

### 3.5b 分享 ＝ 圖 ＋ 一段話（Phase 28，Phase 31 修訂，2026-08 站長修訂）

玩家按下 `S` 分享的時候，帶出去的東西**只有兩樣**：

1. **那張卡的圖**（1200×630，在玩家自己的裝置上畫出來的）
2. **一段話**（預設是世界的說法 ＋ 品牌落款 ＋ 最後一行的網址；框裡可以改成自己想說的）

**那段話的最後一行是遊戲的網址** `https://garyhsieh.com/promptasy`（2026-08 站長決定，
取代 Phase 28 訂下的「那段話裡不准有網址」）。理由：卡片已經是主體，
看到的人得走得過來才有下一個玩家。規矩因此變成三條——

- 網址**自己佔一行**、接在品牌落款後面，不跟世界的說法擠在一起。
- 網址是**落款，不是替代品**：圖仍然一定要跟著走（見下方鐵則），
  任何「只送得出連結」的入口照樣不准做。
- 全遊戲只有 `SHARE_URL` 一個網址常數，換網域只改那一個地方。

#### 鐵則：**主角一定是那張圖**

Phase 24 曾經排了一列「分享到 ⭕⭕」的入口，它們把**連結**丟過去、圖留在原地，
所以 Phase 28 整排拿掉；Phase 31 放回來，但要求每一顆都得自己把圖備好。

**2026-08-03 站長定稿**（現行規則）：

> **面板的主角是「複製圖＋文」**（同一份剪貼簿裡有圖也有話）＋「下載」保底 ——
> 這兩條路一定帶得走那張圖，而且永遠在畫面上。
> 平台那一排是**捷徑**，它們只負責把玩家**送到已經登入的撰寫框**並把話備好；
> 帶不帶得動圖由對方決定，提示要**老實講出接下來要按的那一下**。

現行的兩顆（其餘一律不做）：

| 平台 | 開哪一頁 | 那段話 | 圖 |
|---|---|---|---|
| Threads | `threads.com/intent/post?text=`（官方入口，`.net` 會 301 轉過來） | **網址參數直接帶進撰寫框** | 玩家用「複製圖＋文」或「下載」自己帶 |
| Facebook | `facebook.com/sharer/sharer.php?u=`（無需 app_id 就會開貼文對話框，帶站網址與 og 預覽卡） | **先複製進剪貼簿**（FB 政策禁止程式預填），提示寫明按 `Ctrl+V` | 同上 |

**Instagram 不做**：網頁版沒有任何撰寫入口 —— 不做假按鈕、不編假的深連結。
**系統分享鈕（`navigator.share`）已移除**：它在桌機幾乎不存在，留著只是讓主角換來換去。

#### 一次貼上只帶得走一種東西

W3C 的剪貼簿規格**沒有定義優先順序**——同一份剪貼簿裡同時有圖和字時，
貼到哪裡、貼出什麼由**對方的撰寫框自己決定**（幾乎都是「有圖就貼圖」）。
所以一顆平台捷徑一次只備**一種**東西：Threads 那顆把話帶進網址（剪貼簿一個字都不碰，
不會蓋掉玩家自己複製的東西），Facebook 那顆只把話放進剪貼簿。
**不要設計成「一次貼上就圖文都到」——那是猜的，不是真的。**

主角那顆「複製圖＋文」是刻意的例外：它一次放兩種型別，給的是**聊天室這種收得下兩種**的地方；
收不下的一端會自己挑一種，所以它也永遠不會變成死路（複製失敗就指回「下載」）。

#### 版面：一排圖示，說明走 `aria-label`

那一排（Threads / Facebook / 複製圖＋文）與「下載」全部是**純圖示**（行內 SVG、零圖檔）：
牌面上不寫字、不印鍵帽，名字走 `title`、「按下去會發生什麼」走 `aria-label`。
方向鍵在同一排裡走得完（`rovingList`），`Enter` 就是按下去。

### 3.7 術語小卡（Phase 35）—— 讀得到但不擋路的那一層

畫面上難免會出現 `prompt` / `JSON` / `temperature` 這類英文名詞。它們在課程層是有出處、
有教學的（第二幕的神諭刻文、圖鑑）；但當它只是**順口出現在情境或委託裡**的時候，
一般人會卡在那個字上，而我們又不想為了一個名詞把整段話撐開。

作法：那個字底下多一條很淡的**虛線**（`.gloss`，冷星光色，`cursor: help`），
滑上去（或點一下）浮出一張小卡：**白話一句話 ／ 用途 ／ 一個小到可以照抄的例子**。

| 規則 | 內容 |
|---|---|
| **這一層不教技巧** | 資料在 `src/data/glossary.json`（`authored: "game"`），**不准放連結、不准有 `source` / `teaches`、不准說「官方」**。它跟 `coach.json` 同性質：扶手，不是課本（護欄 2）。 |
| **只標玩家在「讀」的字** | 輸入框、按鈕、鍵帽、`<summary>`、標題、官方出處連結（`.src`）一律不碰。標進按鈕會把可按的東西弄髒，標進 `textarea` 更是直接毀掉玩家打的字。 |
| **一個面板一個字只標第一次** | 整段話裡每個 `prompt` 都畫線＝雜訊。圖鑑以「一條技巧」為一個面板。 |
| **標記不改文字** | 只是把那幾個字包進一層 `<span>`，`textContent` 一個位元組都不變。 |
| **標記的地方** | 第一幕（委託／情境／素材）、第二幕（神諭刻文）、提示框、圖鑑的每一條技巧。呼叫端在重繪之後叫一次 `glossary.annotate(el)`；**沒有 MutationObserver、沒有輪詢**。 |
| **小卡掛在 `<body>` 上** | 面板有 `overflow`，掛在裡面會被裁掉。`position: fixed` ＋ 上下翻面 ＋ 最後一道夾在畫面內的保險。 |
| **`Esc` 先收小卡** | 不准順手把整個面板也關掉（`document` 的 capture 階段 ＋ `stopPropagation`）。 |

**鍵盤（§3.1 的鐵則怎麼交代）**：標記**刻意不進 Tab 順序**。
一段情境可能有五、六個標記，全部塞進焦點鏈會把「開一層 → 焦點落在內容區第一個可按的東西」
（Phase 23）整個打亂——那是玩家每一關都要走的路，不能為了一個彩蛋讓它變慢。
可以這樣取捨的前提是：**這一層沒有任何進度依賴它**。同一個名詞在圖鑑裡有完整的中文說明
與官方出處，那條路純鍵盤走得到。滑鼠 hover 與觸控點按都開得起來。

> 新增這種「讀得到但不擋路」的層時照這條問一次：**沒有它，純鍵盤的人是不是仍然什麼都不缺？**
> 答案是「會缺」的話，就不准犧牲 Tab 順序，要另外設計鍵盤路徑。

### 3.6 用詞

**禁止在畫面上出現的字**：送出評分、按鈕、面板、localStorage、bloom、後製、Web Audio、API key、rubric、check、debug。
設定頁講的是「畫面效果全開」「進度會自動存在這台裝置上」，不是技術規格。

---

### 3.8 今日三事：提議，不是任務（v1.2 · P23）

玩家離開一天再回來，需要一個「今天可以做什麼」的入口。這一層就是那個入口 ——
但它是**提議**，不是每日任務。整格的設計壓力全部集中在「它不做什麼」上：

- **沒有倒數、沒有期限、沒有過期。** 沒做完不會少一樣東西，畫面上不會有一個字提起這件事。
- **沒有連著幾天的紀錄。** 沒有「連續 N 天」、沒有「斷了」、沒有補簽。
- **換日只是換三個提議。** 換日碰得到的只有 `daily` 那三格，一格進度都不刪
  （`test:rubric` 拿整份存檔的快照逐值比對）。
- **可以在設定裡關掉。** 關掉之後那一區整塊不出現（不是變灰、不是留一個空標題），
  而且存檔那三格永遠停在預設值 —— 與「從來沒有這個功能」逐值相同。
- **提議要真的可達。** 只從已經開了的土地挑；已經做完的（拿過 S 的、找到的線索、
  今天走過的土地）不再提。提一件走不到的事等於在騙人。
- **換日看的是玩家自己的今天。** 本地時間的年月日，不是 UTC。

**用語紀律**（與 §1.6 濁靈那一套同源：這個世界不催人）：
`過期／逾期／期限／倒數／截止／連續／錯過／失敗／懲罰／限時` 這幾個字
**一個都不准出現在這一層**；「任務」只准出現在「不是任務」那一句裡。
`test:rubric` 剝掉註解之後掃原始碼與**畫出來的 HTML** 兩面，e2e 再掃一次真的畫面。

三種提議：**重解一關拿 S**（已經過了、還不是 S 的那一關）、
**找一處還沒找到的線索**（祕境／殘頁／刻文 —— 只說種類與土地，**不說是哪一處**，
同 §傳聞那條不劇透的規矩）、**走一趟某一片土地**。
規則住在 `src/progression/daily.js`（純函式），畫面住在 `src/ui/daily.js`（純函式），
存檔那三格住在 `save.js`。

### 3.9 「全部完成」只能有一個定義（v1.2 · P23）

P22 的終局門檻是「**130 條技法全收 ＋ 四宿全亮**」，可是隱藏成就一直算的是
「68 條舊技巧全收 ＋ 四廠徽章」—— 兩個「全部完成」不是同一件事，於是小祠開口
與成就達成會在不同的時間點發生。P23 把它們對齊：`hiddenAchievement()` **直接呼叫**
`turning.js` 的 `shrineOpen()`，兩邊是同一個函式，不可能對不上。

對齊會動到既有存檔，所以配一條鐵則：**已經達成過的不准倒退**。做法是兩個旗標：

- `achievementDone` ——「達成過了」。**只加不減**：達成的那一刻記上（落盤時問一次），
  之後 `complete` 永遠是真。這一條同時擋住未來 —— **課程還會長**，門檻跟著長；
  沒有這一格的話，今天達成的人明天會被新內容打回「還沒達成」。
  （這與 P22 那一課同源：殼散掉了就不准因為門檻變了又長回來。）
- `p23Aligned` —— 對齊那一次性遷移的憑證。開機時**用舊尺量最後一次**
  （68 條全收 ＋ 四廠徽章，或看過成就畫面），量到就記進 `achievementDone`，
  然後插上這個旗標；插上之後**不再回頭用舊尺量**。沒有它的話「舊尺」會永遠有效，
  那就不叫對齊了。

---

## 四、場景敘事（Environmental Storytelling）

四個手法，按「一眼看到 → 走過去 → 蹲下來看」的順序排：

### 4.1 地標（landmark / weenie）

每區一個、高 21–27 公尺、從中央高原就看得到。**周圍強制留白 14–16 公尺**。
（量器坊的刻度之柱：高 24、留白 15，柱身刻度與柱頂那把尺**全部是自發光**，零實體光源。）
地標的功能是導航與慾望：看到它就想走過去，走到它就看得到下一個。

### 4.2 故事小景（vignette / tableau）

**成組擺放，不要均勻灑裝飾。** 兩張對坐的椅子 ＋ 沒收的茶具 > 二十顆隨機石頭。

- 目前 16 組，每區 3–4 組。每一組要能回答 **who / what / why**：誰用的、做什麼、為什麼留在這。
- 每一組都是「一件沒做完的事」。做完的事沒有故事。
- 小景**不在必經路線上**——可以錯過，找到才有意義。

### 4.3 走出來的路

路是**用頂點色染在地形上的**，只改顏色、不動高度場，所以可行走性完全不受影響。
路網連接石座、橋、以及通往小景的岔路。虛空沒有路。

### 4.4 會回應的東西（Phase 22）

**讓世界注意到你走過去了。** 六種，全部程序生成、不新增光源：

| 種類 | 反應 | 進場半徑 |
|---|---|---|
| 風鈴架 `chime` | 晃起來 ＋ 一聲金屬泛音（音高隨機但不重複） | 3.2 |
| 光菇圈 `glowcap` | 一朵一朵依序亮起，離開再依序暗回去（純光，無聲） | 4.4 |
| 音石列 `songstone` | 每顆石頭一個音，沿著走就走出一段五聲音階旋律 | 每顆 1.75 |
| 靜水盤 `ripple` | 一圈水紋盪開 ＋ 一聲低頻 | r + 1.5 |
| 守望的小獸 `spirit` | 遠遠看著你，太近就竄開，約 9 秒後回來 | 4.2 |
| 螢火群 `moths` | 往外散開，站著不動就慢慢聚回來 | 3.0 |

**外交式導向：螢火群會指路（v1.2 · P19）。**
世界一直有一個「下一個建議去處」（指南針的針、守望石的光、HUD 那一行），
可是**站在世界裡的東西**從來沒有指過路。所以讓已經到處都是的螢火群整體偏向那個方向——
**不是箭頭，是一群東西剛好都往那邊飄**。規則五條，改動之前先讀：

1. **它不是第七種反應物**，是既有 `moths` 的一個偏向量：0 新粒子、0 新光源、0 新碰撞體。
2. **分成兩段，而且分得開**：`MOTH_GUIDE_LEAN`（整團往目標側挪 0.9 m，**靜態**）
   ＋ `MOTH_GUIDE_SPAN`（沿同一方向前後流，平均值 0，**會動**）。
   `prefers-reduced-motion` 留下前者、收掉後者——拿掉的是動，不是資訊（§2.4）。
3. **關得掉**（設定頁「螢火指路」，存檔 `settings.guides`，預設開）。
   關掉之後每一幀與 P19 之前**逐值相同**（`test:rubric` 拿重心逐值比對，不是「差不多」）。
4. **低畫質整層關掉**（同石座演出）；畫質是**當下**問的，切換不必重建世界。
5. **目標與指南針同一套** `objectiveTarget()`，也同一個節拍（每 0.5 秒重算一次）——
   絕不允許出現第二套「下一個目標」的算法。每幀只讀兩個純量，**零每幀配置**。

擺法：**成組、靠近路與小景**，目標是每走 20–30 公尺遇到一次，中間要有真正的安靜。
兩個反應之間至少 11 公尺——離太近會同時響，聲音會糊掉。
**先量再放（v1.2 · P06）**：`node scripts/pacing-audit.mjs` 沿路網每 5 m 取樣，印每區「距最近微觸／中景／石座／地標」
的直方圖（0–15／15–30／30–45／>45 m）與 >45 m 死區段（encounter／micro／mid 三種口徑）；`test:rubric` 會印同一份當軟警告。
新增或搬動反應物、小景、刻文之前先跑一次，之後只准變少。

### 4.5 藏起來的地方（secrets）

**12 處**（v1.2 · P15 由 4 處補齊），散在 12 片土地裡的 10 片。
一律在**地標的背面**或地圖角落——從中央高原看不到的那一側，離主動線 8 公尺以上。
走進去就算找到（不用按 `E`：好奇心不該還要學一個鍵）。純風味，不教技巧：
資料層不准有 `source` / `teaches` / `techniqueId`（護欄 2，`test:rubric` 逐條驗）。
圖鑑有一章「**秘境**」收藏它們——找到的收進來，沒找到的只留一行 tell。

**每一處都有一種 tell（v1.2 · P15）**——「藏起來」不等於「碰運氣」：
世界一定先給你一點什麼，你才決定要不要走過去。三種各佔一部分（每種至少 3 處）：

| tell | 世界怎麼先告訴你 | 實作 |
|---|---|---|
| **不對的東西** `odd` | 附近有一小塊顏色與這片土地格格不入，眼角先看到它 | 資料層 `oddAccent`；`test:rubric` 驗它與該區主色**色相差 ≥ 60°**，而且那塊碎片真的蓋在場景裡 |
| **聲音先到** `sound` | 還沒看到就先聽到一點很細的聲音 | 發現半徑之外再套一圈 `SECRET_TELL_RATIO`（1.8 倍）的外圈，走進去響一次；測試逐處驗**響在找到之前** |
| **高處** `high` | 它躺在高台的頂面上——站在地上搆不到，也看不到 | 腳離地要 ≥ `SECRET_HIGH_REACH`（1.4 公尺）；**那是一個固定的常數，不是「那座高台的高度」**——門檻跟著高台走的話，把高台壓矮之後斷言就永遠不會紅 |

**高處那四處刻意不進「互動圈不重疊」那組規則**：走路的人根本碰不到它們
（`reactive.js` 的 `reach` 那一關），它們腳下那一塊地的淨空由**腳下那座高台**自己守
（高台走的是與母題共用的同一份門檻）。`scripts/lib/screen-rules.mjs` 的
`interactionTargets()` 與 `test:rubric` 用的是同一條規則——留在表裡的話，
高台會與自己頂上的東西互相排斥，永遠擺不出來。

### 4.6 動得了的東西（handles，Phase 25）

**這片土地上的東西不能只有「能看」。** 抄寫人留在原地的器物你要真的碰得到：
掀得開、點得著、敲得響、推得動、坐得下。它們不推進主線 ——
**密度比獎勵重要**：走過去、按一下、它有反應，這件事本身就是回饋。

八種，各 2–4 件、共 22 件，五片土地都有（每區至少 3 種不同的）：

| 種類 | 世界裡是什麼 | 按 `E` 之後 | 動詞 | 一次性？ |
|---|---|---|---|---|
| 陶罐 `urn` | 裝碎刻痕的罐子 | 蓋子掀開、一縷光飄出；開一個小窗看罐底的字 | 掀開 | 是（開過就空了） |
| 火盆 `brazier` | 熄了很久的火 | 火重新亮起來，**而且一直亮著**（存檔記得） | 點火 | 是（之後可以撥火） |
| 響石 `gong` | 吊在木架上的石盤 | 盤子震、光環擴散、餘音最長的一支音 | 敲響 | **否**（可以一直敲） |
| 守望石 `watchstone` | 蹲著的石獸 | 睜眼、把頭轉向**你還沒解開的那座石座** | 觸碰 | 否 |
| 撈月池 `moonpool` | 淺水裡有幾條光 | 撈起一條，在半空散成碎星 | 撈一把 | 是 |
| 指路石 `signpost` | 岔路口的四塊牌子 | 開小窗，告訴你四個方向各有什麼 | 閱讀 | 否 |
| 絞盤 `capstan` | 轉不動的機關輪 | **按三次 `E`**，每次咬一格；轉完腳邊石蓋滑開 | 推動 | 是 |
| 長凳 `bench` | 坐著看天的地方 | 人真的坐上去、鏡頭往後退、世界安靜一會兒 | 坐下 | 否 |

> **「人真的坐上去」是這一列的字面意思，而且現在有兩條斷言在守（v1.2 · P22f）。**
> 站長回報「板凳坐起來位置都怪怪的」，量出來是兩件互相獨立的事，而且**七張凳子全中**
> —— 是程式的錯，不是某一筆資料擺歪：
>   · **人面向凳子的長邊**（朝向與長軸的內積實測 **1.000**）＝ 沿著凳子跨坐。
>     `seatLocal.face` 寫的是 `Math.PI / 2`，可是那個角度正好就是凳子的長軸方向；改成 `0`。
>   · **沒有任何程式把人抬到凳面上**：`player.teleport(x, z, face)` **不吃 y**，
>     人留在地面高度，而凳面在地面上方 **0.696** 公尺 —— 凳面就從人腰的位置穿過去。
>     角色的坐姿其實調得很準（髖 0.88 − `SEAT_DROP` 0.42 ＝ **0.44**，小腿 0.4 ＋ 腳 0.06
>     剛好落到地面），只是沒有人把「這張凳子的座面有多高」告訴它；補上 `seat.rise`
>     一路傳到 `character.rest(v, rise)`。修好之後腳離地 0.173 公尺（凳子本來就比坐姿高，
>     腿是懸著的），腳的高度遠低於凳面板底部 —— 不穿模。
>
> **這件事為什麼會出貨**：既有的測試驗了「有沒有擺出坐姿」（`restAmount`、膝蓋角度）
> 與「鏡頭有沒有退」，**卻沒有一條在驗「人有沒有真的在凳子上」** ——
> 姿勢對、位置錯，測試全綠。補的兩條就是位置與朝向，逐張凳子驗。

擺法與規矩：

- **兩件器物之間至少 14 公尺**，離石座 7、刻文小語 8、反應物件 8、石碑 7、地標 14、橋的主動線 8 以上。
  中間要走得到「什麼都沒有」的地方 —— 密度要有節奏。
- **`E` 是唯一的鍵。** 絞盤要推三下也是按三次 `E`，不發明「按住」或第二個鍵。
- **推到一半離開不算失敗**，只是回到原地重來（§3.5）。
- **多半不開窗**：只有陶罐（罐底的字）與指路石（四個方向）開一個很小的窗，
  其餘六種只在畫面上說一句話 —— 走在路上的小互動不該把整個世界蓋掉。
- **純風味（護欄 2）**：不教技巧、不掛 `techniqueId`、不放連結、資料層不准有 `source` / `teaches`。
- 第一次動它給 4 XP，之後怎麼玩都不再給（可以一直敲鑼，但不能刷分）。

### 4.7 三個高度階（scale tiers）

新增任何東西之前先決定它屬於哪一階，**而且三階之間要留得出空隙**——
中間尺寸的東西會把剪影糊成一團，讀不出遠近：

| 階 | 高度 | 例子 | 功能 |
|---|---|---|---|
| 雜物 | ≤ 2.6 公尺 | 器物、刻文小語、碎石、草叢 | 走到眼前才看得到，提供「這裡有人來過」 |
| 中景 | 3–8 公尺 | 小景、石碑、立石環、書架、齒輪 | 走過去的路上看得到，提供方向感 |
| 地標 | 21–27 公尺 | 五座地標 | 從中央高原就看得到，提供慾望 |

（加色混合的光不算剪影：石座的光柱 22 公尺、絞盤的光柱 4.6 公尺，它們是光不是物質。）

**登記的例外：遮擋帶 6–12 公尺（v1.2 · P11）。**
中景階的上限本來是 8 公尺，理由是「三階之間要留得出空隙」。
**遮擋帶（§4.10 的石脊）破這一條，因為它的工作不是被看，是擋。**
一座 26 公尺的地標站在 38 公尺外，要把它（連塔頂那顆光球）壓在背後，
帶頂的仰角必須蓋過塔頂的仰角 —— 算出來就是**離眼睛 13 公尺處要有 11 公尺以上**。
8 公尺擋不住，於是「翻過去才看到」這件事根本不會發生。
配套的三條規矩讓它不會把剪影糊掉：
① 一片土地最多 **2–3 道**（它是一道脊，不是一圈牆）；
② 每一道長 7–20 公尺、**厚 2–3 公尺**，**繞得過去**（端點外側 24 個方向至少 16 個走得到）；
③ 它不站在地標的留白圈裡（≥ 14–16 公尺），所以遠看時仍然只有一個高的東西。
`scripts/sightline-audit.mjs` 逐點量得出來，`test:rubric` 逐條守著。

**厚度為什麼有下限（v1.2 · P12）**：碰撞用的圓串半徑就是**半個厚度**（`solidSpan`），
薄的石脊要用更多、更小的圓才蓋得住同一段長度 —— P11 那版 1.4 公尺厚的帶，一道就吃掉 12 個碰撞體，
12 片土地鋪完會逼近 1,400 的硬上限。加厚到 2.4 之後一道帶 **4–5 個**。
**不准**反過來用「比石脊胖的圓」去省數量：那正是 `solidSpan` 當初要解掉的
「離它兩公尺外就撞到看不見的牆」。

---

### 4.8 濁靈的擺放（v1.2）

- **互動圈不重疊**：離石座 ≥ 12（6.5＋5.5）、離石碑 ≥ 10.1、離刻文小語 ≥ 9.3、離器物 ≥ 8.7、離其他不搶 `E` 的東西（反應物／地標／小景／祕密）≥ 4；離橋主動線 ≥ 4、離頸口與閘門 ≥ 8、離出生點 ≥ 7、離起始祭壇 ≥ 9；濁靈彼此 ≥ 12；必須在該區平地半徑內、`regionAt` 說牠在該區、在加牠之前的世界那一點是清的、加牠之後四面八方 1.7／2.6／3.5m 都走得到。
- 上面每一條都是 `scripts/test-rubric.mjs` 的斷言；石座淨空的**唯一例外**（流程與代理區那一隻，≥ 10，因為 13 座石座飽和）登記在測試裡的例外表，例外表上限 1 條、要寫理由。
- 牠是主體：底座 `solidRadius 0.9`＋`keepSolid`（淨空區掃雜物時不准把牠掃掉）；殼是半透明、不擋人；**0 光源**（眼光、殼、光暈全部自發光）；每隻 ≤ 600 三角形。
- 命名 `murk:<id>`；資料 `src/data/murks.json`（`authored: "game"`）；每區幾隻、綁哪一條技巧，見 `docs/design/spec-murk-encounter.md`。

**大濁靈的擺放（v1.2 · P17）** —— 同一套文法，半徑換成 6.0，三處是**量出來**才改的。
門檻只有一份：`scripts/lib/screen-rules.mjs`（`GREAT_MURK_*`），搜尋器 `npm run murk-fit` 與 `test:rubric` 都 import 它。

| 對象 | 門檻 | 為什麼 |
|---|---|---|
| 石座 | **≥ 9.0**（分歧之廳例外 7.2） | **不是**圈不重疊（6.5＋6.0＝12.5 → 全區 0 個落點）。石座永遠贏濁靈，所以規則反過來寫成「不准站進人家的地盤」——與守夜人（§4.14）同一個結論。例外表上限 1 條，門檻不准超過 `--ceiling` 量到的上限（見下）。 |
| 石碑／守夜人 | ≥ 10.6 | 圈不重疊（6.0 ＋ 4.6）——牠們輸給濁靈，圈疊上去就等於被蓋掉。 |
| 刻文小語／殘頁 | ≥ 9.8 | 同上（6.0 ＋ 3.8）。 |
| 器物 | ≥ 9.2 | 同上（6.0 ＋ 3.2）。 |
| 濁靈彼此 | ≥ 12 | 6.0 ＋ 5.5 取整。 |
| 反應物 | ≥ 9.2 | 小濁靈那一條是 8.7（＝5.5＋3.2，寫在反應物自己的規則裡）；半徑換成 6.0 就是 9.2。 |
| 祕密 | ≥ 4.6 | 不搶 `E`，只守淨空（小濁靈是 4；底座大了 0.6 就補 0.6）。 |
| 地標 | ≥ 6 | 「地標旁邊要矮」管的是高的東西；牠的殼撐到 4 公尺，6 公尺讓剪影不被吃掉一角。 |
| 小景 | ≥ 6 | 同上（小濁靈是 4）。 |
| 中觀層的石頭 | 圓心距 ≥ **4.0** ＋ 玩家 ＋ 那顆圓的半徑 | 那是**碰撞圓**不是互動點：守的是「還走得到牠身邊」。4.0 ＝ 貼身那一圈 2.6 ＋ 一步 1.4。照 6.0 寫的話，兩道遮擋帶會把護欄崗與分歧之廳的落點**全部**吃掉（實測各 0 個）。 |
| 主動線／閘門／出生點／起始祭壇 | 同小濁靈 | 4／8／7／9。 |
| 路網 | 3.5–26 公尺 | 遇得到，但不站在路中間（底座 1.5 ＋ 玩家 0.62 ＝ 2.12，3.5 還留得下 1.4）。量的是**帶 `PATH_BENDS` 的那張路網**（＝ 遊戲真的畫在地上的那一條，與 `sightline-audit`、守夜人那一節同一份）。 |

**真正要守的兩件事**（距離規則怎麼寫都好，玩家得走得到牠面前、按得下 `E`）：

1. **貼身那一圈繞得過去**：半徑 2.6 的圈上 16 個方向全通（2.6 > 底座 1.5 ＋ 玩家 0.62）。
2. **按得到牠的方向 ≥ 8/24**：從 24 個方向走向牠，有幾個方向找得到一個「站得住、而且是牠贏」的位置
   （互動圈裡由內到外試 2.8／3.4／4.0／4.6／5.2／5.8 六格；那一點上沒有石座在 6.5 之內、也沒有別的濁靈更近）。
   量的是**一整條射線**不是某一圈——玩家是朝著牠走過去，路上任何一個站得住又按得到的位置都算數。
   12 片土地實測 10–24（最差的是分歧之廳 10/24），門檻 8 ＝ 上限 − 2，**沒有例外表**。

**例外的「上限」要與搜尋器問同一件事**（P17 審查）：`node scripts/murk-fit.mjs --ceiling`
把石座那一條拿掉、其餘每一條照舊，逐點掃過平地圈，只留**搜尋器真的會收下**的點
（離線篩全過 ＋ 貼身 16/16 ＋ 按得到 ≥ 門檻），再看那些點裡「離最近石座最遠」是幾公尺。

| 土地 | 上限 | 合格點 | 現行門檻 |
|---|---|---|---|
| 分歧之廳 `divergence` | **7.57**（＝ 現行出貨的落點） | 46 | **7.2**（例外，餘裕 0.37） |
| 護欄崗 `wards` | 9.18 | 11 | 9.0（餘裕 0.18，全場最緊） |
| 示範與推理 `reasoning` | 9.51 | 289 | 9.0 |
| 校驗場 `refinery` | 9.66 | 521 | 9.0 |
| 其餘八片 | 12.43–22.39 | 308–1,100 | 9.0 |

分歧之廳照 9.0 掃**一個落點都沒有**（離線篩只剩 5 個點，而且沒有一個四周繞得過去）。
⚠️ 不管其餘規則的話，那片土地離最近石座最遠是 23.52 公尺（外圈沒有石座的地方，
離路網／覆蓋率／中觀層那幾條全部過不了）—— P17 收尾時規則表寫的 6.18、契約檔寫的 7.43
都比真的擺在那裡的那一隻（7.57）還小，**兩個「上限」都不是上限**，只是量錯了範圍。

**淨空半徑 ≠ 互動圈**（P17 審查）：`LAYER_INTERACT_R` 那張表交出來的是**淨空**半徑，
其中兩層刻意比互動圈小 —— 石座 5.6（`PEDESTAL_CLEAR`，真正的互動圈 6.5）、
大濁靈 4.0（貼身那一圈 ＋ 一步，真正的互動圈 6.0）。
問「互動圈上還有站得住的位置嗎」一律走 `interactRingRadius()`，不要拿淨空半徑去量：
一個欄位混兩種語意，守門的斷言就會對著**不是它在守的那一圈**發綠燈。
（大濁靈在真正的 6.0 圈上實測 15–24/24，淨空圈上是 19–24/24。）

底座：`solidRadius 1.5` ＋ `keepSolid`；壓扁到 0.34（半徑 0.8 那一圈的高低差 0.079 > `STAND_FLAT_EPS` 0.06）——
**站不上一個人的頭**這件事靠尺寸成立，不靠旗標宣告。殼一律 detail 0（20 面）：半徑大、面就大，
每隻 ≤ 400 三角形（小濁靈是 ≤ 600）。

### 4.9 殘頁的擺放（v1.2 · P07）

24 頁（12 片土地各 2 頁），資料在 `src/data/letters.json`（`authored: "game"`），互動半徑 3.8，走 `E` 撿起來。

- **一半教、一半不教**：有教學句的掛 `techniqueId` ＋ 一句照著做的白話 ＋ **與 curriculum 逐字相同的官方出處**
  （而且那份文件要在 `source-anchors.json` 裡，點過去會落在被引用的那一節）；
  純風味的**不准**有 `techniqueId` / `hint` / `source` / 連結（同 `secrets.json` 的護欄）。
- **它是撿的，不是藏的**（祕密才藏）：離路網 ≤ 12 公尺、有 tell（紙邊的一點微光 ＋ 地上的光輪廓），
  周圍 5 公尺列入 `keepClear`（不然被草叢埋掉就等於沒放）。
- **互動圈不重疊**：離石座 ≥ 10.3（6.5＋3.8）、濁靈 ≥ 9.3、石碑 ≥ 8.4、刻文小語 ≥ 7.6、器物 ≥ 7、
  會回應的東西 ≥ 8.2、祕密 ≥ 9.3、地標 ≥ 14、殘頁彼此 ≥ 7.6；離橋主動線 ≥ 4、頸口與閘門 ≥ 8、
  出生點 ≥ `SPAWN_CLEAR + 2`、起始祭壇 ≥ 9；`regionAt` 說它在那一區、`coverage > 0.9`、
  四周 16 個方向至少 14 個走得到。上面每一條都是 `scripts/test-rubric.mjs` 的斷言。
  石座淨空的**例外表**（護欄崗 9.5／分歧之廳 8.5／觀象臺 9.5）登記在測試裡、每一條寫理由、上限 3 條。
- **它不是物質**：每一頁低於 0.9 公尺（碰撞稽核判定「跨得過去」）→ 不登記碰撞體、**0 光源**、每頁 ≤ 400 三角形。

### 4.10 中觀：遮擋與母題（v1.2 · P11）

巨觀（一座 21–27 公尺的地標）與微觀（器物、反應物、殘頁、碎石）之間，原本是空的：
從橋頭望進去一眼看到底，「翻過去才看到」這件事不存在
（BotW 的三角形法則裡的**中三角**、Sky 的 meso 層；`docs/design/research-map-2026-08.md` 提案 M1／M8）。
這一節補的就是中間那一階。資料與造型都在 `src/world/screens.js`。

**① 遮擋帶（`SCREEN_BANDS`）—— 擋住「橋頭 → 地標」那一條直線。**

- 一片土地 **2–3 道**，每道 `{ id, region, at, rot, kind, length, depth, height, faceSign }`。
  高 6–12 公尺（§4.7 的登記例外）、長 7–20 公尺、厚 **2–3** 公尺。
- **它有份量 → 一定進碰撞體**（§6.3 的四條規則）：整道帶登記**一個** `solidSpan` 節點，
  沿長邊排一串半徑＝半個厚度的圓（4–5 個），覆蓋每一塊核心石板的中心；
  朝橋頭那一面的矮階刻意壓在 **0.8 公尺**以下（§6.3 第二條說它跨得過去 → 不必也不該有碰撞體）。
  `collision-audit` 逐塊驗過，穿模 0。
- **中觀層每一片土地的碰撞體 ≤ 20 個**（v1.2 · P12 的硬斷言）：12 區鋪完 ≈240 個，離 1,400 夠遠。
- **每一塊石板各自貼自己腳下的地**（P10a 的教訓：一個舞台原點配上散在幾公尺外的零件，
  就會做出一排浮在半空的牆），往下多埋 1.6 公尺咬進階地的落差。
- **0 新光源**：石材吃該區那一盞主色補光，頂緣一道自發光的刻線。
- 淨空：離石座 5.6＋玩家 0.62＋自己的碰撞半徑，其餘各層照互動半徑相加；
  離橋主動線 ≥ `LANE_HALF`+4、離閘門 ≥ 8、**不准踏進地標 14–16 公尺的留白**。
- **路會繞過它**：`PATH_BENDS` 登記折點，`buildPathNetwork()` 把「走出來的路」折過去 ——
  地上畫的那條路與 `scripts/sightline-audit.mjs` 量的那條路是**同一條**。

**② 母題（`MOTIFS`）—— 同一個形狀重複出現，遠看就認得出這是哪一片土地。**

- 一片土地 3–5 座、**同一種造型**、彼此 ≥ 16 公尺、離路網 **7–26 公尺**（看得到、走得過去、不擋路；
  也一定落在 `audit:pacing` 的 45 公尺中景圈內）。
  下限 9 → 7 是 v1.2 · P12 **量出來**改的：一片土地扣掉所有互動圈之後的自由落點幾乎全落在離路 4–12 公尺
  （沉書檔案庫 315 個裡只有 3 個 ≥9），9 公尺會讓「一片土地放 3–4 座」直接變成不可能；
  7 公尺仍然遠得下人（母題碰撞半徑 ≈1.4 ＋ 玩家 0.62，路邊還留得下 5 公尺）。
- 中景階（3–8 公尺）、instanced、共用材質、0 光源。
- **腳下的地要撐得住、也要夠平**：三階腳下 `coverage ≥ 0.96`（**不准擺在崩掉的區緣**，
  不然外側那階的碰撞體會掉進虛空）、三階之間的地形落差 ≤ 1.1 公尺。
- **每一階各自貼自己腳下的地**（與遮擋帶同一條規則）：頂面照最高的那塊地排（剪影仍是階梯），
  底面各自往下追自己腳下的地。P11 審查前只在正中央取一次高度 → 12 塊裡 9 塊浮空、1 塊埋進土裡，
  而且**浮起來的會被穿模稽核當成「從底下走過去」而豁免**，所以貼地要有獨立於稽核的斷言。
- **座標要用「改資料 → 重建世界 → 量」的迴圈搜**：母題進 `keepClear`，移動它會讓
  程序化道具整批重擲 —— 「四周走得到」這種問題離線幾何算不出來。
- **形狀自己說故事，不寫字**，而且**每片土地一種**（照該區的傳說鉤，§1.4）：
  階梯迴廊「示範了兩遍的階梯」（兩階實體、第三階只剩一圈光）、
  沉書檔案庫「讀過的那一疊」（矮台上一疊石板書，翻開的那一頁懸在半空只剩光 —— 枯掉的那幾頁）、
  齒輪工坊「一次只吊一小件」（矮桁架撐著折下來的臂，斷掉的下一節只剩光）、
  面具劇場「掛著的空面具」（柱頂一張沒有人戴的面具，背面那一句只剩光 ——「你不是它」）、
  契約鍛冶場「未命名的工具」（矮鍛台上立著一把工具的柄，該刻名字的那一格是空的，刃只剩光 ——
  v1.2 · P16a：這片土地 P12 就有兩道遮擋帶，卻一直沒有母題，走進去只有入口有厚度）。

**③ 揭露量得出來（`npm run audit:sightline`）。**

從**橋頭**（主動線的內端，也就是區界再往裡 8 公尺）沿著那條路每 3 公尺取一個樣點，
問「站在這裡看得到地標嗎」：`hidden` ＝ 樣點→地標的線段穿過某道帶的核心矩形；
`hiddenTip` ＝ 再加上「帶頂的仰角蓋過塔頂」。門檻是 **前 12 公尺看不到、25 公尺內一定看得到**
（擋住但不迷路），`test:rubric` 拿它當硬斷言。
判定本身住在 `screens.js` 的 `landmarkSight()` —— 遊戲（`world.landmarkSightFrom()`）與稽核腳本
問的是**同一支**，不會各說各話。

**④ 先量再放 —— 用 `npm run screen-fit`，不要用手算。**
`scripts/screen-fit.mjs` 是「**改資料 → 重建世界 → 量**」的搜尋迴圈（世界蓋一次約 0.5 秒）：
離線先用 `scripts/lib/screen-rules.mjs` 的**同一份門檻**篩掉一看就不行的格點，
再把前 N 個候選塞進 `createWorld({ screens })` 對真的蓋出來的世界量。
`--verify` 會把**現行出貨的每一件**重驗一次（自我驗證：篩子改壞了，reasoning 先紅）。
放進資料之後照樣要跑 `npm run audit:pacing`（三口徑死區只准變少）、`npm run audit:sightline`
與 `npm run test:rubric` 的擺位斷言。

**⑤ 有些土地擺不下遮擋帶 —— 那就不要擺。**
沉書檔案庫與齒輪工坊量出來一道都放不下（前者的合法擺法只出現在離橋頭 7–11 公尺處、揭露太早；
後者整片土地 0 個合法擺法 —— 兩座石座的淨空圈在軸線上首尾相接）。
**遮擋帶是可選的，母題不是**：一片土地可以只有母題。硬擺一道過不了門檻的帶
（或為了擺它去放寬石座的淨空）都是倒退。

> **v1.2 · P16a 補的兩片與量不出來的兩片。**
> **量器坊**補了**兩道遮擋帶**（`forms-first-tick`／`forms-second-tick`）——
> 第一道是整片土地**唯一**的合法擺法（2,628 個格點裡就那一個），
> 揭露量到「前 15 公尺看不到、第 15 公尺揭露」；第二道走 `--noSight`，
> 負責的是「走下橋時左右各有一階掠過」。
> **契約鍛冶場**補了 **4 座母題**（掃出 75 種合法擺法）。
> **減法之庭與校驗場兩種都擺不下**，量到的是：母題各只搜得到 **1 座／2 座**
> （低於「3–5 座才叫母題」的下限 —— 兩座同一個形狀還不夠說「這裡是哪」），
> 遮擋帶各 **0** 種合法擺法（兩片都是加建，橋頭到地標之間只有十幾公尺）。
> 它們的中景由 P16a 新蓋的高台擔 —— **高台本來就是中觀層的第三種東西**（§4.12）。
> **母題也是可選的**：這一條在 P16a 從「母題不是可選的」放寬成「量出來擺不下就不擺」，
> 理由與遮擋帶完全相同 —— 硬擺兩座同形狀的東西不會讓那片土地更好認，只會多兩顆碰撞圓。

> **v1.2 · P16b：12 片土地每一片都有中觀層了。**
> 最後三片（**護欄崗 `wards`／觀象臺 `sight`／分歧之廳 `divergence`**）各補**兩道遮擋帶**，
> 遮擋帶因此從 8 道長到 **14 道 · 7 片土地**。`test:rubric` 逐片守著
> 「每一片土地至少有帶、母題或高台其中一種」——**這條線從此不准再退回去**。
>
> 三件事是量出來才做的（每一件都寫得出「原本為什麼是那個數字」）：
>
> ① **`screen-fit` 的離線篩對高台比真的門檻嚴。** 「離閘門」「地標留白」「離主動線」
> 三條，真的門檻（`solidProblems()`）量的是**碰撞圓的圓心**（`gate < 8`、`d < lm.clear`、
> `lane < LANE_HALF + 4 − r`），離線篩卻一律把 `estR` 加上去。對母題與遮擋帶那是刻意的保守
> （它們的圓真的散在 `at` 之外），但**高台只有一顆圓、圓心就在 `at`** ——
> 等於憑空嚴了 1.4 公尺，主動線那一條更嚴了 2.8（規則減半徑、篩子加半徑）。
> 所以 §4.12 那張表裡護欄崗的「格點 0」是**篩子的 0，不是土地的 0**。
> 修好之後重量：護欄崗有 **21 個合法落點**，但最大只塞得下**碰撞半徑 1.24 公尺**的一顆
> （`PLATFORM_RADIUS_MIN` 是 1.4）——**還差 0.16 公尺**，所以它的中景由帶擔。
>
> ② **反應物的淨空跟著它自己的觸發半徑走。** 以前整個反應層套用**最大的那一個**
> （光菇圈 4.4），等於拿光菇圈的尺寸去量風鈴（3.2）、靜水盤（3.2）、螢蛾（3.0）、
> 小獸（4.2）—— 正是 P06c 記下的那條教訓在同一層裡又犯了一次。
> 現在建造器與淨空規則讀同一份（`reactive.js` 的 `REACT_TRIGGER_R`／`reactiveTriggerR()`）。
> **音石列刻意維持整排一個目標（4.4）**：它是「沿著走」的東西，保證是「整排走得完」
> （P06c 已經有逐顆 `isWalkable` 的硬斷言），不是「每一顆四周整圈都走得到」；
> 真的攤成 5 個滿圈目標量過一次，`frugality-emptied-step` 會差 0.47 公尺、
> 而減法之庭再也找不到第二個落點 —— 數字與理由留在 `SONGSTONE_ROW_CLEAR`。
>
> ③ **護欄崗擺不下「會擋住地標」的那一道 → 登記例外（`screens.js` 的 `SIGHT_EXEMPT`）。**
> `screen-fit` 掃過 5,499 個格點 × 30 個角度：擋在「橋頭 → 不會關上的門」那條直線上的擺法
> 有 **6,079 種，每一種都踩到石座的淨空**，最好的一種離 `speaking-letter-75` 6.61 公尺、
> **差 0.61 公尺**（需要 7.22）。而且橋頭那一點是路的起點 ——
> 折點怎麼繞都改不了「站在橋頭看得見」這件事，所以那 6,079 種就是全部。
> 例外**不是把門檻調鬆**：它的兩道帶照樣吃每一條淨空與擺位規則，
> `audit:sightline` 只是不問它「地標擋不擋得住」；而 `test:rubric` 反過來守著
> 「登記過的土地真的沒有任何一道擋在那條直線上」——**哪一天擺得下了，那條斷言會紅**。
>
> 逐片量到的（P16b）：
>
> | 區域 | 遮擋帶 | 母題 | 高台 | 這一格補了什麼 |
> |---|---|---|---|---|
> | `wards` 護欄崗 | 會擋視線的 **0 種**（6,079 種全踩淨空，最好的差 0.61） | 21 個落點、最大半徑 1.24 | 同上，差 0.16 | **2 道帶**（登記 `SIGHT_EXEMPT`）：第二道厚 **2.0**（整份資料唯一的一道 —— 厚 2.4 時最好的擺法差 0.11 公尺） |
> | `sight` 觀象臺 | 19,339 格 × 36 角 → **8 種**（全擠在 ±1 公尺內） | 28 個離線候選 → 1 座 | 110 → 0（最好的要爬 2.34） | **2 道帶**：第一道量到「前 12 公尺看不到、第 12 公尺揭露」——**剛好等於 `HIDDEN_MIN`** |
> | `divergence` 分歧之廳 | 4,920 格 × 24 角 → **9 種**、重建驗過 1 種 | 1 個離線候選 → 0 | 1 → 0（腳下覆蓋不足） | **2 道帶**：第一道量到「前 15 公尺看不到、第 15 公尺揭露」 |
>
> **兩處「剛好過」**（同 §4.12 那兩座高台的規矩，寫下來讓下一個動到它的人先紅）：
> 觀象臺的揭露是 **12 公尺 ＝ 門檻本身**，而樣點每 3 公尺一個 —— 再退一格就是 9（不過）；
> 護欄崗的第二道帶靠**薄一階（2.4 → 2.0）**才站得下，餘裕 0.09 公尺。
> 動這兩片土地的路、石座、器物或這幾道帶之前，先跑 `npm run screen-fit -- --verify`
> 與 `npm run audit:sightline`。
>
> **第二道帶也會改變揭露。** 觀象臺第二道的第一版擺在 `[122, -26.5]`：
> 它「不負責擋視線」，卻把揭露從第 12 公尺推到第 33 公尺（超過 `REVEAL_MAX` 25）。
> **不負責擋，不代表它不會擋** —— 加任何一道帶之後都要重跑 `audit:sightline`。


---

### 4.11 每一片土地專屬的空氣（v1.2 · P12）

在這之前全世界只有一層螢火（`world.js` 的 `buildMotes()`）——12 片土地共用同一種東西，
只有密度與顏色不同。`src/world/drifts.js` 補的是「**空氣本身**也換了」：
走進沉書檔案庫，飄的是往下落的紙屑；走進契約鍛冶場，是往上竄的餘燼；
走進減法之庭，幾乎什麼都不飄。

- **一區一個 `THREE.Points`、一個 draw call**；12 區**共用同一個材質與同一張貼圖**（`+12` draw call，
  三角形 0）。**0 新光源** —— 加色混合的自發光點。
- 每一組的參數（撒法 `disc`／`ring`／`columns`／`low`、高度區間、上升／下沉、擺幅、繞圈、色偏）
  都對得上那片土地的傳說鉤（§1.4），不是隨手調的數字；密度沿用 `REGION_ATMOSPHERE.motes` 當倍率。
- **低畫質整層關**：`group.visible = false` ＋ 每幀零工作。畫質是**當下**問的（`qualityOf()`），
  玩家在設定裡切換不必重建世界（同 `rubric-fx.js`）。
- **`reducedMotion` 只留靜態的點**：位置建構時就是終態，`update()` 一個位元組都不寫
  （§2.4：關掉的是「動」，不是「回應」——點還在、還會亮）。
- **零每幀配置**，而且中心離鏡頭 `CULL_M`（180 公尺）以外的土地整層跳過（§6.2 的距離分級）。
- **這一層刻意沒有 dispose**：材質與貼圖是模組層的共用快取，任何一個實例把它釋放掉，
  其他還活著的世界就會從畫面上消失（P09 的教訓）。每一組自己的 geometry 由 `dispose()` 釋放。

---

### 4.12 高台：站得上去的那一階（v1.2 · P14）

中觀層的第三種東西，也是第一種**為了跳躍而存在**的東西（資料在 `src/world/screens.js` 的 `PLATFORMS`）。

- **它是什麼**：一塊圓的石鼓，頂面平、離自己腳下的地 1.6 公尺，頂面刻著一圈自發光的細線。
  底下一圈矮裙（露出地面 0.55 公尺 —— §6.3 說它跨得過去，不必有碰撞體）讓它看起來是從地裡長出來的。
- **為什麼是圓的**：`standR`（頂面證明過是平的那一段）是一圈一圈往外量的。
  方的頂面在對角線方向上會先量到石頭外面，於是 `standR` 停在半徑的七成 ——
  人走到邊緣還沒到石頭邊就憑空掉下去。圓的頂面量到多遠都是平的。
- **規則**（`test:rubric` 與 `scripts/screen-fit.mjs` 用同一份門檻）：
  1. 高度 `PLATFORM_HEIGHT_MIN`–`PLATFORM_HEIGHT_MAX`（1.2–2.4），而且一定在 `STAND_MIN_H`–`STAND_MAX_H` 之內
     —— 蓋一座跳不上去的高台等於蓋一面牆。
  2. 半徑 ≥ `PLATFORM_RADIUS_MIN`（1.4），量出來的 `standR` ≥ `PLATFORM_STAND_R_MIN`（1.2）。
  3. 其餘擺位規則與母題**共用同一套**（離路網 7–26、離各層互動圈、離主動線、離閘門、
     腳下覆蓋率、四周繞得過去、逐塊貼地）——不另訂一份會分家的門檻。
- **落點用 `npm run screen-fit -- --region <區> --kind platform` 搜，不要手挑**：
  高台進 `keepClear`，移動它一寸，整片土地的程序化道具就重擲一次；
  「它四周走不走得到」「它的頂面站不站得上去」離線幾何都算不出來，只有重建世界才量得到。
- **它是路上的東西**，所以要餵進 `scripts/pacing-audit.mjs` 的中景 POI（P07 的教訓）。

第一座是中央高原的「第一階」（`foundations-first-step` @ 24, -12）。
高原是所有人出發的地方，可是它從頭到尾只有一個高度 —— 平的。
第一階是這片土地上第一個「上面」：形狀自己說「這個可以站上去」，一個字都不寫。

#### 高台不是裝飾（v1.2 · P15）

P15 把高台鋪成 **5 座 · 4 片土地**（中央高原 2 座；沉書檔案庫、面具劇場、量器坊各 1 座），
同時把那四片土地加進 `JUMP_REGIONS` —— **在跳不起來的土地上蓋高台等於蓋裝飾**；
反過來，**沒有高台的土地也不先開跳躍**（開了也沒有東西可以跳上去，只多出風險面）。

- **新的硬門檻：從四周每一個方向都要跳得上去**（`platformRise()`，餘裕 `PLATFORM_JUMP_MARGIN` 0.2）。
  `height` 量的是「頂面離**自己腳下**的地多高」，可是玩家是站在**旁邊**起跳的：
  地形一斜，同一座高台從高的那一側是 1.2 公尺、從低的那一側可能是 2.6 公尺 ——
  於是它 `standable` 為真、`height` 合法、穿模與可站立體稽核全綠，
  **卻有一半的方向跳不上去**。這正是 P14 交接點名的「別做那個」，
  只是這一次不是資料寫太高，是**地形替你寫高的**。
  判準：起跳圈（碰撞半徑 ＋ 玩家半徑 ＋ 0.5）上每一個站得住的方向，
  `standTop − 那一點的地形高度` ≤ 離散彈道頂點（2.04）− 0.2。
  搜尋工具、`test:rubric` 與遊戲問的是**同一支**。
- **為什麼只有這四片、而且多半只放得下一座**：加上那條門檻之後逐片量出來的事實 ——
  契約鍛冶場／觀象臺／分歧之廳／護欄崗**一個合法落點都沒有**（地形太起伏）、
  示範與推理的合法點全部離母題不到 2.4 公尺、
  沉書檔案庫／面具劇場／量器坊各只擺得下一座。**擺不下就不擺**（同 §4.10 ⑤）。
  > **v1.2 · P16a 把這一段量完了，結論改了一半**（表在下一小節）：
  > 契約鍛冶場／觀象臺／分歧之廳／護欄崗確實一個都沒有 —— 那一半是對的；
  > 但**示範與推理、齒輪工坊、減法之庭、校驗場其實各擺得下 1–2 座**，
  > P15 那一句是拿 2 公尺格點掃出來的（示範與推理要掃到 **0.25 公尺**才找得到那一個）。
  > **「量到 0」與「用粗格點量到 0」是兩件事** —— 說「擺不下」之前先把格點壓細一階。
- **高度下限 1.2 → 1.6**：1.6 是 `EYE_HEIGHT`。比它矮的頂面站在地上就看得完，
  「上面有東西」這句話說不出口。上限那一邊真正在守的是**離散彈道的頂點**
  （`jump.js` 的 `simulateApex()`，最差幀率下 2.04）—— `test:rubric` 逐座驗
  「跳得上去還有 0.3 公尺餘裕」，所以現行出貨的每一座都是 1.6。
- **`reveals`：站上去換得到什麼。** 四片土地各有一座頂面上躺著一處
  `tell: "high"` 的祕密（§4.5）——**腳在地上搆不到、腳在頂面才搆得到**，量得出來，
  而且把那座高台壓矮就會紅。`reveals` 是**選配**：P16a 新蓋的六座都沒有
  （`secrets.json` 裡 `tell: "high"` 的就那四處，硬塞第五處等於改那份資料）。
- **造型照該區的母題語彙，頂面永遠是圓的**：`stepStone`（素石鼓）、
  `pageStep`（翻過的那一疊）、`maskStep`（裙上一張很淺的面具）、`gaugeStep`（外圈一圈刻度）、
  以及 P16a 的 `twiceStep`／`hoistStep`／`takenStep`／`mirrorStep`。
  變的只有裙與外圈——**頂面的形狀是判定，不是造型**（`standR` 一圈一圈往外量的那件事，
  不因為換皮而改變）。裝飾一律不擋人，而且都壓在 0.9 公尺以下。
- **「四周繞得過去」只有一份數字**（`scripts/lib/screen-rules.mjs` 的 `AROUND_*`：
  半徑 5 的圈、16 個方向裡要 14 個）。P14 是工具與測試各寫一份、剛好兩邊都過；
  P15 一鋪開就出現「工具說可行、測試說不行」。

#### 先量再放：12 片土地的高台落點表（v1.2 · P16a）

P15 收尾時只量了「加上跳躍門檻之後這幾片還剩下什麼」，其餘 8 片是**推測**擺不下。
P16a 把它量完：對 12 片土地各跑一次 `npm run screen-fit -- --region <區> --kind platform`
（高度 1.6、半徑 **1.4**＝`PLATFORM_RADIUS_MIN`、格點 0.25 公尺、離線篩之後逐個重建驗）。
表裡的數字是**這一格開工時**（P15 的資料 ＋ 新補的 `BAND_CLEAR` 門檻）量到的。
除了 `toolcraft` 之外，現在重跑逐格得出同一份；`toolcraft` 這一格自己補了 4 座母題，
所以**它現在重跑是 3 → 0**（母題的淨空把 742 個格點吃到只剩 3 個，結論沒變：擺不下）。
**逐片的數字只有這一張表**——別的地方要引用就指過來，不要抄一份（抄過一次，
兩份就開始互相打架了：P16a 審查抓到 `expected-counts.json` 抄成 209／2.01）。

| 區域 id | 格點 → 合法候選 | 重建驗過 | 這一格蓋了幾座 | 量到什麼 |
|---|---|---|---|---|
| `foundations` | 5,220 → 1,792 | 0／6 | 0（P14／P15 的 2 座還在） | 全場最平、落點最多的一片；已有的兩座把 16 公尺的同伴間距吃滿 |
| `reasoning` | 192 → 7 | **1** | **1** | 全場最擠：3 座母題 ＋ 2 道石脊 ＋ 石座淨空鋪滿內圈。格點掃到 **0.25 公尺**才找得到那一個（0.5 公尺掃出的那一個「四周繞不過去」） |
| `grounding` | 591 → 7 | 1 | 0（P15 的 1 座還在） | 還有空位，但這一格的預算先給沒有高台的土地 |
| `orchestration` | 29 → 3 | **1** | **1** | 13 座石座的淨空圈幾乎鋪滿；活下來的落點在西南角 |
| `config` | 258 → **0** | 0 | 0（P15 的 1 座還在） | 第二座擺不下：179 個點腳下覆蓋不足、66 個跳不上去（最好的要爬 2.04） |
| `forms` | 202 → 12 | 1 | **1**（共 2 座） | 地形是一階一階降下去的長平台，落差正好卡在跳躍門檻上：190 個點「有方向跳不上去」 |
| `toolcraft` | 742 → **0** | 0 | **0** | **一個都沒有**：509 個點跳不上去，最好的那一個要爬 **1.92**（門檻 1.84）——放射狀的工具溝把每一塊平地都夾在落差之間。半徑再小也沒用（1.4 已是下限）|
| `wards` | **0** → 0（**P16b 修正：篩子的 0**，見表下） | 0 | **0** | 半徑 26 的哨所，閘門（≥8）＋ 地標留白 ＋ 互動圈就吃光了 |
| `refinery` | 369 → 48 | 4 | **2** | 「兩面互相照著的鏡」——淺谷把院子分成幾乎一樣高的兩半，所以它是第一片刻意擺兩座的加建 |
| `frugality` | 160 → **160** | 1 | **1** | 全場最平的一片（東西都被搬走了）：離線篩活下來的每一個點都過得了高台自己那三條。但 16 公尺的同伴間距在當時半徑 32（v1.2 · P22c 之後 41.6）的院子裡只擠得下**一座** |
| `sight` | 110 → **0** | 0 | **0** | 整片坡由西南往東北仰起來 —— 「往上看」這件事就是地形本身，所以四周永遠有一側低很多：最好的一個要爬 **2.34** |
| `divergence` | 1 → **0** | 0 | **0** | 高原上的建物，當時半徑 29（P22c 之後 37.7）：唯一活下來的格點腳下覆蓋不足 |

**兩座「剛好過」的高台**（P16a 審查量出來的）：`forms-second-gauge-step` 的餘裕只有 **0.014 公尺**、
`orchestration-hoist-step` **0.018**（門檻是頂點 2.037 − `PLATFORM_JUMP_MARGIN` 0.2）。
它們今天過得了，但那是「剛好過」不是「有餘裕」—— 而且那兩片土地在這一格之後
**再也沒有第二個合法落點**（forms 重跑 53 → 0）。動地形、動 `JUMP_SPEED`／`PLATFORM_JUMP_MARGIN`／
`PLATFORM_TAKEOFF_PAD`、或在它們旁邊擺新道具之前，先跑一次 `npm run screen-fit -- --verify`；
`test:rubric` 會逐座把餘裕印在訊息裡，這兩座會第一個紅。

> **v1.2 · P16b 的修正（護欄崗那一列）**：那個 0 是**篩子的 0，不是土地的 0**。
> `screen-fit` 的離線篩把「離閘門」「地標留白」「離主動線」三條一律加上 `estR`，
> 而真的門檻（`solidProblems()`）量的是**碰撞圓的圓心** —— 高台只有一顆圓、圓心就在 `at`，
> 所以那三條憑空嚴了 1.4 公尺（主動線那一條嚴了 2.8：規則減半徑、篩子加半徑）。
> 修好之後重量：護欄崗有 **21 個合法落點**，但**最大只塞得下碰撞半徑 1.24 公尺的一顆**
> （`PLATFORM_RADIUS_MIN` 1.4）——**還差 0.16 公尺**，結論沒變（擺不下），理由變了。
> 其餘 11 片的數字不受影響（它們的 0 或候選數都不是被那三條卡住的）。
> **這是「量到 0」那條教訓的第三種形態**：粗格點的 0（P16a）、用錯門檻的 0（P16b）、
> 真的 0 —— 說「擺不下」之前先問一句「我量的是真的那條門檻嗎」。

**讀法**：「格點」是離線篩掃過的格數，「合法候選」是通過所有離線門檻（區域、覆蓋、主動線、閘門、
地標留白、互動圈、離路網 7–26、離同伴、**離石脊**、腳下平不平、**四周每個方向跳不跳得上去**）的點，
「重建驗過」是把前幾個彼此散得開的候選各塞進 `createWorld()` 蓋一次世界之後仍然過的座數。

**結論**：高台 5 → **11 座 · 8 片土地**，`JUMP_REGIONS` 跟著長到 8 片。
**契約鍛冶場、觀象臺、分歧之廳、護欄崗一座都不擺，也不開跳躍** ——
「按了 `J` 卻沒有任何東西跳得上去」比不能跳更糟（同 §4.10 ⑤「擺不下就不擺」）。

出貨的 11 座逐座量到的「最難那一側」（`platformRise()` 的 `worst`，門檻 1.84）：

| 高台 | 半徑 | 最難那一側 | 餘裕 |
|---|---|---|---|
| `foundations-first-step` | 2.6 | 1.73 | 0.31 |
| `foundations-second-step` | 1.6 | 1.71 | 0.33 |
| `grounding-read-step` | 1.6 | 1.66 | 0.38 |
| `config-gallery-step` | 1.6 | 1.60 | 0.44 |
| `forms-gauge-step` | 1.6 | 1.72 | 0.31 |
| `forms-second-gauge-step` | 1.4 | 1.82 | 0.21 |
| `reasoning-third-step` | 1.4 | 1.66 | 0.38 |
| `orchestration-hoist-step` | 1.4 | 1.81 | 0.23 |
| `frugality-emptied-step` | 1.4 | 1.77 | 0.27 |
| `refinery-first-mirror-step` | 1.4 | 1.70 | 0.34 |
| `refinery-second-mirror-step` | 1.4 | 1.64 | 0.40 |

**新門檻：離遮擋帶也要留得下人（`BAND_CLEAR`）。**
母題與高台一路只跟**彼此**比距離（`MOTIF_GAP` 16／`PLATFORM_MOTIF_GAP` 12），
跟遮擋帶之間一條規則都沒有 —— P16a 是第一次出現「先擺帶、再搜高台」的順序，
搜尋器當場把示範與推理的高台放在離石脊核心矩形 **0.45 公尺**的地方（兩顆碰撞圓直接疊在一起）。
中心距不管用：帶是長條，中心離得遠不代表端點離得遠 —— 所以量的是離**核心矩形**的距離
（`screens.js` 的 `bandCoreDistance()`，搜尋器／`--verify`／`test:rubric` 共用同一支），
門檻 `2.64 ＋ 自己的碰撞半徑`。補上這條之後，示範與推理整片土地只剩下一個合法落點。

**造型仍然是一片土地一種**（頂面永遠是同一塊圓的石鼓，變的只有裙與外圈）：
`twiceStep`（示範與推理：兩級小階 ＋ 第三級只剩光）、`hoistStep`（齒輪工坊：裙邊伸出一截短臂吊著一小件）、
`takenStep`（減法之庭：只剩四個空托座 ＋ 一圈懸空的光）、`mirrorStep`（校驗場：一圈磨過的鏡面 ＋ 兩道刻線）。
量器坊的第二座沿用第一座的 `gaugeStep` —— 遠看讀得出是同一把尺上的兩格。

**這一格沒有新的 `reveals`**：`tell: "high"` 的祕密在 `secrets.json` 裡就那四處，
硬塞第五處等於改那份資料。新的六座是「跳這個動詞在這片土地上成立」的證據，不是新的收集點。

---

### 4.13 橋上的缺口（v1.2 · P15）

一座橋的中段斷掉 3 公尺。**跳得過去**（頂點 2.08、水平 11.5 m/s，綽綽有餘），
而**旁邊一定留一條窄板**——所以它是可選的捷徑，不是關卡。資料在 `world.js` 的 `BRIDGE_GAPS`。

- **目前只有一道**：東北那座橋（通往沉書檔案庫）沿橋 66 公尺處，
  在閘門的**內側** —— 檔案庫還沒解鎖的玩家也走得到它，「第一次遇到缺口」不必先過關。
  跳得起來的橋（`jump.js` 的 `JUMP_BRIDGES`）與有缺口的橋**一一對應**：
  沒有缺口的橋一律 0，在上面亂跳只會製造「跳出去」的機會。
- **窄板是例外成立的條件**（§6.4）：`keepFrom`–`keepTo` 整條都在 `LANE_HALF`（3.2）之外，
  甲板在 |s| ≤ 5.0 是平的，所以窄板取 3.5–5.2 那一條。
- **不動高度場。**（P15 當時的寫法；**v1.2 · P16d 之後高度場改過一次** ——
  見 §6.3「走不走得到」。缺口擋人的方式仍然一個位元組都沒動，
  甲板與窄板的可走性 P16d 逐點驗過不變。）`terrainHeight()` 與 `coverage()` 一個位元組都沒改：
  地形網格一格 1.7 公尺，一道 3 公尺的洞刻不出來；而且「掉進虛空」這件事在這個世界
  從來沒有發生過，不該從一座橋開始。缺口擋人的方式與閘門同一套——`isWalkable()` 說那一點走不到。
- **唯一放行的是「腳已經離地夠高」**（`GAP_LIP` 0.35 公尺），
  與「腳站到頂面以上的可站立體不擋人」（`solidAtAbove()`）是同一個形狀的例外。
  虛空與閘門那兩條**一寸都沒鬆**：不管腳在多高，它們照樣擋。
- **不把人關住**：落在缺口裡的人由 `escapeSolid()` 的同一條保險絲請回最近的邊
  （三個出口挑最近的，一步 0.35 公尺，不瞬移）。
- **0 碰撞體、0 光源**：兩道斷口的矮唇露出地面 0.34 公尺、碎樑更矮（§6.3 說跨得過去），
  斷面那一線與窄板的導引線都是自發光。

### 4.14 守夜人的擺放（v1.2 · P16c）

12 位，一片土地一位。他是**站在崗位上的人**，所以擺位守的東西跟石頭不一樣：
石頭守的是「不擋路」，人守的是「**走得到他面前、按得下 `E`**」。
規則與門檻只有一份，住在 `scripts/lib/screen-rules.mjs`，測試與搜尋腳本共用。

1. **比他高階的兩層（石座 6.5、濁靈 5.5／大濁靈 6.0）：不准站進人家的地盤。**
   離石座 ≥ **8.0**、離小濁靈 ≥ **7.5**、離**大濁靈 ≥ 10.6**（`WATCHMAN_ABOVE_MIN`）。
   大濁靈那一格不是另訂一個數字，而是**寫成那條式子本身**（`GREAT_MURK_R + WATCHMAN_R`）——
   §4.8 的大濁靈那一側要求的就是「圈不重疊 6.0 ＋ 4.6」。
   P17 收尾時這一格是死常數 8.0（沒有任何地方讀它），而守夜人的擺位稽核掃 `murks.json`
   的每一筆、一律套 7.5：把守夜人擺在離大濁靈 7.6 公尺處，**這道門會過、那道門會紅**，
   而從常數看不出哪一個才算數。現行出貨最近的一對是 `watch-sky-mirror` ↔ `murk-great-somewhere`（10.97）。
   為什麼不是「互動圈不重疊」（石座要 4.6 ＋ 6.5 ＝ 11.1）：**量出來擺不下**。
   142 座石座已經把 12 片土地填滿 —— 逐點掃過整張地圖，護欄崗（哨所半徑 35.1、6 座石座）
   與分歧之廳（廳半徑 37.7、10 座）**全區 0 個落點**（吃完其餘每一條規則之後，
   全區離每座石座最遠只到 9.10／9.75 —— **這兩個數字是 P17 當時、地圖放大前量的**；
   v1.2 · P22c 把座標整個 ×1.3 之後兩片土地都寬了，這一條因此比當年更鬆，
   但「互動圈不重疊要 11.1」那個結論沒有被重新量過，所以規則維持 7.5 不動）。
2. **同階與更低階（石碑 4.6、刻文小語 3.8、殘頁 3.8、器物 3.2）：互動圈不重疊。**
   他在仲裁裡贏它們，圈疊上去就是把它們蓋掉。
3. **不搶 `E` 的東西（反應物、祕密、小景、地標）≥ 4 公尺**（同濁靈）。
   地標不吃它自己的 14–16 公尺留白 —— §2.2「高的東西旁邊要矮」管的是高的東西，
   而他是一個 1.8 公尺的人；站在地標旁邊正好證明那座地標有多高。
4. **不准把中觀層清零**：離每一顆遮擋帶／母題／高台的碰撞圓 ≥ `4.6 + 玩家半徑 + 那顆圓`。
   P16b 交接的警告是 `sight`／`divergence`／`wards` 各只剩 0–8 種合法擺法 ——
   守夜人的互動圈壓上去，`npm run screen-fit -- --verify` 當場就紅。
5. **離走出來的路 3–12 公尺**：遇得到（走路的人會撞見他），又不站在路中間。
6. **量出來的那一條（真正要守的東西）**：站在他的互動圈上，24 個方向裡至少
   **22 個**「站得住而且是他贏」。十片土地都找得到 24/24 的落點；
   兩片例外是全區上限，不是妥協 —— 護欄崗 18/24（門檻 16）、分歧之廳 11/24（門檻 10）。

造型：斗篷 ＋ 兜帽 ＋ 一盞燈，**156 個三角形**、**0 光源**（燈是自發光材質 ＋ 一片加色 sprite）、
一顆半徑 0.55 的碰撞圓（< `STAND_MIN_R` 0.8 → 站不上一個人的頭）。
三種提燈的方式（`look`：`lantern` 提在身側／`censer` 垂在身前／`wick` 捧在胸前）只差提法與高度，
零件一樣多 —— 一片土地一種性格，但不必為此多花三角形。

他給的四種情報（算法在 `src/progression/watchtalk.js`，全是純函式）：

| 情報 | 讀什麼 | 護欄 |
|---|---|---|
| 卡關提示 | 存檔的 `struggles`：試最多次、還沒過的那一關 → 它 rubric 裡**還沒命中**的第一條檢查器 | 用世界語言講（`watchmen.json` 的 `checkLines`），**不給答案、不貼範例** |
| 指路 | 該片土地**最近一處還沒找到**的殘頁／祕密 → 方位 ＋ 要走多久 ＋ 它自己登記的 tell | 不講名字（找到的那一下才是名字） |
| 世界觀故事 | `watchmen.json` 的 `lore`，一拍一拍，可以追問 | 自撰（`authored: "game"`），不掛任何連結 |
| 技巧小知識 | **引用** `skill-codex-v2.json` 的一條技能：`nameZh` ＋ `oneLiner` ＋ 它自己的官方連結 | 一個字都不是新寫的（護欄 2）；已經會了的優先，問一次換一條 |

**沒得講的那一項不出現** —— 不畫一個按不出東西的選項。
跟他說話**不給 XP、不寫任何一關的評價、不影響解鎖**：他給的是情報，情報本身就是報酬。

### 4.15 守門者的擺放（v1.2 · P18）

一位，站在護欄崗那道「不會關上的門」旁邊。他與守夜人是同一種東西
（一個站著不動、會回答你的人），所以規則整套沿用 §4.14，只有三處不同，而且三處都是**量出來**的
（規則與門檻只有一份，住在 `scripts/lib/screen-rules.mjs`；搜尋器是 `npm run guardian-fit`）：

1. **互動半徑 3.2**（守夜人 4.6）。護欄崗是 12 片土地裡最擠的一片：半徑 35.1 的哨所裡
   6 座石座 ＋ 1 隻大濁靈 ＋ 1 位守夜人 ＋ 2 頁殘頁 ＋ 2 件器物 ＋ 3 處反應物 ＋ 2 個小景
   ＋ 9 顆中觀層的碰撞圓。`npm run guardian-fit -- --survey` 逐點掃過整片土地（0.5 公尺一格）：

   | 互動半徑 | 離線篩剩下的格點 | 離那道門 |
   |---|---|---|
   | 4.6（守夜人那一階） | **0** | — |
   | 4.0 | **0** | — |
   | 3.8 | **0** | — |
   | **3.2（出貨）** | **6**（門邊 ≤12m 的也是 6） | 8.6–10.0m |
   | 2.8 | 20 | 4.0–10.0m |

   也就是說：**照守夜人那一階他根本站不進這片土地**，3.2 是「還擺得下」的第一階。
   現行出貨的落點 (102.75, -153) 不落在 0.5 的格子上（是把格點收細之後找到的），
   離門 10.26 公尺，也是那一帶**唯一**通得過重建驗的一個 —— 上表那 6 個格點全部卡在
   「貼身三圈繞不過去」。
   ⚠️ P18 收尾時這一段寫的是「4.6 剩 5 個、全部離門 21.5 公尺；3.2 有 11 個」——
   三個數字都不是跑出來的（審查 · 第 4 條）。**寫「量出來的」之前先真的跑一次**
   （findings：「快照要註明它是什麼時候的快照」）。
2. **互動圈與每一層都不重疊 —— 連石座也是。** 守夜人做不到這件事（142 座石座把 12 片土地填滿了），
   所以他那一邊只好改守「不准站進人家的地盤」；守門者只有一位、只要擺得下一個口袋，就該守最嚴的那一條。
   這也是他半徑比別人小卻不會被誰蓋掉的原因（§3.2）。
   比他高階的三層另外還要各守一次自己那一格（`GUARDIAN_ABOVE_MIN`：石座 8.0＝`MARKER_R + 1.5`、
   小濁靈 8.7、大濁靈 9.2、守夜人 7.8 —— 後三個一律**寫成兩個半徑相加**，不重打字面值）。

   **餘裕（`GUARDIAN_NO_OVERLAP_MARGIN`）只能是 0。** 這片土地擠到最緊的那一對
   （他 ↔ `watchman:watch-unclosing-door`）量到 7.83、要 7.80 —— **只剩 0.03 公尺**。
   訂 0.1 以上的餘裕，出貨的落點當場過不了（實測：改成 0.5 之後 `test:rubric` 立刻紅在
   「他的互動圈與每一層都不重疊」）。所以這一格就是「圈不重疊」本身，不多留；
   補償的做法照 findings 那一條（「餘裕只剩 0.01 的門檻等於沒有門檻」）：
   **把餘裕印進斷言訊息裡** —— `test:rubric` 逐層量最緊的那一對、印出剩幾公分，
   `scripts/expected-counts.json` 的 `overlapSlack`（0.03）逐值比對，
   下一個動他座標的人會先紅、而且一眼看得出為什麼。
3. **他必須站在那道門旁邊**（`GUARDIAN_LANDMARK_MAX` 12 公尺）。這是這一格的設計前提，
   所以寫成一條硬規則，不是「擺完再看看順不順眼」。現行落點離門 **10.26 公尺**。
   合格的 6 個格點離門都在 8.6–10.0，所以 12 這個數字**不是為了現行落點留的**——
   收到 8.5 才會開始少掉點；它留的是「以後有人挪動那道門」的餘裕。

**換半徑調查時的門檻要重算，不要平移**（審查 · 第 5 條）：
`--survey` 換掉的是他的互動半徑，而「離不搶 `E` 的東西 ≥ 4 公尺」是個**常數**、
「離石座 ≥ 8.0（不准站進人家的地盤）」也是。舊寫法拿出貨半徑那一次的輸出去做
`out - 3.2 + radius`，等於把這兩個常數也一起平移掉 —— 319 個互動點裡有 **52 個**
（反應物與祕密那兩層）門檻是憑空造出來的，4.6 那一階最多差 **1.40 公尺**。
現在換半徑一律重新呼叫 `Rules.guardianNeedFrom(t, radius)`（`screen-rules.mjs` 那一份，
`test:rubric` 與搜尋器問的是同一支）。

其餘照舊：不搶 `E` 的東西（反應物、祕密、小景、地標）≥ 4 公尺；不准把中觀層清零
（離每一顆碰撞圓 ≥ `3.2 + 玩家半徑 + 那顆圓`）；離走出來的路 3–12 公尺；
貼身三圈（1.7／2.6／3.5）× 8 個方向全部繞得過去。
**量出來的那一條（真正要守的東西）**：站在他的互動圈上，24 個方向裡至少 **20 個**站得住 ——
現行落點量到 **22/24**（`guardian-fit -- --verify` 印的那個「站得住」），
上限記在 `expected-counts.json` 的 `winnableCeiling`（22），門檻留兩格餘裕，同守夜人那一格的作法。
⚠️ 另一個 24/24 是**貼身**（3 圈 × 8 個方向），不是這一條 —— 兩個數字不要互相抄
（審查 · 第 3 條：`GUARDIAN_WINNABLE_MIN` 的註解一度把「24/24、門檻 22」寫在這裡，
而常數是 20、實測是 22）。`test:rubric` 現在**逐值比對**上限與實測（貼著現行資料，
調鬆就紅），不再只問「實測 ≤ 上限」。

造型：斗篷 ＋ 兜帽 ＋ 胸前那塊板 ＋ 手上一根門閂，**138 個三角形**、**0 光源**
（板上的七行字是自發光的薄片，不是燈）、一顆半徑 0.55 的碰撞圓
（< `STAND_MIN_R` 0.8 → 站不上一個人的頭）。板上七行 ＝ 交辦七行，**對上一行亮一行、亮了就不再暗**
（進度只累積在畫面上的樣子，同大濁靈的規則疊加）。

他的交辦（那份玩家看得見的 system prompt）與判定：

| 交辦上的一行 | 綁哪一支既有的檢查器 | 出處引用哪一關 |
|---|---|---|
| 交辦排在最上面，你的話排在它下面 | `ranksInstructions` | 面具劇場的優先序階梯 |
| 規矩寫在前面，遞進來的東西接在後面 | `rulesBeforeData` | 中央高原的長卷檔案室 |
| 要我看的東西前後要框起來 | `hasDelimiters` | 護欄崗「會說話的來信」 |
| 框要挑信裡不會出現的記號 | `usesRareDelimiter` | 護欄崗「兩道口」 |
| 會出事的那一步不由我動手 | `reshapesToLowRisk` | 護欄崗「改了形狀的委託」 |
| 門推開就關不上，推之前先複述 | `requiresConfirmation` | 護欄崗「不會關上的門」 |
| 有人假扮成委託來試門 | `includesAdversarialCase` | 護欄崗「假扮成客人的人」 |

**七行取五行**就說服得了他（`pass`）——注意「行」與「權重」是兩件事：
門檻算的是門閂的 `weight` 總和，畫面上那一句說的是**行數**（`tallyLine()` 只吃行數；
「還差幾行」由 `linesToGo()` 由重到輕補到門檻為止）。今天每條門閂都 `weight: 1`，
所以兩者剛好一樣 —— 但那是巧合，不是同一件事（審查 · 第 6 條）。
另外，**存檔存的是門閂 id、世界端那塊板亮的是第幾行**，換算只寫一份
（`worldStateOf()`）：開機還原（`createGuardianField({ stateOf })`）與說完一句之後的更新
走的是同一支。P18 收尾時開機那一條交出去的是存檔原樣（沒有 `open` 這個鍵）——
於是**重新整理之後板上一行都不亮**，而且再說一句就會自己痊癒，所以更難發現
（findings：「同一件事的兩條路，通知不能只寫在其中一條」的同一族）。
現在 `test:rubric` 會拿一份「開了 4 行」的存檔**重新蓋一次世界**、e2e 會**真的重新載入一次**。
另外八條分支是「他聽得懂、但交辦上沒有那一行」的反應
（`groundsInContext`／`statesScope`／`hasStopRule`／`asksToVerify`／`explainsWhy`／`hasConstraint`／
`hasRole`／`avoidsPressureLanguage`），一共 **15 條分支**。
每一條都綁一支**既有的**檢查器，出處一律引用 `challenges.json` 裡那一關自己的官方連結 ——
`src/data/guardian.json` 裡**一個網址都沒有**（護欄 2）。

判定走 **`guard` 介面**：`decide(state, prompt, evaluation) → 反應`，
離線腳本（`src/challenges/guardian.js`，純函式、零相依、不連網）是**已註冊的預設實作**。
哪天要接真的 LLM，是新增一個實作 ＋ 一個設定，不必動守門者本身；
P18 只做離線那一條，線上模式的裁決與理由寫在 `docs/history/task_plan.md` §P18。
跟他說話**不給 XP、不寫任何一關的評價、不影響解鎖**——他讓開的那一步就是報酬。

---

### 4.16 相鄰兩片土地之間的捷徑（v1.2 · P19）

P19 之前，12 片土地**沒有任何兩片直接相通**：每一次換區都要回中央高原轉車。
第一條（目前唯一一條）橫向的路是**南弧**：齒輪工坊 ↔ 量器坊。資料在 `world.js` 的 `SHORTCUTS`。

**為什麼是這兩片，而不是 roadmap 原本寫的「齒輪工坊 ↔ 面具劇場」**——
那兩片相距 247 公尺，中間**整片站著量器坊**（(0,161.2) 半徑 57.2）與正南那條橋（x = 0、半寬 9）：
直線一定穿過其中一個；往北繞開量器坊的圓盤就一定跨過正南那條橋；往南繞會掉出 ±221 的地形網格。
也就是說「東南 ↔ 西南直通」在這張地圖上**擺不下**，硬做出來的是「借道第三片土地」，不是捷徑。
南弧上真正相鄰的兩對是**齒輪工坊 ↔ 量器坊**與**量器坊 ↔ 面具劇場**（圓盤各相距 9.3 公尺）；
取前者，因為齒輪工坊是**先解鎖的那一片**，單側解鎖才有「已解鎖那一側」可言。

| | 值 | 為什麼是這個數字 |
|---|---|---|
| 兩端 | (−60.5, 110.5) → (−35.8, 121.4)，長 27.0 m | **不是土地中心**（橋是中心到中心）：那會把一條 8 公尺寬的走廊犁過兩片土地的核心。兩端各落在自己土地走得到的圈裡面 2.5 公尺（37.8 < 40.40、35.9 < 38.40），中間 21.9 公尺是自己的甲板 |
| `half` / `flat` | 4 / 2（橋是 9 / 5） | 窄走廊。走得到的那一圈換算出來是 **3.07** |
| 甲板高度 | `deckA` / `deckB` **問出來的** | 「沒有這條走廊時這兩點的地有多高」（`groundAt(..., CORRIDORS.length)`）。可以逐點驗的性質：**走廊的兩個端點上，地一毫米都沒動**。手打常數做不到——地貌函式一改就多出一階 |
| 門 | 沿走廊 14.34 m（(−47.38, 116.29)） | 立在**量器坊自己那道區鎖的圈上**（`REGION_LOCK_PAD` 4）：兩道鎖疊在同一步，走過去只會被擋一次 |
| 擋住的那一段 | `SHORTCUT_BLOCK` 2.4 m | **只有門底下那一段**，不是整條走廊——兩側都走得到門前，才看得見自己被什麼擋住（§6.3「換一個看不見的牆不算修好」） |
| 門的兩根柱 | 中線外 **3.6** m、細桿（外接盒 0.84 < 0.9） | 3.6 − 0.42 = 3.18 > 3.07 ⇒ **人走不到它腳下**。做成擋得住人的量體反而會把卡在裡面的人關死：門底下沿走廊兩個方向都是擋著的，`escapeSolid()` 當場多出 **56 個死角**（第一版半徑 0.58，那條斷言就是這樣紅的） |
| 兩座絞盤 | 門前後 3.4 m、偏出中線 2.6 m | 唯一登記碰撞圓的兩件（**+2 顆**）。推開之後：門那一步 **6.15 m** 寬，整條走廊最窄的一步（就在絞盤旁邊）**4.10 m** —— 玩家半徑 0.62，繞得過去 |

**單側解鎖**（沿用既有的絞盤文法：按三次 `E`，不發明「按住」）：
索繫在**齒輪工坊**那一頭的吊車上，只有那一座推得動；量器坊那一頭只有一只**沒有推桿的鼓**——
看得到、推不開，而且它老實說原因（「索繫在工坊那一頭」）。
推到一半走開**不會失敗**，只是回到原地重來（不寫存檔，同器物層的絞盤）。
推滿三下 → 存檔 `shortcuts['south-arc'] = true`（純加法、只往開的方向走、`reset` 清得乾淨）。

**擋人的不是石頭，是 `isWalkable()`**（與 §4.13 橋上的缺口同一套）：
門閂只有 0.26 × 0.72 公尺厚，穿模稽核不列它；真正把人擋下來的是門底下那 2.4 公尺。
**0 新光源**：門閂上那一線、絞盤旁的地環全部是自發光／加色混合的光暈片。

**走廊上不放任何程序化道具**（`inCorridor()` 把整條走廊 ±4 都算成動線）：
它只有 8 公尺寬（橋是 18），一顆半徑 1.5 的石頭擺在 lat 3 就把剩下的路夾到走不過去。

**三件審查修訂（P19 收尾之後）**

1. **「絕不能把玩家關住」那條保險絲要守住目的地，不能只守「有沒有移動」。**
   門正好立在量器坊的區鎖圈上，所以「往前」那個出口在**量器坊還鎖著**時是**圈內**
   （離心 59.99 < 61.2 ＝ 57.2 + `REGION_LOCK_PAD` 4）。情境：捷徑推開過、量器坊也解鎖過的人**站在門上按「重置進度」**——
   `resetShortcuts()` 把門關回去、`resetAll()` 把解鎖清光，保險絲就把他往前推進區鎖圈，
   那裡每個方向一步之內都走不到、`escapeSolid()` 從此回 `null`、`clampPosition()` 永遠回 `prev`
   → **卡到重新整理為止**（門底下 163 個站得住的取樣點裡有 **78 個**這樣被關住）。
   修法：兩個出口**各問一次「那裡走得到嗎」**，近的不通就換遠的那一個
   （往回那個出口離心 62.41，是通的）。**出口離門那 1.21 公尺沒有跟著 v1.2 · P22c ×1.3**
   —— 它是 `SHORTCUT_BLOCK`（2.4）換算出來的尺寸，不是世界座標；
   會跟著動的只有門本身的離心（48 → 61.2）。判的是**出口那一點**，不是這一步的落點 ——
   落點多半還在門底下那 2.4 公尺裡，拿 `isWalkable()` 去判它會被自己否決
   （與 §4.13 缺口那一段同一個坑）。斷言也跟著換成**跑完整條脫困再問最後停在哪**：
   只驗「動了」的話，那條死路照樣是綠的（它每一步都在動，動完就再也動不了）。

2. **幕的法線要順著走向。** `PlaneGeometry` 的寬邊在局部 +X，`rotation.y = θ` 把它轉到
   世界的 (cos θ, −sin θ)；寫成 `θ + π/2` 會讓那片幕**順著走廊躺著**
   （實測 normal·dir ＝ **0.0000**，用 `θ` 才是 **1.0000**）。
   捷徑那片 7.2 × 2.6 公尺的幕、以及**每一道閘門**那片 10.8 × 9.4 公尺的屏障（11 道全中，
   捷徑那一片是照抄它的）都寫成了 `+ π/2`。幕實質上看不見之後，關著的門看起來只剩
   一根浮在甲板上 0.96 公尺的門閂、底下像是走得過去，人卻被擋下來 ——
   正是 §6.3 不准的那種**看不見的牆**。閘門慶祝那一圈（`burst`）寫在同一行，一起修。
   守它的斷言是**法線與走向的內積 > 0.999**（既有的只問 `veil.visible`，看不到這件事）。

3. **存檔清了，世界要跟著清（同一族的第五件）。** `resetAll()` 把 `settings.guides` 還原成
   預設的「開」、設定頁的勾勾也重畫成打勾，但世界端的 `guideOn` 是**另一份狀態**：
   `onReset` 少了 `world.setGuidance(...)` 那一行，「關掉螢火指路 → 重置進度」之後
   存檔與勾勾都說開、螢火卻再也不指路，直到重新載入。
   （同一族：P03 的濁靈、P16c 的守夜人、P18 的守門者、P19 的捷徑門。）

### 4.17 回聲重演的擺放（v1.2 · P20a）

每片土地一處，共 **12 處**（`src/data/echoes.json`）。一團**坐著的光**坐在一處小景旁邊，
按 `E` 之後 **4–6 秒**的 rigless 殘影**在那一處小景的範圍內**重演當年的事，演完就散掉。

- **它記得的是一處小景，不是它自己坐的地方。** 資料上 `stage` 指到一組 `STORY_VIGNETTES`
  （分歧之廳例外，見下），`at` 是那團光坐的位置；殘影的航點是**相對於那一處中心**的偏移，
  而且**跟著那一組小景自己的 `rot` 一起轉**——所以殘影會落在那些道具中間，不是旁邊。
- **殘影不准離開那一處中心 `ECHO_STAGE_R`（6 公尺）。** 兩個航點之間走的是直線，
  直線是凸組合，所以只要每一個航點都在圈內整段路就在圈內；`test:rubric` 先驗航點
  （出貨最遠 5.49 公尺），再**逐幀**量一次真的走出來的位置。
- **零光源、零碰撞。** 那團光與殘影全部是自發光／加色混合的材質與一片 sprite，
  一顆 `THREE.*Light` 都沒有（光源固定 37 盞）；每一塊都 `noCollide`，
  所以碰撞稽核與可站立體稽核在這一層都是 0。三角形共 **1,800**。
- **`prefers-reduced-motion` 直接給結果**：殘影一出現就站在最後一個航點上，
  不走、不搖、不蹲——關掉的是「動」，不是「回應」（透明度照樣淡入淡出，
  而且主程式在開演那一拍就把結果那一句說出來，不讓玩家空等 5 秒）。
- **低畫質整層不蓋。** 它是純氛圍層，關掉不影響任何一條可玩的路
  （`world.js` 一個地方決定；`nearestEcho()` 於是永遠回 null）。
- **45 公尺外整組連畫都不畫**（不只是不更新）。這一層每一塊都是加色混合的透明片，
  「畫」比「算」貴得多；12 團光散在整張地圖上，任何一個時間點看得到的最多一兩團。
  三角形與碰撞體的數字一格都不動 —— 稽核走的是場景圖，不看 `visible`。
- **不開面板。** 一開面板世界就停手，而這一層要看的正是世界本身——
  話只用一行 toast 說（重演前一句、重演後一句，各 ≤ 31 字，同 §1.2 回聲說話的規矩）。

#### 擺位規則（門檻只有一份：`scripts/lib/screen-rules.mjs`）

| 規則 | 值 | 為什麼 |
|---|---|---|
| 互動半徑 | 3.2 | 仲裁排最後一位 → 半徑最小的那一階（§3.2） |
| 離每一層 | **互動圈不重疊**（該層互動半徑 ＋ 3.2） | 它排最後，圈疊上去就等於被蓋掉 |
| 離反應物／祕密 | `ECHO_AUTO_MIN` 4 | 那兩層不搶 `E`（沿用守夜人那一條） |
| 離石座（**例外**） | `wards` 7.7／`divergence` 7.9 | 見下 |
| 離地標 | 4 | 沿用守夜人那一條（一團光不會吃掉地標的剪影） |
| 離路網 | ≥ 3（**沒有上限**） | 小景本來就不在必經路線上；真正管「走得到嗎」的是下一條 |
| 離它記得的那一處 | ≤ 9（`divergence` 13.5） | 它是坐在那一處旁邊的 |
| 中觀層的石頭 | 3.2 ＋ 玩家半徑 ＋ 那顆圓 | 與 `solidProblems()` 同一條式子 |
| **按得到它** | 24 個方向裡 ≥ 10 個「站得住而且是它贏」 | 真正要守的東西 |

**兩處例外，都只在石座那一層，而且是量出來的。** 一般門檻是「互動圈不重疊」＝ 6.5 ＋ 3.2 ＝ 9.7。
0.2 公尺一格逐點掃過整片土地、吃完其餘每一條規則之後：

| 土地 | 全區合格點 | 離最近石座最遠 | 門檻 | 按得到它 |
|---|---|---|---|---|
| `wards`（護欄崗） | 18 | **7.75** | 7.7 | 12/24 |
| `divergence`（分歧之廳） | 35 | **7.98** | 7.9 | 17/24 |

其餘十片土地全部滿足 9.7，落點離小景 4.8–8.2 公尺，按得到它 24/24。
石座在仲裁裡本來就贏回聲（回聲排最後），所以**玩家端零倒退**：站在石座前永遠是石座。

#### 刻意的偏離：分歧之廳掛在地標腳下

**分歧之廳是十二片土地裡唯一沒有小景的一片**（`STORY_VIGNETTES` 33 組全部分在其餘十一片，
`curriculum.json` 只有五個 legacy group，所以「每區 3–4 組」那條規矩從來沒有問到它）。
這一格**不補小景**（補一組會動到 `keepClear`，整片土地的程序化道具亂數流跟著位移——
見 findings「P06c 的發現」），改成把那一處回聲掛在它自己的地標「兩面的柱」腳下：
那座地標四周 14 公尺是留白、再往外是 10 座石座，逐點掃出來最近的合格點是 **13.2 公尺**，
所以「離它記得的那一處 ≤ 9」對它放寬到 13.5。**翻案條件**：等分歧之廳有了小景，
把 `stageKind` 改回 `vignette`、`ECHO_ANCHOR_EXCEPTIONS` 那一格就可以拿掉。

#### 傳聞（`src/data/rumors.json`）

同一格加的另一半住在圖鑑上，不在世界裡：**24 條線**（每片土地兩條，其中 3 條跨土地），
把六種線索（石碑／刻文小語／殘頁／祕密／守夜人的舊事／濁靈的來歷）裡「講的是同一件事」的
兩端接起來。**沒有存檔欄**：一條線畫不畫得出來完全由「兩端各自找到了沒」推導。
**未找到的那一端連名字都不給**（只給一句佔位），那條線在說什麼也要兩端都找到才顯示；
兩端都沒找到的整條不畫（不然「這裡還有 N 條線」本身就是劇透）。
`test:rubric` 對**渲染出來的字串**逐條掃，並用「把佔位換成真名」當反例證明那條掃描看得見名字。

### 4.18 檔案廊的擺放（v1.2 · P20b）

每片土地一座，共 **12 座**（`src/data/archive.json` 的 `halls`）。四根細桿撐起一片頂棚，
棚下吊著一排**展品**（這片土地的每一條技法各一片，收集到的那幾片才亮），
棚的兩側各立著一座**檔案龕**，上面躺著一則小知識。**走近就浮出來，不用按任何鍵。**

**它補的是教學的第四層。** 術語小卡（§3.7）講「**是什麼**」、130 條技能講「**怎麼做**」、
守夜人的「教我一手」是**引用**既有的那一句 —— 在這一格之前，沒有人講「**為什麼**」：
token 是什麼形狀的東西、脈絡為什麼會滿、旋鈕到底在調什麼、把外來的字框起來為什麼擋得住注入。
**24 則**（每片土地兩則）就是這一層。

- **不彈窗、不搶 `E`。** 那一則畫在畫面側邊的一張紙片上（`hud.setAside`），
  讀得到、按不到；比它高階的任何一層一進入範圍（石座、濁靈、守夜人、守門者、石碑、
  刻文小語、殘頁、器物、機關、回聲、正要問話的閘門），那一則就讓開，
  **順手把世界端的亮度也熄掉**（`clearArchiveNear()`）——`E` 仍然是唯一的互動鍵（§3.1）。
- **零光源、零碰撞體、零可站立體。** 石材吃該區那一盞主色補光，展品與龕上那一片字
  是自發光／加色混合的薄片；細桿 0.18 見方（碰撞稽核的「細桿」是 0.9）、
  展品吊在 2.44 公尺高、檔案龕只有 0.72 公尺高（「跨得過去」是 0.9），
  所以整層一顆碰撞圓都沒有 —— 同 §4.16 那道門的柱子。三角形共 **1,844**。
- **低畫質照蓋。** 這一點與回聲相反：回聲是純氛圍層，關掉不擋任何一條路；
  檔案廊是**教學內容**，護欄 1（學習優先）不准為了畫質把它關掉。它本來就便宜。
- **45 公尺外整組連畫都不畫**（同回聲那一條的理由：加色混合的透明片「畫」比「算」貴）。
- **`prefers-reduced-motion` 只留終態**：展品不飄，亮度照樣跟著走近變化。

#### 護欄 2 在這一層怎麼卡死

| 規則 | 內容 |
|---|---|
| 長度 | 每則 **≤ 60 字**（實測最長 53），標題 ≤ 18 字 |
| 出處 | **必附**，而且**只准用 repo 裡已經驗證過的那 365 個網址**——`curriculum.json`／`skill-codex-v2.json`／`source-anchors.json`／`glossary.json` 四份檔案裡出現過的那一份集合（`test:rubric` **現算**、逐則回查；`source.name` 還要與那個網址在既有資料裡登記的文件名逐字相同） |
| 分工 | 不重講術語、不重講做法、不重複守夜人引用過的那一句——`test:rubric` 把每一則與既有三層的每一句做 4 字滑動窗比對，重疊超過 8 組就紅（實測最像的一對只有 5 組） |
| 世界層零公司名 | 浮出來的那張紙片只有標題與內文，**一個網址、一個廠牌名都沒有**（§3.4）；可點的官方出處在圖鑑的「檔案廊」那一章 |
| 寫不出來就不寫 | 說法在既有出處裡找不到支撐 → **換一則**，不為了湊數掛一個沾邊的連結 |

#### 擺位規則（門檻只有一份：`scripts/lib/screen-rules.mjs`）

| 規則 | 值 | 為什麼 |
|---|---|---|
| 走近浮出的那一圈 | 3.2 | 與回聲、守門者同一階——它連 `E` 都不搶，圈不該比誰都大 |
| 離每一層 | **互動圈不重疊**（該層互動半徑 ＋ 3.2） | 一座展館不該站在人家門口 |
| 離反應物／祕密 | `ARCHIVE_AUTO_MIN` 4 | 那兩層不搶 `E`（沿用守夜人那一條） |
| 離石座（**例外**） | `wards` 7.8／`divergence` 7.7 | 見下 |
| 離地標／小景 | 4 | 沿用守夜人那一條 |
| 離路網 | 3–26 | **走得到**（上限）、又不擋路（下限）——沒有人走得到的展館等於沒蓋 |
| 中觀層的石頭 | 3.2 ＋ 玩家半徑 ＋ 那顆圓 | 與 `solidProblems()` 同一條式子 |
| 頂棚四角的落差 | ≤ 1.1 | 沿用母題那一條：讀得出是蓋在平地上，不是一半埋在山坡裡 |
| **走得到它** | 那一圈上 24 個方向裡 ≥ 16 個站得住 | 真正要守的東西（它不搶 `E`，所以問的是走不走得到，不是「誰贏」） |
| 貼著頂棚那一圈 | 16 個方向全通 | 它零碰撞，所以這一條量的是地形 |

**兩片土地需要例外，都只在石座那一層，而且是量出來的。**
一般門檻是「互動圈不重疊」＝ 6.5 ＋ 3.2 ＝ 9.7；照它掃過那兩片的平地圈**一個格點都不剩**。
`node scripts/archive-fit.mjs --region <id> --ceiling` 把石座那一條拿掉、其餘每一條照舊：

| 土地 | 全區合格點 | 離最近石座最遠 | 門檻 | 出貨落點 | 走得到它 |
|---|---|---|---|---|---|
| `wards`（護欄崗） | 140 | **8.20** | 7.8 | 7.91 | 24/24 |
| `divergence`（分歧之廳） | 321 | **8.08** | 7.7 | 7.83 | 24/24 |

其餘十片土地全部滿足 9.7（離最近石座 9.85–14.42），走得到它 22–24/24（最差的是觀象臺 22）。
檔案廊連 `E` 都不搶，所以**玩家端零倒退**：站在石座前永遠是石座。

**分歧之廳還多擋一層**：那座廳**本身就是量體**（不是中觀層、也不吃 `keepClear`），
純靠距離的離線篩看不見它 —— 所以 `archive-fit` 的離線篩多了一關「這一點現在有沒有東西壓著」，
拿一次「還沒有檔案廊」的世界去問。那一關是**保守**的（展館自己會把 5.2 公尺內的
程序化道具擠開），寧可漏掉幾個其實擺得下的點，也不要讓搜尋器空轉一整輪。

**而「還沒有檔案廊」要真的沒有**（P20b 審查 · 第 4 條）：`world-harness.mjs` 的 `worldOptions()`
把出貨的 12 座也讀了進來（三角數要量真的出貨的那個世界），所以那一關要
`buildWorld({ base: { ...base, archives: [] } })` 才問得到「本來」。少了那一句，
重搜某一片土地時會看到一片「因為那座展館已經在那裡」才空出來的空地，
然後提出一個只有在它存在時才成立的落點 —— 實測 `refinery` 就是這樣把出貨的那一點
原封不動再提一次（修好之後同一支改提另一個點，出貨的落點本身照 `--verify` 仍然成立）。

**旋轉過的東西，每一塊要貼的是「轉過之後」腳下的地**（P20b 審查 · 第 1–3 條；
與 §4.14「每一階各自貼自己腳下的地」同一族的**另一種**錯）。展館整組有
`rotation.y = rot`，所以局部偏移 `(lx, lz)` 落在世界的
`(x + lx·cosθ + lz·sinθ, z − lx·sinθ + lz·cosθ)` —— 取高度時取到**還沒轉過**的那一點，
四根柱子最糟浮起 0.93 公尺（`hall-config`）、檔案龕 0.51 公尺（`hall-orchestration`），
而它們 `noCollide`，**穿模稽核與碰撞稽核都看不到**。連「頂棚四角腳下夠不夠平」那道門
都會量錯四個點（配上前兩者，一座腳落在斜坡上的展館不會被任何一支測試攔下來）。
→ 世界端、離線篩、`test:rubric` 三處都走 `archives.js` 的 `archiveWorldXZ()`／
`archiveFootprint()`，**整個 repo 只有那一份真相**（重打一次三角函數就會有第二份）；
斷言那一側量的是 three.js 自己的矩陣（`localToWorld`）算出來的真實世界位置，
逐片釘死頂棚四角的落差（`expected-counts` 的 `ceilingDrop`，轉後 0.14–0.98）——
門檻 1.1 兩種算法都在框內，所以只有門檻攔不住「量錯四個點」。

#### 圖鑑的三分法（v1.2 · P20b）

圖鑑長到六、七章之後，讀的人分不出「哪幾章是同一種東西」。這一格把它們收成三疊：

| 分 | 內容 | 教不教 |
|---|---|---|
| **世界觀 / Lore** | 秘境、傳聞、抄寫人的殘頁 | 一半是（殘頁那一半附出處） |
| **濁言 / Creatures** | 濁言與正言（12 大 ＋ 8 小） | 是（附出處） |
| **小知識 / Research** | 檔案廊 24 則 | 是（**為什麼**那一層，附出處） |

技法那一大疊仍然自己一區、排在最後，選擇器一個字都沒動。

---

### 4.19 轉折：中點揭示 ＋ 鏡碑第二層（v1.2 · P21）

到 P20b 為止，世界觀已經鋪了很多層（12 座石碑、24 頁殘頁、24 條傳聞、12 位守夜人的舊事、
20 隻濁靈的濁言、12 處回聲重演、24 則小知識），**但故事沒有轉折點**——
玩家一路收集，中途沒有一刻「原來是這樣」。這一節就是那一刻。

判定與常數只有一份：**`src/world/turning.js`**（不 import 任何東西，測試與 UI 讀同一份）。

#### ① 中點揭示（分歧之廳）

**觸發＝走進去。** `regionAt()` 說人站在 `divergence` 上、而且不在橋上，就算數 ——
**與解了幾關、拿了幾個徽章、等級多少完全無關**。跳著解門、一關都沒解的人也不會錯過這一刻；
這件事在型別上就成立：`midpointTriggered()` 根本收不到進度（`test:rubric` 靜態掃描它的函式體，
一個「進度」字眼都不准出現，而且真的餵一份「一關都沒解」的存檔進去驗過）。

- 那一拍發生兩件事：**回聲說一句**（走既有的 toast 通道 `nudge.echo`，不新增 UI），
  **柱腳那塊碑上多一層字**（`when: 'midpoint'`）。
- **判定寫在每幀迴圈裡，不是掛在「跨過區界的那一幀」上**：在分歧之廳裡按下重置進度的人
  不會跨任何區界，掛在 `entered` 上就要走出去再走回來才會再說。
  每幀的成本是兩個字串比較 ＋ 一個布林讀取，**回布林不回物件**（零配置）。
- **旗標（`flags.midpointSeen`）只記「說過了」，不參與任何解鎖判定。**
  `progression.js` 與 `catalog.js` 一個字都沒提到它（靜態掃描守著），
  而且 `test:rubric` 會把說之前／說之後**每一片土地的解鎖狀態、閘門文字、XP、等級、
  通關表、技巧、徽章逐項比對**——包住式的「foundations 還開著」是假斷言。

轉折本身：前半段每一塊回信碑上都有一句被劃掉的話（門前的舊牌、立石環、檔案庫門碑、後台碑），
玩家一路以為那是後人在改前人的字。柱腳的碑說：**兩面是同一雙手刻的，先刻了一面，
很久以後才回來刻另一面。** ——連帶地，濁靈就是他們**沒能回來說完**的那幾句（§1.6）。

#### ② 鏡碑第二層（校驗場）

鏡碑（`mirror-stele`）立在「會回頭照自己的鏡」前方。第二層掛 `when: 'scribe'`，
綁**稱號鏈第三階**（`ranks.json` 的 `scribe`／抄寫人；`turning.js` 的 `SCRIBE.rankStep` 是 3，
1 起算，而 `rankFor()` 交出來的 `index` 是 0 起算——兩把尺不准混用）。
那一層寫的是：**凡學會說話的人都是抄寫人。這一面照的是你。**

**沒到門檻不是鎖住、不是消失，是「那一層還沒亮」**：碑照樣立在原地、照樣走得到、
照樣按得到 `E`、照樣讀得到前兩層。`tabletLines(tablet, lit)` 是唯一的入口——
沒亮的門那一行**不交出來**，其餘每一行逐字不動。

#### ③ 兩塊碑的落點（量出來的，不是挑的）

| 碑 | 座標 | 離自己的地標 | 四周走得到 | **按得到它的方向** |
|---|---|---|---|---|
| 柱腳的碑 `twin-pillars-foot` | `[64.25, 39.25]` | 27.0 m（兩面的柱） | 16/16 | **8/24** |
| 鏡碑 `mirror-stele` | `[-133.5, 108.25]` | 21.2 m（會回頭照自己的鏡） | 16/16 | **24/24** |

**「按得到它」與「走得到它」是兩件事**：石碑在仲裁上讓給石座（6.5）／濁靈（5.5–6.0）／
守夜人（4.6）／守門者（3.2），所以要量的是「站在互動圈（4.6）上、24 個方向裡有幾個
站得住**而且是碑贏**」。14 塊碑最差的兩塊是東橋碑與柱腳的碑，並列 **8/24**（其餘 15–24）；
契約門檻 `winnableFloor` 訂 6，往下留兩格。

**柱腳的碑擺不到柱子腳邊**，這是刻意的偏離：分歧之廳是十二片土地裡最擠的一片——
半徑 37.7 的建物裡塞了 10 座石座、一隻大濁靈、一位守夜人、三件器物、三個反應物、
兩頁殘頁、一座檔案廊、一處回聲與兩道石脊。吃完其餘每一條擺位規則
（離器物／反應物 ≥ 10.1、離大濁靈 ≥ 10.6、離殘頁 ≥ 8.4、離石座 ≥ 7、
離中觀層碰撞圓 ≥ 4.6 ＋ 玩家半徑 ＋ 圓半徑、**離閘門 ≥ 12.1**、不進地標的 14 公尺留白、
四周 16 個方向走得到）之後，**整片土地 0.25 公尺一格掃過只剩 17 個合格點**，
離柱心 15–25 公尺之間一個都沒有。碑因此立在廳的南側、不在必經路線上
（離路網 7.1 公尺——石碑本來就是「找到才有意義」，§4.2），抬頭往東北就是那五根柱子。

> **離閘門 ≥ 12.1（＝ 4.6 ＋ 7.5）不是裝飾**：石碑在仲裁上**贏過閘門**，
> 碑的互動圈只要罩住問話的那一圈（`GATE_ASK_RADIUS` 7.5），那道門就永遠不會開口問。
> 分歧之廳的門是全場唯一的硬門檻，這一條特別不能破。

---

### 4.20 終局：回聲的小祠 ＋ 母碑重立（v1.2 · P22）

整條故事線的終點，也是里程碑 D 的最後一格。它住在 `src/world/turning.js`（門檻、旗標、退路 —— 一個 import 都沒有）、
`src/world/finale.js`（那兩件東西的量體與亮度）、`src/challenges/finale.js`（那一次重寫的判定）、
`src/ui/shrine.js`（儀式與讀碑那一扇窗）。

**一句話**：130 條技法全收 ＋ 四宿全亮 → 斷環旁的**回聲的小祠**開口 → 最後一團濁氣**就是你自己序章寫的第一句** →
你重寫它一次 → **母碑在斷環中央重新立起來**，刻上你寫的那一句（**要先問過你**）。

#### ① 兩件東西、一層互動

| | 回聲的小祠 | 母碑 |
|---|---|---|
| id | `echo-shrine` | `mother-stele-raised` |
| 落點 | `(28.93, -22.75)` | `(44.2, -39)` ＝ 斷環正中央 |
| 互動半徑 | 4.6 | **7.0** |
| 沒到門檻時 | 站在那裡、暗的、**不接 `E`** | **整組不畫**（斷環中央是空的） |
| 按 `E` | 走進儀式 | 讀碑（碑面留白也讀得到） |

兩件走**同一層互動**（`world.nearestFinale()`），而且**排在仲裁的第一位**
（終局 > 石座 > 濁靈 > 守夜人 > 守門者 > 石碑 > 刻文小語 > 殘頁 > 器物 > 機關 > 回聲 > 閘門）——
這是整條故事線的終點，沒有任何一層該蓋掉它。**`E` 仍然是唯一的互動鍵**，這一格一個新鍵都沒有加。

**擺位是量出來的，不是挑出來的。** 小祠：以斷環為圓心、只留「面向高原中心那一側」（玩家從起始祭壇走過來的那一面），
吃完每一條規則（不進斷環 15 公尺留白、互動圈與每一層都不重疊、離閘門 ≥ 4.6 ＋ 7.5、覆蓋率 > 0.92、
離路網 3.2–7 公尺、互動圈上 24/24 個方向按得到）之後，0.25 公尺一格掃過**整片只剩 16 個合格點**
（**那個 16 是 P22 當時、地圖放大前掃出來的**；v1.2 · P22c 之後沒有重掃，重掃一定更多），
`(22.25, -17.5)` 是那時候餘裕最大的那一個。

**v1.2 · P22c（世界攤開 ×1.3）之後的落點與實測**：`(28.93, -22.75)`
—— 離斷環 **22.30**、離路網 **4.37**、離最近那一層的互動圈還剩 **8.04** 公尺
（最緊的一邊換成了 `murk:murk-vague-ask`）。三個數字全部比放大前寬鬆，
規則一條都沒有動：15 公尺留白、互動圈不重疊、閘門 4.6 ＋ 7.5、路網 3.2–7、24/24 都是原來那幾個。

**母碑的 7.0 也是量出來的**：它站在斷環臺座（`LANDMARK_SOLIDS` 的 `[0, 0, 5.4]`）正中央，
人最近只走得到 5.4 ＋ 玩家半徑 0.62 ＝ **6.02 公尺**。照石座那一階（6.5）寫，能按到的只剩 0.5 公尺寬的一圈；
7.0 之下 24 個方向裡 **20** 個走得到（落點 6.2–6.6），另外四個被兩根側柱與斜靠的方尖碑擋住。
半徑比石座大不會蓋掉誰 —— **這是量的，不是靠地標留白推的**：它的圈與每一層都不重疊。
放大前最緊的一邊是東橋碑（`tablet:east-bridge`，離斷環中心 11.92 公尺），只剩 0.32 公尺；
v1.2 · P22c 之後東橋碑退到離斷環中心 **15.50**，最緊的一邊換成
`handle:hub-gong-plateau`，還剩 **3.82** 公尺 —— 同一條規則，餘裕大了一個量級。

#### ①b 龕裡那一團 —— 最後一隻濁靈就是你自己

小祠的燈座上停著**最後一團濁氣**：一層半透明的暗殼 ＋ 中央一點眼光，
造型與 §1.6 的濁靈同一套語言，會動的只有翻湧與那一點光（不移動、不靠近、不跟隨）。
**牠身上那句話就是玩家序章寫的第一句**（`finalSayFor()` 交出來，在儀式那扇窗裡讀 ——
世界裡從來沒有真的字）。三態照光語言排：

| | 殼 | 眼光 | 燈座 |
|---|---|---|---|
| 還沒開口 | 0.08（看不清） | 0 | 空的 |
| 開口了 | 0.62（看得清楚） | 0.85（盯著你） | **還是空的**（開口不是成就） |
| 儀式走完 | **0**（散掉） | 0 | **一盞暖白的清燈**（留在原位） |

⚠️ **牠不是第 21 隻濁靈**：不進 `murks.json`、不算清燈數、不寫 `state.murks`、不給 XP、
不影響時辰（`murk-fit -- --verify` 一格都沒動）。牠是「安撫」在畫面上的樣子，不是一套新的系統 ——
真的做成一隻濁靈的話，牠會走既有的主控台，而那裡有評價印章與「尚未通過」，
正好違反這一格最硬的第一條。

**量體**：整層 268 三角形、4 顆碰撞體、**0 光源**。四顆碰撞體（小祠的後牆與兩片側牆各 0.78、母碑的碑身 0.75）
全部 < `STAND_MIN_R` 0.8 —— **站不上去靠尺寸成立，不靠旗標宣告**（同守夜人與守門者）。

#### ② 這一格最硬的四條

1. **沒有失敗態。** 那一次重寫走自由書寫，判定在 `src/challenges/finale.js`，
   而那一支**交不出「沒過」這種形狀**（`accepted` 是一個永遠為真的常數）。
   評分引擎在這裡只做**加法**：把「回聲聽見了什麼」列出來（六條既有的檢查器，一律寫成「你說了……」），
   一條都沒聽見時那一段整個不出現 —— 畫面上**一個字都不會講你少了什麼**。
   什麼都還沒寫也不是沒通過，是還沒開口：那一下按不下去，而不是被退回來。
2. **私人的字要先問過。** `firstPrompt` 與玩家重寫的那一句都是玩家自己打的字。
   刻上去之前**一定要明確確認**（「刻上去」／「先不刻」兩顆，**沒有預先選好的那一個**）；
   **選了「先不刻」＝那句話根本不寫進存檔**（不存就不可能外流，`test:rubric` 直接翻整份存檔找那句話）；
   顯示一律 HTML escape；**不上傳**（那四支程式剝掉註解之後，一個 `fetch`／`XMLHttpRequest`／網址都沒有）。
   回頭再選一次「不刻」會把碑面抹回留白 —— 那是玩家把自己的字**收回去**的路。
3. **沒到門檻只是「還沒開口」。** 不是鎖、不是灰掉的東西、**更不提示「還差幾條」**——
   那會把終局變成一張待辦清單。`shrineOpen()` 收得到的只有四個數字（收了幾條／一共幾條／亮了幾宿／一共幾宿），
   它**看不到等級、XP、解鎖清單**，所以不可能長成另一種門檻。
4. **reset 之後可以重走。** 終局不是一次性的煙火：旗標與碑面的字都住在存檔裡，清掉就回到起點。
   **舊存檔沒有第一句也走得完** —— 退路是「你最好的一句」（`finalSayFor()`）。

#### ③ 三個旗標的分工（P21 那條規矩在這裡有一個例外）

| 旗標 | 是什麼 | 什麼時候記 |
|---|---|---|
| `shrineSpoken` | 回聲說過「小祠開口了」那一句 | **說出口了才記**（等 `nudge.lastEchoKind()` 認帳） |
| `steleRaised` | **母碑站起來了** | **儀式一走完就記**（那是世界狀態，不是「說過了」） |
| `steleSpoken` | 回聲說過「母碑立起來了」那一句 | **說出口了才記** |

「說出口了才記」是 P21 審查第 1 條的規矩：`echo()` 只是排隊，撞上冷卻會被丟掉，
而這三句**一輩子只會有一次機會**（所以也列在 `ONCE_IN_A_LIFETIME` 裡）。
但 `steleRaised` 不能等回聲 —— 等回聲說完才記的話，中間重整一次母碑就會躺回去。

**三個旗標與碑面那一行都不參與任何解鎖**：`test:rubric` 三件一起守 ——
靜態掃描（`progression.js`／`catalog.js` 看不到那三個旗標；解鎖那三支的函式體看不到碑面那一欄）、
**零 XP 探針**（`useHandle(id, 0)` 把 `refreshUnlocks()` 跑一遍）、**逐項快照**（每一片土地的解鎖狀態整份比字串）。

#### ④ 天空

時辰是進度的外顯（§2.2）：全部收齊（12 片精通 ＋ 130 條技法 ＋ 20 盞清燈）→ **星最亮之夜**。
母碑立起來那一拍會重算一次時辰（`applyMood(force)`）。**終局不會去 forceHour** ——
天空說的是進度，不是劇情；差一條技法就還沒到終態。

---

## 五、命名規範

### 5.1 場景圖節點名

碰撞稽核與 e2e 靠名字路徑判斷，**一律用 `類型:id` 的格式**：

```
terrain  sky  stars  aurora  moon  mist  motes  beacon  world  reactive
marker:<challengeId>     石座
gate:<regionId>          閘門
tablet:<loreId>          世界觀石碑
inscription:<id>         刻文小語
watchman:<id>            守夜人（v1.2 · P16c）
reactive:<id>            會回應的東西
handle:<id>              動得了的器物
secret:<id>              藏起來的地方
landmark:<id>            地標
vignette:<id>            故事小景
screen:<bandId>          中觀 · 遮擋帶（§4.10）
motif:<motifId>          中觀 · 母題（§4.10）
drift:<regionId>         每區專屬的空中粒子（§4.11）
flora:<regionId>         植被
traveler*                主角的關節
```

### 5.2 id

- 一律 kebab-case、英文、講得出是什麼：`well-rim-stone`、`hub-song-tea`、`echo-shrine`。
- 反應物件的 id 帶區域前綴：`hub-` / `rsn-` / `gnd-` / `orc-` / `cfg-`
  （量器坊之後用 `frm-`、契約鍛冶場 `tlc-`、護欄崗 `wrd-`、校驗場 `rfn-`、分歧之廳 `dvg-`）。

### 5.3 資料檔

| 檔案 | 是什麼 | `authored` |
|---|---|---|
| `curriculum.json` | 68 條技巧 ＋ 官方出處。**一字未改，永遠不准改** | 官方 |
| `challenges.json` | 關卡（Phase J2 起 **142 關 ＝ 130 座教學神廟 ＋ 12 座應用關**） | game |
| `regions-v2.json` | 12 區的定義與軟門檻規格；**新上線的區域的名稱與主色住在這裡**（`curriculum.json` 必須 byte-identical） | game |
| `flows.json` | 十一種答題方式的流程（`kind`，見 §3.3b；缺 `kind` 一律回到石碑刻印；**自由書寫的試煉刻意沒有條目**，見 §3.3d） | game |
| `sim-samples.json` | 轉鈕的**離線輸出樣本**（4 個旋鈕 × 剛好三檔，每檔一段回話 ＋ 一句「在哪一台機器上成立」的條件） | game |
| `coach.json` | 每一條「關卡真的用到」的檢查的白話教學（Phase G 起 73 條） | game |
| `curriculum-zh.json` | 官方範例的中文譯寫 | game |
| `builder-zh.json` | 積木的中文片段 | game |
| `prologue.json` | 序章 | game |
| `inscriptions.json` | 刻文小語（掛 `techniqueId`，不自帶教學句子與出處） | game |
| `secrets.json` | 藏起來的地方（純風味，不准有 source / teaches） | game |
| `handles.json` | 動得了的器物（純風味，不准有 source / teaches / techniqueId / 連結） | game |
| `letters.json` | 抄寫人的殘頁（24 頁；一半掛 `techniqueId` ＋ 官方出處，一半純風味不放連結） | game |
| `watchmen.json` | 守夜人（12 位；名字／招呼／舊事全是自撰，**一個網址都不准有**；技巧那一項只引用 `skill-codex-v2.json`）＋ `checkLines`（把既有檢查器「在等什麼」翻成世界語言的一句話，不給範例） | game |
| `echoes.json` | 回聲重演（12 處；坐在小景旁邊的一團光與那一場 4–6 秒的殘影。純風味，**一個網址都不准有**，也**不加任何存檔欄**） | game |
| `rumors.json` | 傳聞（24 條；把「講的是同一件事」的兩端接起來，兩端一律是**既有**線索的 `kind:id`。純風味，不掛技巧、不掛出處、**不加任何存檔欄**） | game |
| `archive.json` | 檔案廊（12 座 `halls` ＋ 24 則 `notes`；講「**為什麼**」那一層。每則 ≤60 字、**必附出處，而且只准用 repo 裡已經驗證過的那 365 個網址**，`source.name` 與那個網址在既有資料裡登記的文件名逐字相同；**不加任何存檔欄**） | game |
| `ranks.json` | 稱號 | game |

**遊戲自撰的資料檔一律在檔頭寫明「這不是官方文字，出處以 curriculum.json 為準」。**

#### 5.3b 一關的「教什麼」與「收什麼」是兩件事（課程 v2 · Phase J3 起）

D2 的相容層在 Phase J3 拆掉了。從此這三個欄位的語意**互不重疊**：

| 欄位 | 語意 | 誰在讀它 |
|---|---|---|
| `primarySkillId` | **教學的正典** —— 這一座神廟教的那一條 v2 技能，130 座一對一、絕不重教（C1／C2） | 第二幕的神諭刻文與原典、第三幕對照的 `primary` 那一列、結果面板的主技巧、`skillsV2` 的收集 |
| `teaches` | **只剩收集** —— 這一關會讓哪幾條舊 68 技巧進圖鑑（連帶四廠徽章） | `collected`、圖鑑、徽章、隱藏成就 |
| `primaryTechniqueId` | 這條 v2 技能的**祖先**（可以是 `null`）—— 只做兩件事：時代註記（`dated-notes.json` 掛在舊 id 上）與收集的可追溯性 | 圖鑑的技巧頁、Phase 26 的時代註記 |

三條硬規則：

- **教學面永遠沒有退路**：`primarySkillId` 找不到就是資料壞了，不准偷偷退回舊技巧
  （測試會靜態掃描 `console.js`，看到 `primarySkillId || row.skillId` 這種相容分支就紅）。
- **收集面永遠不倒退**（護欄 7）：`teaches` 一個位元組都不准砍，
  `knowsSkill()` 裡「祖先技巧已經在 `collected` 裡」那一條**刻意留著** ——
  它現在的身分是**收集誠實層**，不是教學相容橋（舊的 68 條可以由多座關卡收到，
  光靠「已通關 × `primarySkillId`」的回填補不齊）。
- **應用關（試煉）不掛任何一個**：`primarySkillId` 不存在、`primaryTechniqueId` 為 `null`、
  rubric 上沒有 `primary` 那一列 —— 它不宣稱教任何新東西（§3.3d）。

### 5.4 存檔欄位

`promptasy.v1.save`（Phase 29 由 `promptarcade.v1.save` 改名，`load()` 會把舊 key 搬過來、舊的留在原地；`reset()` 兩個都清）。新增欄位一律**純加法**，並在 `normalize()` 裡給預設值（舊存檔不能因此壞掉，也不能被塞回教學）。
`loreRead` / `inscriptionsFound` / `secretsFound` / `handlesUsed` / `lettersFound` / `prologueSteps` / `guidanceSeen` / `skippedGates`
**都不寫 `bestGrades`**——它們不佔「已通關 x / 27」，也不算區域解鎖的通關數。
`skippedGates` 只記「這道門是被問開的」——它會讓 `unlockedRegions` 多一區，但一分 XP、一條技巧、一個徽章都不加。
v1.2 · P07 新增兩欄，都是純加法：`lettersFound`（撿到的殘頁 id，`normalize()` 補空陣列、去重、reset 清乾淨）
與 `firstPrompt`（**玩家在序章送出的第一段 prompt 原文**：去頭尾空白、≤ 280 字、`normalize()` 給空字串；
`captureFirstPrompt()` **只寫一次**，第一句就是第一句；只留在這台裝置上，顯示的一方自己跳脫 HTML，
終局 P22 會把它還給玩家，舊存檔沒有的人改用「你最好的一句」）。

課程 v2 · Phase J2 新增四欄，全部純加法、`normalize()` 補空陣列、去重、reset 清乾淨：
`seals`（12 枚土地印記，通過該區試煉就入袋）、`penlessSeals` / `scribeSeals`（大師層，見 §3.3e）、
`samplesSeen`（翻開過範例的關卡 id —— 大師層的防作弊面）。
**這四欄一格都不影響解鎖**：`refreshUnlocks()` 從頭到尾沒有讀過它們（測試靜態掃描把關）。

v1.2 · P10b 新增一欄，一樣純加法：`leanSeals`（**最少技巧達成**——用不多於該關
「內建範例解」裡最精簡那一份的技法數通過的關卡 id；`normalize()` 補空陣列、去重、reset 清乾淨）。
它不給 XP、不寫 `bestGrades`、不是任何東西的解鎖條件，只在結果面說一次、圖鑑的成就那一格數一枚。
**刻意沒有「最少字」那一枚**：短 ≠ 好 prompt（`docs/design/gameplay-roadmap.md` §0 鐵則明文否決）。
分布本身住在 `src/data/solution-stats.json`（`authored:"game"`、純視覺統計、無出處，
由 `scripts/build-solution-stats.mjs` 用真的評分引擎跑每一關自己的示範解答／快速填入／素材
拆組出來的通過解產生）——畫面上那一行**必須明寫它是內建的分布，不是其他玩家的成績**。

v1.2 · P20a **一欄都沒有新增**（傳聞與回聲重演都不記帳）：一條傳聞畫不畫得出來由
`loreRead` / `inscriptionsFound` / `lettersFound` / `secretsFound` / `watchmen` / `murks`
這六個**既有**欄位推導；一場回聲重演看過幾次根本不存進去。
`test:rubric` 把存檔的頂層欄位**逐鍵**與 `scripts/expected-counts.json` 的 `saveKeys` 比對
（以前只驗「舊欄位都還在」，偷偷加一欄不會有人紅）。**不加存檔欄**是這一格的硬規則。

v1.2 · P20b **也一欄都沒有新增**（檔案廊不記帳）：24 則小知識看過沒有不存進去，
展品亮不亮完全由**既有**的 `skillsV2` 推導（收了那一條技法，那一片就亮）——
所以「重置進度 → 展品全暗」是自動成立的，不必再多一個欄位去追。

v1.2 · P16c 新增兩欄，都是純加法的**物件欄**：
`watchmen`（`{ [id]: { met, seen } }` —— 聊過了沒、問過哪幾種情報）與
`struggles`（`{ [challengeId]: { tries, hits } }` —— 這一關沒過幾次、跨次累積命中過哪幾條檢查器；
**通關那一刻整筆刪掉**，過了就不是卡關）。兩欄都**一格都不影響進度**：不給 XP、不寫 `bestGrades`、
不收技巧、不算徽章、不是任何東西的解鎖條件（測試靜態掃描 `refreshUnlocks()` 與 `gateSatisfied()`）。
`normalize()` 逐鍵驗形（`seen` 只認得四種情報 id、`tries` 取整且 ≤ 0 就整筆丟），`reset()` 自然清空。
守夜人的「卡關提示」讀的就是 `struggles` —— 它是玩家卡住時唯一的出口，所以那一欄必須真的記得住。

v1.2 · P18 新增一欄，同樣是純加法的**物件欄**：
`guardians`（`{ [id]: { hits, turns, convinced } }` —— 守門者的交辦上對上了哪幾行、
跟他說過幾句、說服過了沒）。`hits` 是**跨次聯集，永不清零**；`convinced` 一旦為真就不再落回
（`tellGuardian()` 從頭到尾只有聯集，沒有一行減法）。同樣一格都不影響進度：
不給 XP、不寫 `bestGrades`、不收技巧、不算徽章、不是任何東西的解鎖條件
（測試靜態掃描 `refreshUnlocks()`）。`normalize()` 逐鍵驗形（`hits` 不是陣列就整筆丟、
`turns` 取整且不為負），`reset()` 自然清空 —— 而且世界端那塊板要跟著暗
（`onReset` 真的呼叫 `world.guardians.reset()`，測試靜態掃描守著那一行）。

v1.2 · P19 新增一欄與一個設定：
`shortcuts`（`{ [shortcutId]: true }` —— 推開了哪幾條相鄰區捷徑，見 §4.16）。
**只往開的方向走**：`normalize()` 只留明明白白是 `true` 的那幾條（`false`、字串、陣列、
太長的鍵一律不收），所以沒有任何一條路可以把已經開的門關回去。同樣一格都不影響進度：
不給 XP、不寫 `bestGrades`、不收技巧、不算徽章、不是任何東西的解鎖條件
（測試靜態掃描 `refreshUnlocks()`）；`reset()` 自然清空 —— 而且世界端那道門要跟著關回去
（`onReset` 真的呼叫 `world.resetShortcuts()`，測試靜態掃描守著那一行）。
`settings.guides`（**螢火指路**，見 §4.4）預設 **開**，`normalize()` 只認得明寫的 `false`。

v1.2 · P23 新增一欄與一個設定：
`daily`（`{ day, ids, visited }` —— **今日三事**，見 §3.8）。`day` 是**玩家本地時間**的
`YYYY-MM-DD`（不是 UTC —— 住在 UTC+8 的人半夜十二點換日，不是早上八點）；
`ids` 是今天那三個提議；`visited` 是今天走過哪幾片土地（「走一趟」那一種提議看它）。
`normalize()` 驗形：日期不合形就**整筆當成還沒挑過**（沒有日期的三件事沒有意義）、
`ids` / `visited` 去重截斷。一格都不影響進度：不給 XP、不寫 `bestGrades`、不收技巧、
不算徽章、不是任何東西的解鎖條件（測試靜態掃描 `refreshUnlocks()` 與 `gateSatisfied()`）。
`settings.daily` 預設 **開**，`normalize()` 只認得明寫的 `false`；
**關掉之後這三格永遠停在預設值** —— `test:rubric` 把「關掉之後的整份存檔」與
「從來沒有這個功能的那一份」逐值比對（除了開關本身那一格）。

v1.2 · P23 另外新增兩個**旗標**（`flags`，不是新欄位）：
`achievementDone`（隱藏成就達成過了 —— **只加不減**）與
`p23Aligned`（門檻對齊那一次性遷移的憑證）。理由見 §3.9。

課程 v2 · Phase J3 新增一次**純加法的開機回填**（不是新欄位）：
開機時照「`bestGrades` × 那一關的 `primarySkillId`」把漏掉的技能補進 `skillsV2`
（Phase A–I 期間玩到一半的存檔，有些關卡是在它還沒接上 v2 技能之前通過的）。
只加不刪、冪等、只補真的通關過的；補完立刻寫回 localStorage。

---

## 六、效能與碰撞的硬規則

### 6.1 預算

| 項目 | 上限 | 目前（高畫質實測 · v1.2 · P25b 發版前逐項重量，與 P22c 逐值相同） |
|---|---|---|
| 三角形 | 420,000（`test:rubric` 的框是 **295,000** —— v1.2 · P22c 為了把格距還原成放大前的 1.70 公尺） | **285,796**（低畫質 182,272） |
| 光源 | 56 | **37**（低畫質 20） |
| 碰撞體 | 1,400（`test:rubric` 的框是 **1,060**） | **1,049**（低畫質 953；v1.2 · P25b0 實測） |
| draw call（蓋出來） | **3,100** | **3,033**（低畫質 2,787） |
| draw call（最貴的一幀） | — | **2,543**（低畫質 1,986 ＝ 高畫質的 0.78） |
| 加色混合的透明片 | **815** | **725**（一幀 521） |
| 材質 | — | **1,422**（一幀 1,083） |
| 穿模稽核 | 0 | **0 / 0**（高／低畫質） |

> **v1.2 · P22c 的教訓：放大地圖會從一個沒有人想到的地方吃掉預算。**
> 座標 ×1.3 之後，程序化植被與碎石的**名額第一次被吃滿** —— 它們的上限
> （`buildFlora` 的 `perType` 30/14、`rockCount` 150/70）放大前從來沒有達到過，
> 因為石座淨空、小景與動線把土地佔滿了，整個世界只長得出 **138 株**植被。
> 土地大了 1.69 倍、淨空一寸沒變，同樣的上限忽然吃得滿：植被一口氣長到 **588 株**，
> ＋11,786 個三角形、＋296 顆碰撞圓，三角形與碰撞體**兩道框同時破掉**。
> 修法不是抬框，是把名額壓回世界本來就長得出來的量（8/4 與 100/46，實測 143 株、
> 碰撞體 1,053 ＝ 放大前那個數字）—— **這一格要的是「東西一樣多、彼此離得更遠」，
> 不是「地大了就多種樹」**。下一次動世界尺度時先問一次：哪些上限其實一直沒被吃滿？

**這幾個數字一律由腳本實測，不引用文件舊值。** 量測方式：在 node 裡把整個世界蓋起來
（`scripts/test-rubric.mjs` 的碰撞區段就是這樣做的），高／低兩種畫質各跑一次。

**新增場景內容時不新增光源**——用自發光材質（`emissive`）與加色混合的光暈片。
（Phase 25 加了 22 件器物：三角形 +9.3k、光源 **+0**、碰撞體淨 −21。）
（Phase B 把石座的燈改成**常數 8 盞的燈池**，燈數從此不隨關卡數成長。）
（Phase E 加了整片量器坊：三角形 +7.8k、碰撞體 +66、光源 **+1** ——
那一盞是「每區一盞主色補光」，與其他四片土地同一個模式；**地標本身零實體光源**。
新增一片土地時這是唯一允許的那一盞。）
（Phase H 加了整片減法之庭：三角形 +7.0k、碰撞體 +46、光源 **+1**（同樣是那一盞主色補光）；
**地標「空的基座」零實體光源** —— 銘文、光印子、被拿走那件東西的輪廓全部是自發光。）
（Phase J1 加了整座分歧之廳：三角形 +4.9k、碰撞體 +30、光源 **+1**（同樣是那一盞主色補光）；
**地標「兩面的柱」零實體光源** —— 五根柱子兩面的刻痕、中間那道細縫、柱頂的環全部是自發光。）
（Phase I 加了整片觀象臺：三角形 +7.0k、碰撞體 +59、光源 **+2** ——
一盞是那一區的主色補光，另一盞是小景「一次改了五處的那一張」裡本來就有的製圖桌燈；
**地標「朝天的鏡」零實體光源** —— 鏡面、星點、刻度全部是自發光材質。）
（v1.2 · P11 加了中觀那一階（兩道遮擋帶 ＋ 四座母題，見 §4.10）：三角形 **+528**、
碰撞體 **+31**、光源 **+0** —— 石材吃該區那一盞主色補光，刻線與第三階的輪廓全部自發光。）
（v1.2 · P12 把中觀鋪到另外四片土地（遮擋帶 6 道／母題 16 座）並把一道帶的碰撞體從 12 收到 4：
三角形 **+1,228**、碰撞體 **淨 −4**（新增的四片土地 +40、reasoning 收斂 −16、程序化道具讓位 −28）、
光源 **+0**。同一格另加了每區一種空中粒子（`drifts.js`）：三角形 **+0**、光源 **+0**、
碰撞體 **+0**、draw call **+12**（12 個 `THREE.Points`，共用同一個材質）。）
（v1.2 · P14 加了第一座高台（中央高原「第一階」，見 §4.12）：三角形 **+272**、
碰撞體 **+1**、光源 **+0** —— 石鼓吃高原那一盞主色補光，頂面那一圈刻線是自發光。
高台是**第一個為了跳躍而存在的東西**：它必須有碰撞體（§6.3 第 3 條），
而且它的頂面必須 `standable`。）
（v1.2 · P15 把高台鋪成 5 座／4 片土地、祕密由 4 處補到 12 處、並開了第一道橋缺口：
三角形 219,914 → **222,410**、光源 **37 不變**、碰撞體 975 → **974**
（高台 +4、程序化道具因為 `keepClear` 長大而讓位 −5）。
中觀層每片土地的碰撞體上限 20 → **22**。
**橋缺口 0 碰撞體、0 光源** —— 擋人的是 `isWalkable()`，不是石頭。）
（v1.2 · P16a 把高台鋪成 11 座／8 片土地，並補上契約鍛冶場的 4 座母題與量器坊的 2 道遮擋帶：
三角形 222,410 → **224,946**、光源 **37 不變**、碰撞體 974 → **992**
（高台 +6、母題 +8、遮擋帶 +8、程序化道具因為 `keepClear` 長大而讓位 −4）。
新蓋的六座高台一律用**下限半徑 1.4** —— 起跳圈跟著縮到 2.52 公尺外，
地形的起伏才吃得進「四周每個方向都跳得上去」那條門檻（§4.12 的表）。）
（v1.2 · P16b 把中觀鋪滿 12 片土地（最後三片各補兩道遮擋帶，帶 8 → **14 道**）：
三角形 224,946 → **225,738**、光源 **37 不變**、碰撞體 992 → **1,017**
（6 道帶 +25、程序化道具讓位 0）、網格 3,441。低畫質：三角 166,584／光源 20／碰撞體 893。
中觀層自己：三角 7,080、碰撞體 109、光源 **0**（石材吃該區那一盞主色補光）。）
（v1.2 · P16c 加了 12 位守夜人（§1.5）：守夜人自己是三角形 **+1,872**（每位 156）、
碰撞體 **+12**（每位一顆半徑 0.55 的底座 —— 0.55 < `STAND_MIN_R` 0.8，所以
**站不上一個人的頭**這件事靠尺寸成立、不靠旗標宣告）、光源 **+0**（燈是自發光材質
＋ 一片加色混合的 sprite）。全場實測 225,738 → **228,920**、碰撞體 1,017 → **1,029**、
draw call 3,441 → **3,731** —— 守夜人以外的差額來自**程序化道具重擲**
（12 位進了 `keepClear`，`place()` 被退一次就多抽兩次亂數，整片土地的擺放跟著位移，
見 findings「P06c 的發現」）。低畫質：三角 169,413／光源 20／碰撞體 905。）
（v1.2 · P17 加了 12 隻大濁靈（§1.6／§4.8）：大濁靈自己是三角形 **+3,590**（每隻 282–342：
body 80 ＋ head 80 ＋ core 20 ＋ 殼 6–8 × 20 ＋ sprite 2）、碰撞體 **+12**（每隻一顆半徑 1.5 的底座）、
光源 **+0**（眼光、殼、光暈全部自發光／半透明）。全場實測 227,586 → **231,230**、
碰撞體 1,041 → **1,052**（+12 減去程序化道具讓位的 −1）。低畫質：三角 172,012／光源 20／碰撞體 929。
殼用 detail 0（20 面）而不是小濁靈的 detail 1（80 面）：8 層照 detail 1 一隻就 800+，
整層會吃掉 9k —— **半徑大、面就大**，稜角本來就是這個世界的低多邊形語言。）
（v1.2 · P18 加了一位守門者（§1.5／§4.15）：他自己是三角形 **+138**、碰撞體 **+1**
（一顆半徑 0.55 的底座）、光源 **+0**（胸前那塊板上的七行字是自發光的薄片）。
全場實測 231,230 → **232,726**、碰撞體 1,052 → **1,053** ——
守門者以外的差額同樣來自**程序化道具重擲**（他進了 `keepClear`，見 findings「P06c 的發現」）。）
（v1.2 · P19 加了第一條相鄰區捷徑（南弧的吊板，見 §4.16）：三角形 **+834**
（門的兩根細桿 ＋ 門閂 ＋ 那一片幕 ＋ 兩座絞盤）、碰撞體 **+2**（只有兩座絞盤登記圓——
門的柱子是細桿，理由在 §4.16 那張表）、光源 **+0**。
全場實測 232,726 → **233,560**、碰撞體 1,053 → **1,055**、draw call 3,869 → **3,888**。
**走廊本身 0 三角形**：甲板就是地形網格（七座橋也一直是這樣），只有門與絞盤是新的量體。
同一格的外交式導向（螢火群指路）是既有 `moths` 的一個偏向量：
三角 **+0**、粒子 **+0**、光源 **+0**、碰撞體 **+0**。）
（Phase F 加了契約鍛冶場與護欄崗：三角形 +13.2k、碰撞體 +139、光源 **+3** ——
兩盞是那兩區各自的主色補光，第三盞是鍛冶場那組小景裡的一盞燈；
**兩座地標本身都是零實體光源**（e2e 逐一數過）。）

（v1.2 · P20b 加了 12 座檔案廊（§4.18）：檔案廊自己是三角形 **+1,844**
（每座 132 ＋ 每片展品 2；130 片展品 ＝ 130 條技法）、碰撞體 **+0**、光源 **+0**、
可站立體 **+0** —— 細桿 0.18 見方、展品吊在 2.44 公尺高、檔案龕只有 0.72 公尺高，
碰撞稽核的三道豁免（細桿／飄在半空且頂面站不上去／跨得過去）各自成立。
全場實測（同一棵樹，只把 `archives` 換成空陣列量一次基準）：
三角 233,976 → **235,436**（+1,460 ＝ 檔案廊 1,844 − 程序化道具讓位 384）、
碰撞體 1,055 → **1,047**（−8，全部是道具讓位）、draw call 3,777 → **4,038**。
差額來自**程序化道具重擲**（12 座進了 `keepClear`，`place()` 被退一次就多抽兩次亂數，
見 findings「P06c 的發現」）。低畫質 172,982 → **174,698**、碰撞體 932 → **929** ——
這一層**低畫質照蓋**：它是教學內容，不是氛圍層。）

（v1.2 · P22b **把 draw call／材質／透明片也放進預算**。守錯預算比沒有預算更危險：
三角形／光源／碰撞體一路都在框內，可是閘門 C → D 一格之內 draw call 長了 16%、
加色混合的透明片長了 28%、材質長了 25%，而沒有一條斷言在數它們。
`npm run audit:perf` 量兩種 —— **蓋出來**（這個世界裝了多少東西）與**最貴的一幀**
（站在出生點或十二片土地中心，跑過四幀之後真的要畫多少）；
`scripts/expected-counts.json` 的 `perf` 逐值守，`npm run test:rubric` 逐值比對。
三件事把 4,144 → **3,024**（−27%）、加色 1,164 → **725**（−38%）、材質 2,118 → **1,413**（−33%），
**三角形一個都沒動**：
① 石座合批 —— 142 座 × 5 件（底座／光核／腳下的圈／光柱／走近的光環）＝ 710 個 draw call
收成 **5 個**；每一座仍握著自己那一份沒掛進場景圖的 `THREE.Mesh` 當代理，既有介面一個字都沒改。
② 靜態合批（`src/world/batching.js`）—— 故事小景／地標／中觀三層裡「同一份幾何 ＋ 同一份材質
＋ 同一組 userData」的靜態網格收成 `InstancedMesh`；**碰撞登記與穿模稽核本來就逐實例走
InstancedMesh，所以一顆圓都沒少**。**形狀被斷言看著的節點整棵留著**——
高台（`platform:`）逐一被量每一塊裝飾、遮擋帶（`screen:`）被量「至少四塊網格」，
合掉那兩種就會讓那幾條斷言安靜地量到 0（空泛通過）。
③ 細節分帶 —— 判準是「它在螢幕上占幾個像素」（`2r/d × PX_K`，1080p／55°），不是距離：
高畫質 2 像素、低畫質 7 像素，進 / 出用 0.75 倍的滯後。
**低畫質省的是「畫」不是「裝」**：一幀比高畫質少 22%，蓋出來只少 8%。）

（v1.2 · P23 把等級穿到角色的披肩上（見 §3.8 旁邊那一格）：披肩下緣一圈光點，
**格數 ＝ 等級**，一圈 33 格、最多三圈 —— 33 × 3 ＝ **99** ＝ `levelFromXp()` 的等級上限，
所以「格數 ＝ 等級」在整個定義域上成立，永遠不必用「超過就不畫」這種會說謊的收尾。
成本：**一個 `InstancedMesh`**（draw call **+1**、材質 **+1**、幾何 **+1**），
三角形滿級 **+792**（一格 8 面 × 99；等級低的時候只畫 `count` 格）、
**光源 +0**（自發光材質）、**碰撞體 +0**（角色不在 `collectSolids()` 掃的那棵樹底下，
所以「零新碰撞體」是結構上成立，不是靠記得）。
**低畫質照蓋** —— 它是進度回饋，不是氛圍層（同「殘頁／刻文低畫質照蓋」那一條）。
這一格**世界端的數字一個都沒動**：`npm run audit:perf` 的高／低畫質逐值不變
（draw 3,033／2,787、加色 725／641、三角 285,796／182,272、光源 37／20、碰撞體 1,053／957）。）

**飄在半空的東西要真的飄在半空。** 下緣離地要 > 1.6 公尺（`FLOAT_MIN`），
而且要用**它自己的最低點**算，不是用中心點 —— 一片半徑 6 公尺的躺平圓盤，
圓心離地 5 公尺時下緣其實已經垂到地面了（Phase 25 修掉的一個穿模點就是這個）。

**成組擺的東西，每一件貼的是自己腳下的地**（v1.2 · P25b0）。
一組小景／一座地標是一個 `holder` 落在「組中心那一點」，組內每一件只給局部位移；
地會起伏，所以**離組中心越遠的零件浮空或埋地就越明顯**。
P11 對母題那一層修過這個錯，小景那一層當時沒跟上：實測 33 組 160 件裡
**44 件差超過 0.25 公尺**（最糟 1.12 公尺，`tool-yard` 一組就中了四件）。

判準不必發明新資料 —— `parts` 的 `[dx, dy, dz]` 裡的 **`dy` 就是「刻意的垂直位移」**：

| `dy` | 意思 | 該落在哪 |
|---|---|---|
| `=== 0` | 這件東西站在地上 | **它自己腳下的地** |
| `!== 0` | 擺的是**相對關係**（桌上的墨、懸在階梯上方的浮階） | 組中心的地 ＋ `dy`（**不准**被貼地，一貼就從桌面掉下來） |

局部位移要**先套上 `holder.rotation.y`** 才是世界座標（少了這一步，
組別轉得越多錯得越離譜 —— 這是 `scripts/vignette-fit.mjs` 的第三條紅測）。

稽核在 `scripts/vignette-fit.mjs`，`test:rubric` 逐件跑（160 件、兩層）：
**原點**（節點自己的高度，門檻 ±0.05 公尺）＋ **腳**（那一件真的畫出來的幾何最低點，
門檻 −0.25／+0.15 —— 水池、火塘、石堆是刻意陷進地裡的，所以下沉與浮空分開給）。
量兩層是因為只量原點會被「道具的原點哪天不在底面」整層騙過去。

**修完碰撞圓會多幾顆，那是對的**：原本埋在地裡（頂緣低於地面 0.5 公尺）
或被推到「從下面走得過去」（下緣高於地面 2.0 公尺）的零件，
回到地面之後就重新符合「有份量」——實測高畫質 1,046 → **1,049**、
低畫質 950 → **953**（多出來的四顆與消失的一顆全部是**已經有碰撞圓的那幾件身上的內圈**，
同心、半徑更小，所以穿模稽核前後都是 0）。

**這條規則管的是「一組各自站在地上的零件」，不是「一座整件的雕塑」**：
一座塔、一個環、一棵樹是**一件**東西（多數地標還立在自己的臺座上，
見 `LANDMARK_SOLIDS`），它的每一塊本來就共用同一個平面 ——
那一種**不要**逐件貼地，貼了會把它拆散。

重複元素一律 InstancedMesh。幾何體與材質全部走模組層的快取（`GEO` / `MAT` Map）。

### 6.2 每幀迴圈

- 比較**平方距離**，不開根號、不 raycast、不算 Y（地面上的東西用圓柱體判定）。
- **零每幀配置**：暫存向量／矩陣提到模組層重複使用，tick 裡不 `new`、不 `map/filter`、不建閉包。
- **距離分級**：45 公尺外整組跳過；15 公尺外每 3 幀才算一次，用 index 錯開讓工作量平均分散。
- **不要把時間敏感的互動綁在幀數上**（軟體渲染一幀可能好幾百毫秒）——用計時器。

### 6.3 碰撞

一件東西同時滿足這四條就**必須**有碰撞體（`scripts/collision-audit.mjs` 會逐一稽核）：

1. 佔地半徑 ≥ 0.5 公尺
2. 露出地面的高度 ≥ 0.9 公尺
3. 底緣離地 < 1.6 公尺（`FLOAT_MIN`）**而且頂面站不上去**
4. 最薄的兩軸都 ≥ 0.9 公尺

例外必須寫進 `EXCEPTIONS` 並說得出理由（地形、光、霧、水、閘門力場）。

第 3 條的後半句是 **v1.2 · P13** 加的。原本那句話預設了「玩家不會站到東西上面」——
一旦有跳躍就是漏洞：飄在半空、卻有一片平頂面的東西會**兩頭落空**，
既不必擋人、也沒人稽核（P11 的浮空母題就是這樣整座漏掉的）。

#### 頂面：碰撞的第四個維度（v1.2 · P13）

碰撞從此不只是「一張圓的清單」——每一顆圓多帶五個欄位（`src/world/world.js` 的 `collectSolids()`）：

| 欄位 | 意思 |
|---|---|
| `top` | 這顆圓上方**最高**的頂面世界高度（＝剪影的頂；**永遠是數字**） |
| `standTop` | **腳踩得到的那一面**（離地 0.6 公尺以上之中**最低**的上向面）——站不站得上去、抬到多高都看它 |
| `topFace` | `top` 是不是量自真的**上向面**（否＝退回「圓心正上方那一塊表面的最高點」） |
| `standable` | 站不站得上去 |
| `standR` | 頂面**證明過是平的**那一段半徑（0 ≤ `standR` ≤ 碰撞圓半徑；站不上去的一律 0） |

`top` 與 `standTop` 為什麼要分開：一座**有頂蓋**的平臺（走的那一面 1.2 公尺、頂蓋 5 公尺）
在同一顆圓裡有兩個上向面。剪影的頂是頂蓋（稽核的「飄在半空」看它），
可是人站的永遠是下面那一面 —— 只留一個欄位就會兩件事都講錯（P13 審查 · 第 2 條）。

**`standable` 的五條判準**（常數在 `world.js`，`collision-audit.mjs` 讀的是同一組）：

1. 頂面是**上向面** —— 面法線離正上方 ≤ 10°（`STAND_UP_DOT` ＝ `Math.cos(π/18)`）。斜插的石板、圓錐的尖、球的頂點都不算。
2. 頂面**夠平** —— 以圓心為心、半徑 0.8 公尺的一圈上取 12 個點，每一點都踩得到上向面，而且與圓心的高差 ≤ **6 公分**（`STAND_FLAT_EPS`）。斜 3° 還算平，斜 6° 就不是。
3. **面積夠站** —— 上面那一圈就是 `STAND_MIN_R` 0.8 公尺；登記的碰撞圓也要 ≥ 0.8。
4. **離地 0.6–3.0 公尺**（`STAND_MIN_H`／`STAND_MAX_H`）：再低是「跨過去」，再高就不該跳得上去。
5. **不准懸在虛空上方** —— 腳下那一點的 `coverage()` ≥ 0.45（與 `isWalkable()` 同一條門檻）。

四條實作上的堅持：**逐圓各自算**（`solidSpan` 的圓串每一顆用自己的圓心去量，
不共用一個原點的高度 —— P10b／P11 連兩次的教訓）、**保守優先**（量不出上向面一律不可站，
量體大到攤不開（> 2,048 三角面）也一樣，`top` 退回外接盒的頂但仍然是個數字）、
**平到多遠就只抬到多遠**（碰撞圓的半徑是外接盒的長邊 —— 一塊 7.2 × 1.8 的長石板會登記成
半徑 3.6 的圓，可是平頂只有窄邊那一段；抬高的範圍用 `standR`，不然人會站在一塊
沒有幾何體的空氣上）——往外量的步長是 **0.15** 公尺（`STAND_RING_STEP`），**這個數字就是這支量法的解析度**：
比它窄的環狀缺口量不到（兩圈之間沒有取樣點）；0.15 遠小於玩家半徑 0.62，所以量不到的洞人也掉不下去
（P13 審查前是 0.4，實測會讓一道 0.28 公尺的縫整個被跳過、把空氣認證成平的）——、
**稽核與資料層共用同一份判準**（`measureSurface()`，不會有第二套）。
另外：**光與水不是可以站的面** —— 半透明材質一律不量，InstancedMesh 那條路也一樣
（同一件東西改成 instanced 不該突然變得站得上去）。

`world.groundHeightAt(x, z)` ＝ `max(地形, 站得上去的頂面)`（只在 `standR` 那一段裡抬高）。
**v1.2 · P13 刻意沒有把它接到玩家身上**（沒有跳躍就沒有 Y 軸速度）——
它是 P14 的資料通路。玩家走得到的每一點，它的答案與 `terrainHeight()` **逐點相同**
（`test:rubric` 在 341 × 341 的全地圖網格上證明過；`test:e2e` 再走過去撞三塊可站立體驗一次）。

**v1.2 · P14 接到玩家身上的不是它，是多問一句「腳現在在多高」的 `world.supportAt(x, z, feetY)`。**
規則只有一條：**`feetY >= standTop - LEDGE_EPS` 的可站立體才撐得住人**（`LEDGE_EPS` ＝ 0.02）。
兩支共用同一段迴圈（`groundHeightAt()` ＝ `supportAt(..., Infinity)`），判準不會分家。
這一句同時回答了 P13 交接的第 1 條 —— `escapeSolid()` 會讓玩家短暫地站在圓內，
可是**脫困中的人腳在地形高度**，比任何一個 `standTop`（至少離地 `STAND_MIN_H` ＝ 0.6）都低，
所以他拿到的仍然是地形高度：會被慢慢請出來，**不會被瞬間抬到屋頂上**。

同一個 `feetY` 也是 `clampPosition()` / `escapeSolid()` 的**可選**第末個參數：
給了才讓「腳已經站到頂面以上」的那一顆不擋人（`solidAtAbove()`）——
那是跳上高台唯一需要的例外，從下面撞過去照樣擋得死死的，
掉回頂面以下的那一瞬間它**立刻恢復擋人**。玩家貼著地形走的時候一律不給（`null`），
走的是 P13 之前那一支，一個位元組沒動。

#### 走不走得到：覆蓋率 ＋ 坡度 ＋ 崖唇（v1.2 · P16d／P16e）

站長 2026-08-26 實玩回報：「會墜落懸崖，然後可以走回來，但是讓主角會超出會墜落區域。」
查證屬實而且是結構性的 —— `coverage()` 是**土地半徑的水平混合值**、與高度場毫無關係，
而 `terrainHeight()` 正好在同一段裡往下崩。兩者各說各話，於是「走得到」的邊界
落在崖面的**半山腰**：實測玩家走得出中央高原邊緣 5.5 公尺、往下走 18.9 公尺
（最糟的校驗場 **21.22** 公尺），撞到看不見的牆，再走回來。

**三道鎖，順序不能反。**

**① 高度場：走得到的地方一寸都不崩。**
虛空的塌陷不再看覆蓋率，改看**離「走得到的那一圈」還有幾公尺**（`rimDistance()`）：

- `coverage(x, z) >= STAND_COVER_MIN`（0.45）⟺ `rimDistance(x, z) <= 0` ⟺
  `terrainHeight() === terrainRelief()`（三句話**逐點等價**，`test:rubric` 是硬斷言）
- 「那一圈」是每片土地／每座橋各一個半徑（`SITE_RIM` / `CORRIDOR_RIM`）：
  `radius − smoothstep⁻¹(0.45) × (radius − flat)`，開檔時各算一次
- 原本整片「崖肩」（覆蓋率 0.45–0.99）是走得到的斜坡，24 件器物、2 座石座、
  兩根閘門柱的擺位都落在上面；填平之後它們站在實地上（**擺位規則一條都沒放寬**）
- 加建院落頸口那道 6.6 公尺深的凹溝（覆蓋率只掉到 0.805）直接消失 ——
  **不必替它們另外登記走道**

**② 崖唇是一段肩，不是一道摺線**（v1.2 · P16e —— 審查抓到 P16d 只修了一半）。

P16d 把 34 公尺壓進覆蓋率 0.45→0 那一小段（中央高原 5.75 公尺、約 80°），
**但地形網格是固定格點**：`buildTerrain()` 高畫質 200 段 ＝ **1.70 公尺一格**、
低畫質 110 段 ＝ **3.09 公尺一格**。格點插不出那道摺線，於是站在崖唇上的人
**浮在畫面的地面上**：高畫質 **9.2 公尺**、低畫質 **17.5 公尺**（比 P16d 之前的
2.99／7.27 還糟）。P16d 新加的 e2e 抓不到，因為它拿 `player.y` 跟**同一支解析式**比。

現在往外的崩是一段**肩 ＋ 斷崖**（`rimDrop(s)`，`s` ＝ 離那一圈幾公尺）：

| 常數 | 值 | 為什麼是這個數字 |
|---|---|---|
| `RIM_SHOULDER` | 5 m | 低畫質一格的**對角線**是 3.09 × √2 ＝ 4.37 m —— 肩要比它寬，站在邊界上的人腳下那一格四個角才都還在肩上 |
| `RIM_CURVE` | 0.4 | 肩的曲率（每往外一公尺斜率加多少）。太大 → 格點又插不出來；太小 → 「還是平地卻走不過去」的那一圈變寬（≈ √(0.6 / `RIM_CURVE`)） |
| `RIM_PLUNGE` | 4 | 肩以外斜率再加速，10 公尺內收到 −34（斷崖仍然是斷崖） |

**修完的實測**（0.5 公尺格、走得到的每一點 vs **真的被畫出來的那張網格**）：

| | 高畫質（1.70 m 一格） | 低畫質（3.09 m 一格） |
|---|---|---|
| 腳下 vs 畫面（總差） | **0.442 m** | **1.072 m** |
| 其中**虛空那一崩**的份 | **0.158 m** | **0.492 m** |
| 其中**地貌自己的坎**的份 | 0.442 m | 0.830 m |
| P16d（同一批點） | 9.2 m | 17.5 m |

> **低畫質那 1.072 公尺不是崖唇的問題**：把崖唇整段拿掉、只留「地本身的起伏」，
> 3.09 公尺一格的網格**本來就**差 0.830 公尺（低多邊形地貌自己的坎與橋頭的混合帶，
> P16d 之前就有，而且不在虛空上方）。要把它壓到 0.6 以下得把低畫質的網格加到
> 170 段（三角 24,200 → 57,800）—— 為了低畫質獨有的 0.4 公尺付 2.4 倍的地形三角
> 不划算。所以斷言分兩把尺：**虛空那一崩 < 0.25／0.6**（這一格修的東西），
> **總差 < 0.6／1.2**，另外一條釘死「剩下的差幾乎全是地貌自己的坎」。

> **v1.2 · P22c 之後這四把尺一度全部量到框外 —— 已解，解法是把格距還回去。**
>
> 地圖 ×1.3 之後地形平面從 340 長到 442 公尺見方。段數若不跟著加，
> 高畫質一格就從 **1.70 變成 2.21 公尺** —— 而上面那四個門檻當初**就是在 1.70／3.09
> 的格距上量出來的**。網格變粗，畫出來的地面就跟不上腳下的解析式，
> 於是四把尺同時紅：總差 0.798（框 0.6）／1.864（框 1.2）、虛空那一崩 0.260（框 0.25）、
> 減法之庭邊緣 3.04（框 3）。
>
> **解法兩件事，都不是調門檻：**
>
> ① **地貌紋理跟著世界一起放大**（`RELIEF_SPAN` = 1.3）。
>    「座標乘 k、尺寸不乘」這條規則對**地**要例外：同樣寬的階梯、書架溝、工具槽
>    鋪在 1.3 倍大的土地上只會變得更**密**，那跟「攤開」正好相反。
>    地貌函式改吃除以 k 之後的區內座標 —— 形狀一模一樣，只是橫向拉寬 1.3 倍。
>    高畫質總差 0.798 → 0.642。
>
> ② **段數跟著平面一起長**（高 200 → **260**、低 140 → **143**），格距回到 **1.70／3.09**
>    ＝ 放大前那一份。高畫質總差 0.642 → **0.466**、低畫質 → **1.032**，四把尺全綠。
>
> **為什麼不是「調大一點看會不會過」**：落差對段數**不是單調的**（實測 seg 210 是 0.837，
> 比 200 還差；低畫質 seg 150 是 1.195，比 143 還差）—— 那是格線落在陡坡哪個相位的運氣。
> 挑數字要挑「跟放大前一樣的格距」，那才有依據；挑「剛好會過的那一個」是在對雜訊過擬合。
>
> **代價：`WORLD_TRI_CEIL` 241,000 → 295,000**（WORLD.md §6.1 的硬上限 420,000 沒動）。
> 地形三角 80,000 → 135,200。這個決定要說得出口：
>   · 那 55,200 個三角是**同一個不透明 mesh** 多切幾刀 —— draw call ＋0、材質 ＋0、
>     透明片 ＋0、overdraw ＋0。
>   · 而 P22b 剛好量出來，真正貴的是那三項（draw call −27%、加色混合 −38%）。
>     241,000 這個框守的是一個**已經被證明量錯東西的代理指標**。
>   · 不放寬只剩兩條路：讓玩家浮在畫出來的地面上 0.642 公尺（P16e 要擋的就是這個），
>     或者把三片土地的階梯地貌磨平。兩條都比多五萬個不透明三角糟。
>
> 「不准偷加內容」那一條沒有因此失效：`test:rubric` 改成**扣掉地形之後**再跟 P22b 的基準比
> （≤ ＋1%），所以拿地形當藉口塞內容仍然會紅。

**③ 走路的斜度上限 `WALK_SLOPE_MAX` = 45°**（`world.js`，`isWalkable()` 裡；
中央差分、取樣距離 `SLOPE_PROBE` = 0.35 公尺 → 一次 **4 個高度取樣、零配置**）。
它管的是**地本身的起伏**：橋的甲板邊、院子的崖唇、地貌自己的坎。
與虛空、閘門同一條規矩：**不管腳在多高都擋**（跳到一半也不准落在崖面上）。
唯一的例外是 `escapeSolid()` 脫困的那一步（見下）。

45 是量出來的，不是拍的：

| 量到的東西 | 實測 |
|---|---|
| 橋的甲板（平段 \|s\| ≤ 5）沿線最陡 | **13.6°** |
| 橋的主動線（\|s\| ≤ 3.2） | **12.9°** |
| P15 的窄板（3.5–5.2） | **3.8°** |
| 四片加建院落的頸口中線 | 護欄崗 12.3°／校驗場 19.9°／減法之庭 6.7°／分歧之廳 11.9° |
| 12 位守夜人四面八方 288 個點裡最陡的 | **41.9°**（`watch-nameless-tool` 東北 2.6 公尺） |
| 走得到的取樣點最陡 | **44.9°**（0.5 公尺格；40–45° 有 153 個點） |

設計**已經把人擺在 41.9° 的坡邊上**（`watchmen.json` 是內容紅線，搬不了人），
所以門檻不能訂到 35°（實測 7 條斷言當場紅）。
P16e 之後這條規則擋掉的點從 925 掉到 **21**（45.1–49.4°）—— **少擋不是壞事**：
P16d 的崖唇是一道摺線，半公尺的探針一伸出去就讀到虛空，於是它兼著把崖唇前
那一圈（613 個點）也收掉；崖唇改成肩之後那件事歸高度場管，剩下的 21 個點
才是這條規則真正要擋的**地本身的坎**。

**「還是平地卻走不過去」的那一圈有多寬**（換一個看不見的牆不算修好）：
量法是**到「走得到的地」的真實距離**（0.5 公尺格上的 chamfer 距離，與方向無關）——
P16d 那把尺是沿徑線往外走，量到的其實是 `唇寬 / sin(徑線與邊界的夾角)`，
徑線一擦過鄰居的地界就會把同一圈 1.5 公尺的唇讀成 7–9 公尺。
現在**土地邊緣與橋的甲板邊用同一把尺**（P16d 只驗了土地）：

- 全地圖最寬 **1.50 公尺**（12 片土地各 < 2、7 座橋各 < 2）
- 反例：把肩放軟四倍，同一把尺立刻讀出 > 2 公尺

**修完之後土地邊緣最多還能往下走幾公尺**（12 片各 120 條徑線）：
中央高原 0.76、示範與推理 0.84、沉書檔案庫 0.66、齒輪工坊 0.68、面具劇場 0.00、
量器坊 2.40、契約鍛冶場 1.47、護欄崗 1.42、校驗場 2.48、減法之庭 1.46、
觀象臺 0.65、分歧之廳 2.00 —— **全部 < 3 公尺**（P16d 之前是 17.8–21.2）。

**絕不能把玩家關住**（護欄）：`escapeSolid()` 從「只往離圓心最遠那一個方向推」
改成**試一圈方向**（`ESCAPE_FAN`：0°／±25°／±50°／±75°／±88°），第一圈照完整規則問，
第二圈才鬆掉坡度那一條（虛空與閘門在兩圈裡都擋）。
實測（每顆碰撞圓 × 24 角 × 4 半徑、160,209 個「站得住又在圓裡」的位置）：
只推一個方向有 **526 個**推不出去（石頭立在甲板邊，最遠的方向正好指著虛空），
試一圈之後 **0 個**。最外那一對 ±88° 幾乎是沿著石頭的邊滑 ——
留著是因為有 13 個位置只剩這一條路（人卡在土地最外緣的石頭裡，往外一步就是虛空）。

**崖面怎麼接出去**：離所有土地與橋都超過自己的 `radius`／`half` 時，混不出高度 ——
P16e 之前那裡直接從 0 起算（一道硬邊）。現在按 **1 / (離自己的邊幾公尺 + 0.02)**
混最近的那幾片，崖面才會從甲板／地面的高度長下去。`d = radius` 那條縫實測：
直接「挑最近的一片」是 **4.14 公尺**的硬邊，混過之後 **1.26 公尺**——
剩下的 1.26 是**兩片外緣剛好交叉**的那幾個點（從高原那側逼近讀到高原的地貌、
從橋那側逼近讀到甲板），那個落差在 `terrainRelief()` 裡本來就有，接法換不掉。

**效能**（同一台機器 A／B，各 20 萬次）：

| | P16d | P16e |
|---|---|---|
| `isWalkable`（**走得到的點**，玩家每幀真的在問的） | 2.497 µs | **2.321 µs** |
| `isWalkable`（整張平面均勻取樣，九成落在虛空） | 1.388 µs | **1.625 µs** |
| `terrainHeight` | 0.515 µs | **0.477 µs** |

P16e 把「起伏」與「離邊界多遠」併成同一趟迴圈，順手拆掉 P16d 那個
「`terrainHeight()` 讀 `terrainRelief()` 留在模組層的覆蓋率」的隱藏耦合 ——
那個寫法只要有人去記憶化 `terrainRelief()` 就會靜靜地用到別的點的覆蓋率。
虛空那一邊多出來的 0.24 µs 是崖面接出去的那一圈（只在混不出高度時才跑）。

### 6.4 淨空（絕對不准被擋住的地方）

- 石座周圍 **5.6** 公尺
- 四條橋的主動線 **±3.2** 公尺
- 出生點 **7** 公尺
- 起始祭壇 **半徑 + 1.5** 公尺
- 石碑 / 刻文小語 / 地標 / 小景各有自己的留白半徑（見 `keepClear`）

**擺放時就避開動線**，不要靠事後把碰撞拿掉（那會做出「看得到卻走得過去」的鬼影）。

#### 捷徑走廊的淨空（v1.2 · P19）

捷徑走廊（§4.16）**整條 ±4 都是動線**（`inCorridor()`）——它只有 8 公尺寬，
一顆半徑 1.5 的石頭擺在 lat 3 就把剩下的路夾到走不過去。
站在走廊上的只有那兩座絞盤（偏出中線 2.6 公尺），推開之後門洞仍留得出 **4.2 公尺**的路
（`test:rubric` 用 0.05 公尺的步長逐點掃）。

#### 登記例外：橋上的缺口（v1.2 · P15）

「橋的主動線不准被擋住」目前**只有一條例外**：`world.js` 的 `BRIDGE_GAPS`（見 §4.13）。
例外成立的條件寫死在資料裡、也寫死在測試裡：**同一段一定要留一條窄板**
（`keepFrom`–`keepTo`，整條都在 `LANE_HALF` 之外），不用跳就走得過去。
`test:rubric` 用步長 0.1 公尺逐點掃過那 3 公尺，再用**全地圖洪水填充**證明
「不按 `J`，每一座橋走得完、每一個互動點到得了」。
沒有窄板的缺口＝把路挖斷＝倒退（護欄 7），不准放。

### 6.5 聲音

- 分區配樂與主要音效用站長自己做的音檔（`public/audio/`，見 `public/LICENSE.md`）。
  issue #3 之後是 **13 支配樂（開場曲 ＋ 十二區各一首）＋ 24 支音效**，磁碟上約 37.4 MB。
- **合成引擎是備援，不是遺跡**：檔案還沒到 / 抓不到 / 解不開，就退回 Web Audio 即時合成。
  把 `public/audio/` 清空，遊戲照樣有聲音、照樣能玩（護欄 3）。
- **響度統一發生在播放時，不在檔案裡。** 音檔一律不做響度處理（轉檔指令裡沒有任何
  volume filter），每個檔案在資料層記著量到的 integrated LUFS 與 true peak，
  gain 由公式算出來：配樂 `10^((-20 - lufs)/20)`、音效 `10^(((-19 + trim) - lufs)/20)`，
  且套上去的峰值不准超過 −3 dBFS（會頂到的標 `clamped: true`）。
  配樂床 **−20 LUFS**、音效 **−19 LUFS** —— **只比床高 1 LU**。十二區的配樂全部沒有
  attack transient，音效是全場唯一的瞬態來源、本來就會自己跳出來，所以規矩是
  **「跳出來一點點就好，寧可太小聲不要太大聲」**：任何要取捨的地方一律往安靜那一邊靠。
  逐檔的量測值、算式與重驗方法在
  [`docs/design/audio-loudness.md`](./docs/design/audio-loudness.md)，
  `npm run test:rubric` 會逐檔把公式再算一次。
- **音效交付清單怎麼寫，資料層就怎麼寫。** 每一顆音效的「最短間隔」與「同時發聲數」
  來自交付清單（`cooldown_ms` → `throttle`、`polyphony` → `poly`），不是隨手填的：
  轉鈕的卡榫是 1（轉快了是**換一下**，不是兩下疊在一起）、敲模 2、鍛打 3（連打要疊得起來才像手工）。
  超過上限時**掐掉最舊的那一把**（12 ms 淡出）—— 玩家剛做的動作永遠聽得見。
  要改這些數字，就是在推翻音效設計者的交代，請先確認自己知道為什麼。
- **還沒有音檔的區域要誠實登記，不准借別區的來墊。**
  新區域上線時如果配樂還沒錄，就把它的 id 放進 `audio.js` 的 `SYNTH_ONLY_REGIONS`，
  並替它寫一組**自己的** `REGION_MOODS`（根音、音階、鐘聲密度都要與其他區不同）。
  借面具劇場那一首來墊，過橋時會聽起來像沒換地方 —— 那比誠實地播一段合成 pad 更糟。
  音檔補上時只要在 `BGM_TRACKS` 加一行、把 id 從 `SYNTH_ONLY_REGIONS` 移走，其餘程式碼一個字都不必動。
  **目前這份清單是空的**（`SYNTH_ONLY_REGIONS = []`）：量器坊、契約鍛冶場、減法之庭、校驗場、
  觀象臺、分歧之廳在 issue #3 都拿到了自己的一首，護欄崗的《The Unclosing Door》
  也在 2026-08-03 補齊 —— 十二區至此全部有自己的音檔。
  它們的 `REGION_MOODS` 仍然留著，現在的角色是「檔案還沒到 / 抓不到」時的備援；
  機制本身留著，任何新區域上線時先進這份清單，音檔到了再搬走。
- 音檔一律等到**開場曲那一刻**（自動播放放行就是開機，被擋住就是推開入場門那一下）
  之後才開始抓，不拖慢第一個畫面。
- **開機的岔路（Phase 33 ／ 門面 Phase 34）**：`audio.titleIntro()` 先試一次；同步就 running
  （返客／測試環境）→ 直接開標題卡，零摩擦。被政策凍住 → 先出一道**入場門**
  （`src/ui/entrygate.js`：近乎全黑、**一盞呼吸燈**、「推開夜色之門」、「點擊進入⋯」，
  三樣東西一件一件出現、全部延遲 ≥0.3s），推開那一下是 trusted gesture，音訊才解得開，
  同一拍放一聲 `gateOpen`（石門滑開）；門淡出 600ms 後標題卡才 `open()`，揭示與開場曲同時開始。
  `audio.whenRunning(ms)` 是那個探測器；標題卡預設 `hidden`，動畫要等 `open()` 才起跑。
- 配樂用**兩個 source 交替、等功率交叉淡入**做無縫循環（不是 `loop = true`，素材沒有做成完美 loop）；
  跨區也是等功率交叉淡入約 3 秒。解碼很吃記憶體，同時最多只留兩首。
- 世界的反應音（風鈴 / 音石 / 水紋 …）仍然全部合成——它們要能隨機變音高，音檔做不到這件事。
- **同一個動作的兩顆素材要輪播**（`SFX_FILES[].alt`）：契約鍛冶場的鍛打連打時隨機挑一顆，
  否則連續敲會像機器。節流是**逐 cue 各自算**的 —— 敲一下鍛打不該讓刻印牌的按鍵音變啞。
- **五片新土地的「刻上一段」有自己的材質**（`REGION_CARVE_CUES`：量測 / 鍛打 / 刪除 /
  重跑 / 對焦），刻滿了也可以有（`REGION_SEAL_CUES`）。換皮的是材質，不是文法 ——
  其餘區域仍然是通用的那一聲。
- **同一件事變大了，不是換一套語言**：應用關（試煉）過關響的是**鑼**（`trialPass`），
  一般過關是頌缽 —— 鑼是頌缽的放大版，同屬擊奏金屬共鳴。硬門檻真的存在時開的是
  **厚重閂鎖**（`hardGate`），不是一般解鎖的微光 ＋ 石門：你是達成條件把它解開的，
  不是走過去它讓開的。（2026-08-03 起沒有任何一區是硬門檻，這支 cue 是留著的退路。）
- master 之後掛 `DynamicsCompressorNode` 再進喇叭；配樂前面再掛一顆 duck，
  過關的頌缽響的那幾秒把床壓 3 dB。
- 反應音要三層節流：**全域冷卻**（任兩聲 ≥ 90ms）＋ **每個觸發點自己的冷卻**（≥ 2 秒）＋ **「最近放過的音」環狀緩衝**。
- 環狀緩衝的作法是**換一個音**，不是不放聲音——沉默不是回應。
- 面板打開 / 序章進行中：整組停手。讀題的時候世界不該在旁邊叮咚響。

---

## 七、參考（這些規則是從哪裡來的）

- [Bruno Simon — Portfolio case study](https://medium.com/@bruno_simon/bruno-simon-portfolio-case-study-960402cc259b) — 互動區域「走進去就放大」＝ 區域本身就是回饋，不用 UI；碰撞音靠「同一組相似音隨機調整音量與速度」；用地磚鋪出一條路來暗示「那邊還有東西」。
- [Creating an Interactive Audio Environment — Game Developer](https://www.gamedeveloper.com/audio/creating-an-interactive-audio-environment) — 環境音用**稀疏的一次性觸發**而不是更大聲的循環；保留「最近放過的 N 個音」並拒絕重複；區域之間用 2–3 秒的重疊帶交叉淡入，不要硬切。
- [Developing game audio with the Web Audio API — web.dev](https://web.dev/articles/webaudio-games) — 改播放速率（同時改音高）來模擬真實世界的隨機性；最後一級放 `DynamicsCompressorNode`，一秒內連響好幾聲才不會削波。
- [Three.js Instances — Codrops](https://tympanus.net/codrops/2025/07/10/three-js-instances-rendering-multiple-objects-simultaneously/) — 用 `InstancedBufferAttribute` ＋ 一個 `uPlayerPos` uniform，整片反應元素只要一個 draw call。

Phase 25（動得了的器物）多查的四份：

- [Open world level design: spatial composition and flow in Breath of the Wild — Robert Yang](https://www.blog.radiator.debacle.us/2017/10/open-world-level-design-spatial.html)
  — **三個明確分開的高度階**，而且「階與階之間的對比要守住，不要做出一堆糊在一起的形狀」；
  避免 A 到 B 是一條直線的視線（走過去要能**揭露**新東西）；用地形做「重力」，不要畫路線。
  → 本文件 §4.7 的三階表就是從這裡來的。
- [Environmental Storytelling — Don Carson（Game Developer）](https://www.gamedeveloper.com/design/environmental-storytelling-creating-immersive-3d-worlds-using-lessons-learned-from-the-theme-park-industry)
  — 細節是**有限的預算**，要留給你想把人吸過去的地方；「一間大房間裡一道光打在一件道具上」
  勝過「塞滿細節的房間」；玩家要能在 15 秒內回答「我在哪裡」。
  → 器物之間硬性 14 公尺、中間留白，就是這一條。
- [Proximity Prompts — Roblox Creator Documentation](https://create.roblox.com/docs/ui/proximity-prompts)
  — 提示要拆成**物件名 ＋ 動作動詞**兩格（不是一句「互動」）；同時在範圍內的多個提示
  **由鏡頭指向決定顯示哪一個**，不是由距離決定；`HoldDuration = 0` 給便宜、可逆的動作，
  按住只留給需要確認的動作。→ §3.2 的面向排名與「動詞不重複」就是這一條。
- [GDC 17: Breath of the Wild, science, and clever little lies（Thumbsticks 的整理）](https://www.thumbsticks.com/gdc-17-breath-of-the-wild-science-lies/)
  — 整個世界只有兩類東西（元素 / 材質）與三條規則，其中「材質彼此不互相影響」是**成本控制**規則，
  把互動矩陣從「所有東西 × 所有東西」壓成「元素 × 材質」。
  → 我們的版本：**一個動詞（`E`）× N 種器物**，永遠不寫 N×N 的特例。

三份既有的設計基礎：thibault-introvigne.com（探索／氛圍／發現／配樂的體感）、
Asher Vollmer 的 GDC「How to Make Great Game Tutorials」（教會 / 安撫 / 引發興趣 / 尊重玩家）、
學習科學的 worked example → faded scaffolding（序章三堂課的鷹架遞減）。

---

## 八、維護檢查表（MAINTENANCE）

> **每一次新增或修改世界內容，逐條回答。有任何一條答不出來就先別動手。**

### A. 世界觀
1. 這件東西在世界觀裡是什麼？誰做的、做什麼用、為什麼還留在這？
2. 它用的是世界的說法，還是系統術語？（見 §3.6 的禁字表）
3. 如果它會說話——是回聲嗎？如果不是，能不能改成一件留在原地的東西？

### B. 互動文法
4. 它屬於四層互動的哪一層（§3.2）？互動半徑有沒有跟上下層打架？
5. 有互動的話，是不是只用 `E`？走近提示有沒有標題 ＋ 狀態 ＋ 動詞？
5b. **不用按鍵的那一層呢？**（v1.2 · P20b 起有兩種：會回應的東西、檔案廊）——
   它一定要在「有任何一層拿得到 `E`」的時候**自己讓開**，而且讓開時要順手把世界端的
   亮度／狀態一起熄掉（`clearNear()` 那一支）：`nearest()` 是唯一會清那個旗標的地方，
   而互動迴圈在高階層贏的時候就早退（P20a 審查 · 第 4 條記過一次，P20b 照抄）。
6. 有沒有做到「一次只有一件事擁有畫面」？揭示是分幕／分行的嗎？
7. 玩家會不會「失敗」？如果會，能不能改成「不收但教你」？
7b. 它會不會被玩家「打」？如果會——攻擊必須＝prompt 品質，威脅不懲罰、進度只累積（§1.6／§3.5）；牠是不是留在原地的東西？會動的是不是只有頭、光、霧、殼？
7c. 它會不會「不放你過」？如果會——那不是失敗態，是**還沒被說服**（§1.5 守門者）：畫面上不准出現「失敗／拒絕／再試一次」那一類字（禁字表逐句掃），而且已經說服到的每一格**永不退回**，分兩次說完也要成立。

### C. 教學內容（護欄 2）
8. 它教東西嗎？
   - **教** → 一定要掛 `techniqueId`，教學句子取自既有的中文說法（不自己編一份），
     並顯示技巧名 ＋ **可點的官方出處**（`神諭原典：<文件名> ↗`）。資料層不准手抄 `source`。
   - **不教** → 那就一個字都不准宣稱技巧，不准放連結，不准有 `source` / `teaches` 欄位。
9. 有沒有新增中文字串？**跑過 `npm run fonts` 了嗎？**（沒跑測試會直接失敗）
10. 玩家看得到的文字有沒有整句英文？（`scripts/zh-scan.mjs` 的規則：連續 3 個英文單字＝一句英文句子）
10b. **新的教學句子是哪一層？**（v1.2 · P20b 之後這裡有四層，寫錯層就會變成重講一次）
   - 「**是什麼**」→ 術語小卡（`glossary.json`，§3.7）：白話一句話，**不准放連結**。
   - 「**怎麼做**」→ 130 條技能（`skill-codex-v2.json`）：出處是 master list 帶來的，不手抄。
   - 「**引用**」→ 守夜人的「教我一手」：一個字都不新寫，只引既有那一條的 `oneLiner` ＋ 它自己的連結。
   - 「**為什麼**」→ 檔案廊（`archive.json`，§4.18）：≤60 字，出處**只准用 repo 裡已經驗證過的那 365 個網址**
     （`curriculum.json`／`skill-codex-v2.json`／`source-anchors.json`／`glossary.json` 裡出現過的），
     而且要與另外三層逐則比對過不重複。**在既有出處裡找不到支撐的說法就不要寫**——誠實留白永遠比掛一個沾邊的連結好。

### D. 美術與調性
11. 有沒有維持「平靜的夜間探索」？有沒有不小心變亮、變吵、變擁擠？
12. 用的是該區的色階（`kitFor`）嗎？暖金有沒有被拿去用在非成就的地方？
13. 高的東西旁邊留白了嗎？新東西有沒有蓋掉地標的剪影？
14. `prefers-reduced-motion` 下還讀得懂嗎？（關掉的是動，不是回應）
14b. 動到地面顏色或粒子？—— 地面走 `src/world/ground.js`（每區兩色 ＋ 區界 6 公尺漸變 ＋ 碎紋，
    **低畫質只留基底**）、粒子走 `src/world/drifts.js`（一區一個 `Points`、共用材質、低畫質整層關）。
    改了 `color-script.json` 就重跑 `npm run colors:table` 貼回 §2.2 的表，**不手抄**。

### E. 效能（§6.1）
15. 新增了幾個三角形、幾盞燈、幾個 draw call？**有沒有新增光源？（預設答案應該是「沒有」）**
15b. 動到中觀層（§4.10 / §4.12）？—— 座標一律用 `node scripts/screen-fit.mjs` 搜（「改資料 → 重建世界 → 量」），
    放進去之後 `node scripts/screen-fit.mjs --verify` 要全綠；**每一片土地的中觀碰撞體 ≤ 22 個**
    （v1.2 · P15 由 20 抬到 22：一片多兩座高台）。
    **同一片土地裡的三種東西之間也有距離**（v1.2 · P16a）：母題↔母題 16、高台↔高台 16、
    高台↔母題 12、母題／高台↔**遮擋帶核心矩形** `BAND_CLEAR`（2.64）＋ 自己的半徑 ——
    最後那一條是 P16a 才補的，補之前「先擺帶、再搜高台」會把高台放在離石脊 0.45 公尺處。
    **母題與遮擋帶都是可選的**：`screen-fit` 說擺不下就不擺（§4.10 ⑤），不要為了湊數放寬別人的淨空。
    **但每一片土地至少要有其中一種**（v1.2 · P16b 起是硬斷言）——12 片全部有了，這條線不准退回去。
    加任何一道帶之後都要重跑 `npm run audit:sightline`：**「不負責擋視線」的第二道帶照樣會改變揭露**
    （P16b 實測：觀象臺第二道擺錯位置會把揭露從第 12 公尺推到第 33 公尺）。
    量到「這片土地擺不下會擋住地標的那一道」時，登記進 `screens.js` 的 `SIGHT_EXEMPT`
    （要寫理由、要說得出還差多少）——`test:rubric` 會反過來守著「登記過的土地真的沒有一道擋得住」。
15c. 動到反應物的觸發半徑？—— 只有 `reactive.js` 的 `REACT_TRIGGER_R`／`reactiveTriggerR()` 一份，
    建造器與中觀層的淨空規則都讀它（v1.2 · P16b；在那之前淨空整層套用最大的那一個 4.4）。
16. 重複的元素用 InstancedMesh 了嗎？幾何體與材質走快取了嗎？
17. 每幀迴圈裡有沒有 `new`、`map/filter`、閉包？距離判定是平方比較嗎？有沒有分級？

### F. 碰撞與淨空（§6.3 / §6.4）
17b. 動到濁靈（§1.6／§4.8）？—— 先問是哪一個尺寸：小濁靈（`kind` 缺省、3 層殼、5.5）
    還是**大濁靈**（`kind: "great"`、6–8 層殼、6.0、底座 1.5）。
    門檻只有一份（`scripts/lib/screen-rules.mjs` 的 `GREAT_MURK_*`），
    落點一律用 `node scripts/murk-fit.mjs --region <id>` 搜、`--verify` 逐隻重建驗過，不要手挑。
    放鬆了石座那一條（9.0）就要用**看得到石座**的量法補回來：
    不准堵住石座周圍 5 公尺那一圈，而且「按得到牠的方向 ≥ 8/24」。
    **既有的濁靈一個位元組都不准改**（`test:rubric` 逐位元組驗指紋）。
18. 這件東西符合「有份量」的四條規則嗎？符合就一定要有碰撞體。
18b. 動到**一組各自站在地上的零件**（小景 `STORY_VIGNETTES`，或任何
    「一個 holder ＋ 一堆局部位移」而每一件都是獨立一件東西的層）？
    —— 每一件貼的是**自己腳下的地**，不是組中心那一點（§6.1）。
    整件的雕塑（地標）不算，它立在自己的臺座上、本來就共用一個平面。
    局部位移要先套 `holder.rotation.y` 才是世界座標；
    `dy !== 0` 的那幾件擺的是相對關係，**一件都不要貼地**。
    改完跑 `node scripts/vignette-fit.mjs --world`（`test:rubric` 逐件跑同一支）。
    碰撞圓的數量會跟著動 —— 那是對的，不是回歸（理由在 §6.1）。
19. 有沒有壓到石座淨空圈、橋的主動線、出生點、祭壇？
20. 走近它的四周（互動半徑內）**每個方向**都走得到嗎？
21. 落點在區域內、`coverage > 0.85`、沒有掉進虛空嗎？
21b. 它的**頂面**站得上去嗎（§6.3「頂面：碰撞的第四個維度」）？——
    站得上去就一定要有碰撞體（就算它飄在半空），而且 `standTop`／`standR` 要量得出來；
    刻意要讓人跳上去的，走 §4.12 的高台那一套（`PLATFORMS` ＋ `screen-fit --kind platform`）。
21c. 動到跳躍（§3.1）？—— 常數只有 `src/player/jump.js` 一份；
    「不按 `J` 就與之前逐幀相同」這條要**結構上**成立，不是量出來很接近；
    空中的水平位移一律走 `clampPosition()`，護欄一寸都不准放寬。
    多開一片土地就要問：**那一片真的有高台嗎**（不然是白開）？
21d. 動到高台（§4.12）？—— 高度要在 1.6–2.4，而且**最差幀率下的離散頂點還要多 0.3 公尺**
    （`simulateApex()`；蓋一座跳不上去的高台等於蓋一面牆）；
    **而且要從四周每一個站得住的方向都跳得上去**（`platformRise()`——
    `height` 量的是離自己腳下的地多高，人卻是站在旁邊起跳的，地形一斜就會做出
    「standable 為真、卻有一半方向跳不上去」的高台）；那一片土地要在 `JUMP_REGIONS` 裡，
    反過來也一樣（沒有高台的土地不先開跳躍）；
    造型換皮可以，**頂面永遠是圓的**；裝飾一律不擋人、壓在 0.9 公尺以下（或做成薄片／細桿 ——
    穿模稽核看的是「中間那一維 ≥ 0.9」）；**一片土地一種造型**，而且實作出來的每一種都要有土地在用。
    **開一片新的土地之前先量**（§4.12 的表）：`screen-fit --kind platform` 說 0 個合法落點，
    那一片就不蓋高台、也不進 `JUMP_REGIONS`。
21e. 動到橋（§4.13 / §6.4）？—— 主動線上開洞是**唯一的登記例外**，
    條件是同一段一定要留一條 `LANE_HALF` 之外的窄板；改完要跑
    `test:rubric` 的「不按 `J` 的可達性」洪水填充（每一座橋走得完、每一個互動點到得了）。
21g. 動到**高度場**或**走路判定**（§6.3「走不走得到」）？—— 三件事一起看，不准只改一半：
    `coverage ≥ STAND_COVER_MIN`、`rimDistance ≤ 0` 與「地面沒有往下崩」必須是**同一句話**
    （`test:rubric` 逐點驗 `terrainHeight() === terrainRelief()`）；
    斜度上限 `WALK_SLOPE_MAX` 只擋**地本身的起伏**，虛空歸崖唇管。
    改完一定要重跑四件事：
    ① 12 片土地的**邊緣落差 < 3 公尺**；
    ② 「不按空白鍵」的**全地圖洪水填充**（每一個互動點、每一座橋、四個頸口）；
    ③ 「還是平地卻走不過去的那一圈 < 2 公尺」——量法是**到走得到的地的真實距離**
    （chamfer，與方向無關），而且**土地邊緣與橋的甲板邊用同一把尺**
    （P16d 只驗了土地，甲板邊當時是 0.30–3.20 公尺）；**換一個看不見的牆不算修好**；
    ④ **對著真的被畫出來的那張網格**量「人有沒有浮在畫面的地面上」——
    高度場是解析式、地形是固定格點（高畫質 1.70 m／低畫質 3.09 m 一格），
    拿解析式自己跟自己比是一條永遠成立的斷言（P16d 就是這樣漏掉 9.2／17.5 公尺的浮空）。
    任何新的崖／坎，**過渡帶要比低畫質一格的對角線（4.37 公尺）寬**。
    碰撞體的數量也會跟著動：地面一抬，原本被判成「飄在半空」而整件跳過的道具
    會回到地面上開始擋人（P16d：+11 顆圓）。
    最後：任何收緊「走得到」的規則都要順手問一次 `escapeSolid()` ——
    **絕不能把玩家關住**（`test:rubric` 掃每顆碰撞圓 × 24 角 × 4 半徑，一個都不准推不出去）。
21h. 動到**捷徑**（§4.16）？—— 走廊走的一定是與 `CORRIDORS` **同一套**
    （`groundAt()` 的走廊分支 ＋ `coverage()`，`LANES` 就是「橋 ＋ 捷徑」那張表），
    不准另外造一套高度場；甲板兩端的高度是**問出來的**（「沒有這條走廊時這裡多高」），
    不是手打的常數；擋人的是 `isWalkable()`，不是石頭；門底下那一段擋得住、
    **兩側都要走得到門前**；改完照 21g 重跑那四件事，再加一條：
    **走廊上除了那兩座絞盤不准有第三顆碰撞圓**（`test:rubric` 逐點掃）。
21i. 動到**導向**（§4.4）？—— 它是既有螢火群的偏向量，不是新的一層：
    0 新粒子、0 新光源；**關得掉**，而且關掉之後要逐值回到原樣（不是「差不多」）；
    `reducedMotion` 只留靜態的那一段；低畫質整層關掉；目標只准走 `objectiveTarget()` 那一套。
21f. 新增祕密（§4.5）？—— 每一處都要有一種 tell（`odd` / `sound` / `high`），
    三種都要有人用；`high` 那一種要指得到一座真的高台，而且**兩邊互相指得回去**
    （`PLATFORMS.reveals` ↔ `secrets.onPlatform`）。

### G. 存檔
22. 有新欄位嗎？`normalize()` 給預設值了嗎？舊存檔會不會壞、會不會被塞回教學？
23. 新的收集會不會不小心寫進 `bestGrades`／被算成區域解鎖的通關數？（不該）
24. 重置清得乾淨嗎？
24b. 世界端有沒有「單向」的視覺狀態（隱藏的殼、清燈、點亮的東西）？進度重置**不重載頁面**時，它有沒有 `reset()` 路徑跟著歸零？（濁靈：`world.murks.reset()`）

> **Phase 25 逐條過的結果**（新增的第五層互動「動得了的器物」）：
> A1–3 抄寫人留在原地的東西、用世界的說法、沒有會說話的角色；
> B4 第五層／半徑 3.2（排在刻文小語 3.8 底下）、B5 只有 `E`（絞盤要推三下也是按三次 `E`）、
> B6 開窗的兩種走 `.reveal` 分行揭示、其餘六種只說一句話、B7 推到一半不算失敗；
> C8 **不教** → 零 `techniqueId` / 零 `source` / 零連結（測試強制驗證）、C9 `npm run fonts` 已重跑、
> C10 `zh-scan` 掃過每一段玩家看得到的字；
> D11–14 夜色不變、用該區 `kitFor` 的色階、暖金只給火與成就、`prefers-reduced-motion` 下只關「動」不關「回應」；
> E15 **新增 0 盞光源**（三角形 +9.3k、網格 +214）、E16 幾何體與材質全部走模組快取
> （22 件各自獨立、遠低於 InstancedMesh 划算的 50 份門檻）、E17 每幀零配置 ＋ 平方距離 ＋ 45／15 公尺分級；
> F18–21 碰撞由 `markSolidParts` 統一判定、22 件的四周 20 個方向全部走得到（碰撞體淨 −21）；
> G22–24 `handlesUsed` 純加法、`normalize()` 補預設、不寫 `bestGrades`、重置清得乾淨。

> **Phase 27 逐條過的結果**（兩種新題型 ＋ 第 27 座石座）：
> A1–3 神諭工坊是抄寫人的派工檯、用世界的說法（工具牌 / 值石 / 派工單 / 立規矩）、沒有新的會說話的角色；
> B4 石座那一層（半徑 6.5，沒有新層）、B5 走近仍然只按 `E`、B6 四幕分鏡未動且新題型一次只有一步擁有畫面、
> **B7 排錯時手掌印根本不出現，挑錯 / 放錯只是「不收 ＋ 教你」**；
> C8 **教** → 每條 rubric 掛真的 `techniqueId`、第二幕照舊顯示「神諭原典：〈文件名〉↗」、
> 遊戲自撰的示範層一律在 `flows.json`（`authored: "game"`）且不自帶連結、C9 `npm run fonts` 已重跑、
> C10 `zh-scan` 掃過工具牌 / 值石 / 參數教學 / 規矩 / 排序石版的每一句；
> D11–14 夜色不變、暖金只給「位置對了」與「收下了」這種成就熱點、
> `prefers-reduced-motion` 下拖曳與搬動變成瞬間就位（光還是會亮、聲音還是會響）；
> E15 世界只多了一座石座（+1 盞石座光、仍在 56 的預算內），介面那兩種題型是純 DOM、零場景成本、
> E17 拖曳不做每幀 raycast，只在 pointermove 比 `clientY`；
> F18–21 新石座落在 `[-115, 66]`，離最近的石座 20.9 公尺、避開所有小景 / 地標 / 石碑 / 刻文 / 器物 / 祕密的淨空圈、
> 24 個方向 × 2–6 公尺都走得到（高低兩種畫質都掃過）；
> G22–24 **沒有新增存檔欄位**（新關卡只是多一筆 `bestGrades`），舊存檔完全不受影響。

> **課程 v2 · Phase E 逐條過的結果**（第六片土地：量器坊）：
> A1–3 抄寫人量過所有東西、最後量到自己頭上的鑄場；用世界的說法（模子 / 量尺 / 鑄槽 / 刻度）；
> 沒有新的會說話的角色（14 個委託人都只在關卡文字裡出現）；
> B4 石座那一層（半徑 6.5，沒有新層）、B5 走近仍然只按 `E`、B6 四幕分鏡未動、
> B7 這一區沿用既有八種題型，選錯一律「不收 ＋ 教你」；
> C8 **教** → 每座掛真的 `primarySkillId`，第二幕顯示「神諭原典：〈文件名〉↗」（出處逐條回查
> master list）、C9 `npm run fonts` 已重跑（CJK 1771 字）、C10 `zh-scan` 掃過每一句玩家看得到的字；
> D11–14 夜色不變（冷錫色、螢火密度 0.62 是全世界最低）、用該區 `kitFor` 的色階
> （主色 `#c2c79f` 寫在 `regions-v2.json`）、地標周圍留白 15 公尺、`prefers-reduced-motion` 下照舊；
> E15 **光源 +1**（每區一盞主色補光，與其他四片土地同一個模式；**地標本身 0 盞**）、
> 三角形 +7.8k、E16 量尺柱／刻度／鑄槽／植被全部 InstancedMesh ＋ 共用快取、E17 每幀迴圈未動；
> F18–21 14 座石座在高／低兩種畫質下 24 個方向 × 4 段距離全部走得到、正南那條橋的主動線整條無阻擋、
> 落點全部 `coverage > 0.8`、避開地標／小景／橋／出生點的淨空圈；
> G22–24 **沒有新增存檔欄位**（新關卡只是多幾筆 `bestGrades` 與 `skillsV2`），舊存檔完全不受影響。

> **課程 v2 · Phase G 逐條過的結果**（加建：校驗場；流程與代理收尾；新題型：兩輪刻印）：
> A1–3 改 prompt 的 prompt 在齒輪工坊旁的院子裡被改；兩面互相照著的鏡是它的地標；
> 用世界的說法（刻印中的 prompt ／ 神諭的回話 ／ 量尺單 ／ 交班紀錄 ／ 派工單）；沒有新的會說話的角色；
> B4 石座那一層（半徑 6.5，沒有新層）、B5 走近仍然只按 `E`、B6 四幕分鏡未動、
> B7 這一區新增第九種題型「兩輪刻印」，選錯一律「石碑不收 ＋ 教你」，手掌印與結尾完全沿用；
> **中間那一段神諭回話一律標 `authored: "game"` 並在畫面上掛 ⓘ 明講它不是模型跑出來的**（§3.3b 第 7 條）；
> C8 **教** → 23 座各掛真的 `primarySkillId`，第二幕顯示「神諭原典：〈文件名〉↗」；
> C9 `npm run fonts` 已重跑、C10 `zh-scan` 掃過每一句玩家看得到的字；
> D11–14 夜色不變（銀灰、螢火密度 0.86）、用該區 `kitFor` 的色階、地標周圍留白 14 公尺、
> `prefers-reduced-motion` 下回話卡不做入場動畫但照樣看得見；
> E15 光源 +1（每區一盞主色補光；**地標本身 0 盞**）、E16 照面架／稿堆／植被全部 InstancedMesh ＋ 共用快取、
> E17 每幀迴圈未動；F18–21 23 座石座在高／低兩種畫質下 24 個方向全部走得到、頸口整條無虛空、
> 落點全部 `coverage > 0.8`、避開地標／小景／石碑／刻文／反應物／器物／祕密的淨空圈
> （加建吃掉工坊西南角之後，有四個既有物件搬了家，逐一記在 `findings.md`）；
> G22–24 **沒有新增存檔欄位**（新關卡只是多幾筆 `bestGrades` 與 `skillsV2`），舊存檔完全不受影響；
> **輪次狀態刻意不落地**（§3.3b 第 9 條）。

> **課程 v2 · Phase F 逐條過的結果**（第七片土地：契約鍛冶場；加建：護欄崗）：
> A1–3 抄寫人替神諭打造它的手、卻沒有替任何一把刻上名字的鍛冶場，與檔案庫外那座沒有人在的哨所；
> 用世界的說法（工具牌 / 值石 / 派工單 ／ 試門單 / 內容石 / 權限表）；沒有新的會說話的角色；
> B4 石座那一層（半徑 6.5，沒有新層）、B5 走近仍然只按 `E`、B6 四幕分鏡未動、
> B7 這一區沿用既有八種題型，選錯一律「不收 ＋ 教你」；
> C8 **教** → 16 座各掛真的 `primarySkillId`，第二幕顯示「神諭原典：〈文件名〉↗」；
> **護欄崗的安全敘述一律掛回官方安全文件**，而且畫面上不把 prompt 文字宣稱成安全邊界（rubric ＋ e2e 各守一次）；
> C9 `npm run fonts` 已重跑（CJK 1790 字）、C10 `zh-scan` 掃過每一句玩家看得到的字；
> D11–14 夜色不變（鍛冶場暖褐、哨所最冷）、用該區 `kitFor` 的色階
> （主色寫在 `regions-v2.json`）、地標周圍留白 15 / 13 公尺、`prefers-reduced-motion` 下照舊；
> E15 **光源 +3**（兩盞主色補光 ＋ 鍛冶場小景的一盞；**兩座地標本身 0 盞**）、三角形 +13.2k、
> E16 工具架／刻痕／崗柱／矮牆／鑰匙／植被全部 InstancedMesh ＋ 共用快取、E17 每幀迴圈未動；
> F18–21 16 座石座在高／低兩種畫質下 24 個方向 × 4 段距離全部走得到、正西那條橋的主動線整條無阻擋、
> **加建的頸口整條路都是實地**（測試逐點掃過）、母土地的 13 座石座一座都沒有被改判；
> G22–24 **沒有新增存檔欄位**（新關卡只是多幾筆 `bestGrades` 與 `skillsV2`），舊存檔完全不受影響。

> **課程 v2 · Phase H 逐條過的結果**（第十片土地：減法之庭 · 高原加建 ＋ 新題型「轉鈕」）：
> A1–3 抄寫人在這裡學會**拿掉**：石匠只刻該刻的、抄了兩遍的抄寫人、每天從頭疊石頭的疊石人；
> 用世界的說法（空的基座 / 托座 / 印子 / 旋鈕的檔位）；沒有新的會說話的角色；
> B4 石座那一層（沒有新層）、B5 走近仍然只按 `E`、B6 四幕分鏡未動、
> B7 新增第十種題型 `sim`（轉鈕），轉錯不會失敗、轉完才開放刻印、結尾仍是同一隻手掌印；
> C8 **教** → 7 座各掛真的 `primarySkillId`，第二幕顯示「神諭原典：〈文件名〉↗」；
> **轉鈕的三檔輸出是遊戲自撰的離線樣本**，畫面上掛 ⓘ 明講、並附「在哪一台機器上成立」的條件（§3.3b 第 10–12 條）；
> C9 `npm run fonts` 已重跑（CJK 1832 字）、C10 `zh-scan` 掃過每一句玩家看得到的字；
> D11–14 夜色不變（減法之庭霧最淡、螢火最少）、用該區 `kitFor` 的色階（主色寫在 `regions-v2.json`）、
> 地標周圍留白 13 公尺、`prefers-reduced-motion` 下照舊；
> E15 **光源 +1**（那一盞主色補光；**地標本身 0 盞**）、三角形 +7.0k、
> E16 空托座／光印子／印子／植被全部 InstancedMesh ＋ 共用快取、E17 每幀迴圈未動；
> F18–21 7 座石座在高／低兩種畫質下 24 個方向 × 4 段距離全部走得到、
> **加建的頸口整條路都是實地**（測試逐點掃過）、母土地的 15 座石座一座都沒有被改判
> （代價：高原北緣的 `wordfork-12` 原本就站在頸口正中央，往南挪了 16 公尺 —— 只動座標，
> 題目與評分一個位元組沒動，理由記在 `findings.md`）；
> G22–24 **沒有新增存檔欄位**（新關卡只是多幾筆 `bestGrades` 與 `skillsV2`），舊存檔完全不受影響。

> **課程 v2 · Phase I 逐條過的結果**（第十一片土地：觀象臺 · 正東偏北的小地形）：
> A1–3 抄寫人在這裡學會**看**：觀星人、校對人、製圖人、修圖人、分鏡師、傳令、門面師、工造人；
> 用世界的說法（觀測紀錄 / 落鏡 / 觀測架 / 分鏡牆的石版）；沒有新的會說話的角色；
> B4 石座那一層（沒有新層）、B5 走近仍然只按 `E`、B6 四幕分鏡未動、B7 **沒有新題型**
> （8 座沿用既有的 choice／fix／order／multi／tradeoff 五種）；
> C8 **教** → 8 座各掛真的 `primarySkillId`，第二幕顯示「神諭原典：〈文件名〉↗」；
> **多模態的誠實界線**見 §3.3c：不引進任何媒體資產、零外部請求、素材是文字；
> C9 `npm run fonts` 已重跑（CJK 1847 字）、C10 `zh-scan` 掃過每一句玩家看得到的字；
> D11–14 夜色不變（觀象臺空氣最乾淨、看得最遠）、用該區 `kitFor` 的色階
> （主色寫在 `regions-v2.json`）、地標周圍留白 14 公尺、`prefers-reduced-motion` 下照舊；
> E15 **光源 +2**（一盞主色補光 ＋ 小景裡本來就有的製圖桌燈；**地標本身 0 盞**）、三角形 +7.0k、
> E16 觀測架／落鏡／植被全部 InstancedMesh ＋ 共用快取、E17 每幀迴圈未動；
> F18–21 8 座石座在高／低兩種畫質下 24 個方向 × 4 段距離全部走得到、
> 正東偏北那條橋的主動線整條無阻擋、與沉書檔案庫之間留得出 6.3 公尺的虛空、
> **那條橋不會擦過檔案庫**（橋線離檔案庫中心最近 81.5 公尺）；
> G22–24 **沒有新增存檔欄位**（新關卡只是多幾筆 `bestGrades` 與 `skillsV2`），舊存檔完全不受影響。

> **課程 v2 · Phase J2 的逐條結果**（12 座應用關 ＋ 土地印記 ＋ 大師層印記）：
> A1–3 試煉是「地標腳下的一座考場」，考官是既有的 NPC 家族，回聲沒有多說話；
> B4–7 它走的是既有的 `E` ＋ 四幕文法，只是**第二幕整幕不存在**（§3.3d 第 1 條）——
> 誠實不畫，不是鎖住；選錯／寫不好仍然只是「還沒到」，不會失敗；
> C8–10 **它不教東西**，所以畫面上一個官方連結都沒有（§3.3d 第 2 條），
> 資料層的 `source` 只溯源不上畫面；中文字串有變 → 已重跑 `npm run fonts`
> （**撞到 1.5 MB 硬牆，換掉 11 個新字**才回到預算內）；zh-scan 全綠；
> D11–14 沒有新增任何美術資產，印記在圖鑑上是最安靜的一塊（沒有色塊、沒有動畫），
> 暖金只用在「已經拿到」的那一枚；`prefers-reduced-motion` 下沒有動的東西可關；
> E15–17 **光源 +0**、三角形 +12 座石座的量、每幀迴圈未動；
> F18–21 12 座石座在兩種畫質下 24 個方向 × 4 段距離全部走得到、穿模稽核 0 件、
> 離地標 ≥18、彼此 >13、離橋的主動線 ≥8.5；**護欄崗把地標讓到邊緣**才排得下第六座
> （§1.4 的例外，理由寫在 `findings.md`）；13 座既有石座只動座標；
> G22–24 新增 `seals` / `penlessSeals` / `scribeSeals` / `samplesSeen` 四欄，
> 全部純加法、`normalize()` 補空陣列、去重、reset 清乾淨，
> **一格都不影響解鎖與既有 finale**（靜態掃描把關）；
> H25–27 三份測試都補了（rubric 的 Phase J2 一節、playtest 的三種情境、e2e 的完整走查）。

> **v1.2 · P07 逐條過的結果**（第七層互動「抄寫人的殘頁」＋ 回信碑 ＋ `firstPrompt`）：
> A1–3 抄寫人留下的工單／信／筆記，全部是留在原地的紙，沒有新的會說話的角色；用世界的說法、禁字表逐句過；
> B4 第七層／半徑 3.8，仲裁排在刻文小語之後、器物之前（§3.2）、B5 只有 `E`、
> B6 小窗一行一行浮現（`.reveal`）、B7 撿不撿都沒有失敗態；
> C8 **一半教一半不教**：教的那 12 頁掛真實 `techniqueId`、教學句取自既有中文層、附得出與 curriculum 逐字相同的官方連結；
> 純風味的 12 頁零 `techniqueId` / 零 `source` / 零連結（測試強制驗證）、C9 `npm run fonts` 已重跑、C10 zh-scan 全綠；
> D11–14 紙是最小的雜物階（< 0.9 公尺），只有紙邊一點微光當 tell，`prefers-reduced-motion` 下沒有東西在動；
> E15–17 **光源 +0**、24 頁共 < 8k 三角形、幾何與材質走模組層快取、每幀只有既有的那一個迴圈多掃 24 個物件（零配置）；
> F18–21 每一頁都低於 0.9 公尺 → 碰撞稽核判定跨得過去、不登記碰撞體；落點逐條驗（§4.9），穿模稽核仍 0；
> G22–24 新增 `lettersFound` 與 `firstPrompt` 兩欄，純加法、`normalize()` 給預設、不寫 `bestGrades`、reset 清乾淨；
> H25–29 rubric 補了「抄寫人的殘頁」一整節（先紅後綠實測），e2e 補了撿一頁 → 圖鑑第五列 → 重整還在、
> 回信碑三種筆跡、序章第一句只記一次。

> **v1.2 · P20a 逐條過的結果**（傳聞連線頁 ＋ 回聲重演）：
> A1–3 回聲本來就是這個世界唯一會直接說話的存在（§1.2），它記得這裡發生過什麼；
> 用世界的說法（那一處／那一天／殘影），沒有新的會說話的角色；
> B4 **第 ⑪ 層、仲裁排最後一位**、半徑 3.2（最小的那一階，誰要用 `E` 都讓）、
> B5 只有 `E`、走近提示是標題 ＋ 狀態 ＋ 動詞、B6 **不開面板**（畫面留給那一幕，話只用一行 toast）、
> B7 沒有失敗態（看幾次都可以，也可以不看）；
> C8 **不教** → `echoes.json` / `rumors.json` 零 `techniqueId` / 零 `source` / 零連結（測試強制驗證）、
> C9 `npm run fonts` 已重跑、C10 `zh-scan` 掃過每一段玩家看得到的字；
> D11–14 夜色不變、用該區 `kitFor` 的色階、暖金一格都沒動到、
> **`prefers-reduced-motion` 直接給結果**（位移全關、透明度照樣淡入淡出，而且結果那一句提前說）；
> E15 **新增 0 盞光源**（三角 +1,800、碰撞體 +0）、E16 幾何體走模組快取、材質逐處各一份
> （透明度是每一處自己的狀態）、E17 每幀零配置 ＋ 平方距離 ＋ 45／15 公尺分級、**低畫質整層不蓋**；
> F18–21 這一層 **0 個碰撞體**（每一塊都 `noCollide`），穿模與可站立體稽核於是都是 0；
> 擺位吃既有那一整套（互動圈不重疊、離地標 4、離路網 ≥3、中觀層的淨空、離閘門 8），
> 兩處石座例外是逐點掃出來的上限（§4.17）；
> G22–24 **一欄都沒有新增**（存檔頂層欄位逐鍵比對契約）、不寫 `bestGrades`、
> 重置時 `world.echoes.reset()` 把正在演的那一場收掉（24b 同一族的第六件）。

> **v1.2 · P21 逐條過的結果**（轉折：中點揭示 ＋ 鏡碑第二層）：
> A1–3 兩塊碑都是抄寫人留在原地的東西（柱腳的碑記下柱子兩面各說了什麼、鏡碑記下那面鏡照的是什麼）；
> 用世界的說法（刻／筆跡／照）；會說話的只有回聲，而且只說一句（§1.2）；
> B4 **沒有新增互動層**——兩塊碑走的是既有的石碑那一層（半徑 4.6，仲裁位置一格沒動）、
> B5 `E` 仍然是唯一的互動鍵、走近提示照舊是標題 ＋ 狀態 ＋ 動詞、
> B6 揭示仍然是一行一行浮現（`--i`），回聲那一句走既有的 toast 通道、**零新增 UI**、
> B7 沒有失敗態：**沒到門檻只是「那一層還沒亮」**，碑還在原地、還走得到、還讀得到前幾層；
> C8 **不教** → 兩塊碑零 `source` / 零 `teaches` / 零連結（測試強制驗證）、
> C9 `npm run fonts` 已重跑（CJK 1951 字，一個新字都沒加）、C10 `zh-scan` 掃過每一句；
> D11–14 夜色與色階一格沒動（`buildTablet()` 完全沿用），暖金只在碑頂那一點成就光上，
> `prefers-reduced-motion` 下石碑面板照舊（本來就沒有位移）；
> E15 **新增 0 盞光源**（光源 37 不變；三角 235,436 → 237,264、碰撞體 1,047 → 1,049 ＝ 兩塊碑各一顆）、
> E16 幾何體與材質走既有的模組快取（碑就是既有的那一種）、
> E17 **每幀零配置**：中點揭示每幀只做兩個字串比較 ＋ 一個布林讀取，`shouldRevealMidpoint()` 回布林不回物件；
> 稱號只在**讀碑那一下**現算一次；
> F18–21 兩塊碑照既有的石碑規矩擺（離石座 ≥ 7、彼此 > 10、四周 16 個方向都走得到、
> `coverage > 0.8`、不進地標留白），**再多兩條**：離每一層的擺位下限（器物／反應物 10.1、
> 大濁靈 10.6、殘頁 8.4、中觀層碰撞圓 4.6 ＋ 玩家半徑 ＋ 圓半徑）與**離閘門 ≥ 12.1**
> （碑贏過閘門，罩住問話那一圈的話那道門就永遠不問）；「按得到它的方向」逐塊量（§4.19）；
> G22–24 **沒有新增存檔頂層欄位**（只多一個 `flags.midpointSeen`，`normalize()` 本來就吃任意布林旗標）、
> 不寫 `bestGrades`、不給 XP、不收技巧、不算徽章、**不參與任何解鎖**
> （`progression.js` 與 `catalog.js` 靜態掃描 ＋ 零 XP 探針逐項比對解鎖狀態）、重置清得乾淨；
> H25–29 rubric 補了「轉折」一整節（先紅後綠實測八條：綁在關卡數上、旗標影響解鎖、
> 那一層不掛門、稱號階數寫錯、碑往石座挪一步、碑文用禁字、判定掛回跨區那一幀、面板不收 `lit`），
> e2e 補了走進去 → 回聲說 → 柱上多一層 → 再走進去不再說 → 清掉旗標又會說，
> 以及鏡碑「沒到門檻兩行、到了三行、前兩行逐字沒動」。

> **v1.2 · P22 逐條過的結果**（終局：回聲的小祠 ＋ 母碑重立）：
> A1–3 小祠是回聲留在斷環旁的一個地方（牠兌現序章那句「我記得每個人的第一句話」），
> 龕裡那一團濁氣是**玩家自己那句沒說完的話**（不是新的一隻濁靈，見 §4.20 ①b），
> 母碑是抄寫人想把四部原典合寫成一塊、寫不下才倒的那一塊碑；用世界的說法
> （開口／濁氣／刻／碑面留白／刻印記錄）；會說話的只有回聲，而且一次一句（§1.2）；
> B4 **新的第 ⑬ 層**（小祠 4.6、母碑 7.0，兩件同一層、**仲裁排第一位**，理由與量法在 §4.20）、
> B5 `E` 仍然是唯一的互動鍵（面板裡零新增鍵盤監聽，那兩顆走 `rovingList`）、
> 走近提示照舊是標題 ＋ 狀態 ＋ 動詞、B6 一拍一個畫面（還你第一句 → 牠聽懂了 → 刻不刻）、
> **B7 這一格是「不會失敗」的極端解**：判定交不出「沒過」這種形狀，
> 畫面上沒有評價印章、沒有分數條、沒有一句話講你少了什麼；
> 「什麼都還沒寫」是那一下按不下去，不是被退回來；
> C8 **不教** → 小祠零 `source` / 零 `teaches` / 零連結（測試對剝掉註解的原始碼掃過）、
> C9 `npm run fonts` 已重跑（CJK 1955 字）、C10 `zh-scan` 掃過每一句；
> D11–14 夜色與色階沿用中央高原的 `kitFor`，暖金只在燈心與碑面那一點成就光上，
> `prefers-reduced-motion` 下呼吸與「站起來」那一段整個停掉、只留終態（光照樣亮）；
> E15 **新增 0 盞光源**（光源 37 不變；三角 237,264 → **237,536**（＋268）、
> 碰撞體 1,049 → **1,053**（＋4：小祠的後牆與兩片側牆、母碑的碑身））、
> E16 幾何體與材質全部走模組快取、E17 每幀零配置（`nearest()` 回同一個共用物件、
> 那兩支判定回布林不回物件、45／15 公尺分級跳過）；
> F18–21 四顆碰撞體**全部 < `STAND_MIN_R` 0.8**（站不上去靠尺寸成立，不靠旗標宣告）、
> 互動圈與每一層都不重疊（小祠還剩 4.45、母碑還剩 0.32）、
> 按得到它的方向小祠 24/24、母碑 20/24、離閘門 41.5／30.1（都遠大於 R ＋ 7.5）、
> 穿模稽核與可站立體稽核仍然 0 件、四支 `--verify` 全部維持；
> G22–24 **新增一欄 `motherStele`**（`normalize()` 補空字串、壞值落成空字串、與 `firstPrompt`
> 共用同一支清洗）＋ 三個 `flags`；不寫 `bestGrades`、不給 XP、不收技巧、不算徽章、
> **不參與任何解鎖**（靜態掃描 ＋ 零 XP 探針 ＋ 逐項快照三件一起）、重置清得乾淨；
> G24b 世界端由 `world.finale.reset()` ＋ `refreshFinale()` 跟著歸零（不重載也不會演出失聯）；
> **G 這一格多守一條**：那一欄裝的是**玩家自己打的字** —— 選了「先不刻」就根本不寫進存檔
> （測試直接翻整份存檔找那句話），顯示一律 `esc()`，那條路上一個網路呼叫都沒有；
> H25–29 rubric 補了「終局」一整節（先紅後綠實測九條：拿掉刻字前的確認、重寫變成有失敗態、
> 碑上的字不跳脫、那條路上連了網、門檻偷偷綁上等級、解鎖判定偷看碑面那一行、
> 母碑還沒立起來就畫出來、沒到門檻照樣接得到 `E`、舊存檔沒有第一句時擺空白），
> e2e 走完整段終局（還沒開口 → 收齊 → 開口 → 走進去 → 重寫 → 先不刻 → 讀碑 →
> 再走一次刻上去（含 `<script>`）→ 分享 → 舊存檔走退路 → 重置之後重走）。

### H. 測試
25. `npm run test:rubric` — 新資料的合法性、落點、淨空、護欄（不放連結／掛得回真技巧）補上了嗎？
26. `npm run test:e2e` — 至少一條端到端路徑（走過去 → 有反應 → 存進 localStorage → 重整還在）補上了嗎？
27. `npm run test:playtest` — 動到關卡或評分了嗎？動了就要補。
28. `npm run build` 過了嗎？**console error 是零嗎？**
29. CLAUDE.md 的變更紀錄加了一行嗎？

> **課程 v2 · Phase J3 逐條過的結果**（拆掉 D2 相容層：最後兩座教學神廟接上 v2 技能）：
> A1–3 清晰之門與迷路的自動機的世界觀、NPC、素材、情境**一個字都沒動**（改的只有它們教什麼、怎麼計分）；
> B4–7 沒有新增互動層、沒有新按鍵、四幕分鏡未動、仍然不會失敗；
> C8 **教** → 兩座都補上 `primarySkillId`（`clear-specific` / `clear-positive`），
> 第二幕的神諭原典換成**這條技能自己的官方文件**（Microsoft · Prompt engineering techniques ／
> Anthropic · Prompting best practices），標籤上從此看得出是哪一家（`sourceForSkill` 補了廠商前綴）；
> **收集不倒退** → `teaches` 一個位元組沒動，68 條照樣收得滿（§5.3b）；
> C9 `npm run fonts` 已重跑（CJK 1844 字／1463.4 KB）、C10 `zh-scan` 掃過改動到的每一句；
> D11–14 兩座的視覺與世界一格沒動；
> E15–17 **沒有新增任何場景內容**（三角形／光源／碰撞體的變動來自量測本身，不是這一期加的東西）；
> F18–21 世界未動，穿模稽核高／低畫質仍然 0 件；
> G22–24 **沒有新增存檔欄位**，只加了一次純加法的開機回填（§5.4），舊存檔逐欄實測不倒退；
> H25–29 rubric 新增「拆掉 D2 相容層」與「R4：舊存檔搬家與重置實測」兩節（先紅後綠各驗過），
> e2e 補回拆碑的完整鍵盤走查並把手掌印的固定 sleep 全面改成輪詢。

> **課程 v2 · Phase J1 逐條過的結果**（第十二片土地：分歧之廳 ＋ 新題型「拆碑」＋ 唯一一道硬門檻）：
> A1–3 抄寫人把兩份相反的抄本並排釘在同一座廳裡；用世界的說法（兩面的柱 / 模型卡 / 名牌 / 誘餌）；
> 沒有新的會說話的角色（9 個委託人都只在關卡文字裡出現）；
> B4 石座那一層（半徑 6.5，沒有新層）、B5 走近仍然只按 `E`、B6 四幕分鏡未動、
> B7 拆碑貼錯只是「碑不收這個名字 ＋ 就地教學」，而且**整份拆完之前手掌印根本不出現**；
> C8 **教** → 每座掛真的 `primarySkillId`，第二幕照舊顯示「神諭原典：〈文件名〉↗」；
> **反差題的模型卡另外掛得出可點的官方出處**（兩張卡的立場並排，出處逐條回查
> `skill-codex-v2.json`，不得杜撰）、C9 `npm run fonts` 已重跑（CJK 1847 字）、
> C10 `zh-scan` 掃過每一句玩家看得到的字；
> D11–14 夜色不變（青灰、曝光 1.14 是全場最高 —— 廳裡沒有暗處）、用該區 `kitFor` 的色階
> （主色 `#d8b8c6` 寫在 `regions-v2.json`）、地標周圍留白 14 公尺、
> `prefers-reduced-motion` 下拆碑只關掉抖動與位移（名字、金線、教學都還在）；
> E15 **光源 +1**（每區一盞主色補光；**地標本身 0 盞**）、三角形 +4.9k、
> E16 對柱／落碑／植被全部 InstancedMesh ＋ 共用快取、E17 每幀迴圈未動；
> F18–21 9 座石座在高／低兩種畫質下 24 個方向 × 4 段距離全部走得到、
> 穿模稽核兩種畫質都 0 件、落點全部 `coverage > 0.9`、彼此至少 13.7 公尺、
> **中央高原一座石座都沒有被吃掉**（代價是「第一根軌」往北挪了 5.7 公尺 —— 只動座標）；
> G22–24 **沒有新增存檔欄位**（`skippedGates` 的既有行為對其餘 11 區一字未動，
> 分歧之廳則永遠不會被寫進去），舊存檔完全不受影響。

> **v1.2 · P16c 逐條過的結果**（第 ⑧ 層互動「守夜人」：12 位站著不動的人 ＋ 選項式對話）：
> A1–3 他是抄寫人之後留下來守夜的人，用世界的說法（崗位／燈／舊事／神諭的規矩）；
> **A3 是這一格的重點** —— 他會說話，但他不是回聲，而且他**本來就不走**：
> 「沒有會走動的 NPC」那條鐵則一個字都沒改（§1.5）；
> B4 新的第 ⑧ 層、半徑 **4.6**（與石碑同一階，仲裁排在石碑之前 —— 同刻文小語與殘頁那一對）、
> B5 只有 `E`（面板裡完全沒有新的鍵盤監聽）、走近提示是「名字 ＋ 崗位 ＋ `E` ＋ 說話」、
> B6 招呼一行一行浮出、一次只答一種情報、舊事一拍一拍可追問、B7 沒有「答錯」這回事（用選的）；
> C8 **引用** → 技巧那一項的技能名、那一句、官方連結**逐字取自 `skill-codex-v2.json`**，
> `watchmen.json` 裡**一個網址都沒有**（測試對原始檔案掃 `https?://`）；
> `checkLines` 是把既有檢查器「在等什麼」翻成世界語言的一句話 —— 不給範例、不給答案、
> 也不是把檢查器的教學提示搬過來（測試逐條比對）；C9 `npm run fonts` 已重跑（CJK 1932 字）、
> C10 `zh-scan` 掃過每一句；
> D11–14 夜色不變、斗篷與臉用該區 `kitFor` 的色階、燈心是暖白（**人手上的燈**與成就熱點同一個位階）、
> 地標留白改吃 4 公尺並寫下理由（§4.14 第 3 條）、`prefers-reduced-motion` 下呼吸整個停掉、
> 只留終態（頭照樣轉、燈照樣亮 —— 關掉的是動，不是回應）；
> E15 **光源 +0**（燈是自發光材質 ＋ 一片加色 sprite）、三角形 **+1,872**（每位 156）、
> E16 幾何體與材質全部走模組快取、E17 每幀零配置 ＋ 平方距離 ＋ 45／15 公尺分級；
> F18–21 12 位各一顆半徑 0.55 的碰撞圓（< `STAND_MIN_R` → 天生站不上去）、
> 四周 16 個方向 × 3 公尺全部走得到、穿模稽核 0 件、可站立體稽核 0 件、
> **中觀層一片都沒有被清零**（`screen-fit -- --verify` 12 片仍全 ✓）；
> G22–24 `watchmen` 與 `struggles` 兩欄純加法、`normalize()` 逐鍵驗形、
> 不寫 `bestGrades`、不給 XP、不影響解鎖（靜態掃描 `refreshUnlocks()`／`gateSatisfied()`）、
> 重置清得乾淨；G24b 世界端的「聊過了」由 `world.watchmen.reset()`／`setMet()` 跟著歸零。

> **v1.2 · P18 逐條過的結果**（第 ⑨ 層互動「守門者」：一個帶著 system prompt 站在門邊的人）：
> A1–3 他是抄寫人派在護欄崗那道門邊的人，用世界的說法（交辦／門閂／框／信）；
> 同守夜人：他**本來就不走**，「沒有會走動的 NPC」那條鐵則一個字都沒改（§1.5）；
> B4 新的第 ⑨ 層、半徑 **3.2**（**量出來**的，理由與量法在 §3.2／§4.15；
> 他的互動圈與每一層都不重疊，所以半徑比石碑小也不會蓋掉誰）、
> B5 只有 `E`（面板裡完全沒有新的鍵盤監聽）、走近提示是「名字 ＋ 對上了幾行 ＋ `E` ＋ 說話」、
> B6 交辦一行一行浮出、一次答一句、B7／B7c **沒有失敗態** ——
> 他只是「還沒被說服」，禁字表逐句掃資料層與畫面上的字（含反例），
> 而且**進度只累積**：對上的行永不退回，分兩次說完也成立（rubric 與 e2e 各驗一次）；
> C8 **引用** → 15 條分支各綁一支**既有的**檢查器（`checks.js`），
> 出處一律引用 `challenges.json` 裡那一關自己的官方連結（測試驗那一關的 rubric 真的含這支檢查器），
> `guardian.json` 裡**一個網址都沒有**；C9 `npm run fonts` 已重跑（CJK 1945 字）、
> C10 玩家看得到的每一句都掃過；
> D11–14 夜色不變、斗篷與臉用該區 `kitFor` 的色階、板上的字是冷白（他手上沒有燈 ——
> 這片土地的光在門那一頭）、地標留白吃 4 公尺（同守夜人的理由）、
> `prefers-reduced-motion` 下呼吸整個停掉、只留終態（板上已經對上的那幾行照樣亮著）；
> E15 **光源 +0**（板上七行是自發光的薄片）、三角形 **+138**、
> E16 幾何體與材質全部走模組快取、E17 每幀零配置 ＋ 平方距離 ＋ 45／15 公尺分級；
> F18–21 一顆半徑 0.55 的碰撞圓（< `STAND_MIN_R` → 天生站不上去）、
> 貼身三圈 × 8 個方向全部走得到、互動圈上 22/24 個方向站得住、穿模稽核 0 件、可站立體稽核 0 件、
> **中觀層一片都沒有被清零**（`screen-fit -- --verify` 12 片仍全 ✓、`murk-fit -- --verify` 12 ✓）；
> G22–24 `guardians` 一欄純加法、`normalize()` 逐鍵驗形、不寫 `bestGrades`、不給 XP、
> 不影響解鎖（靜態掃描 `refreshUnlocks()`）、重置清得乾淨；
> G24b 世界端那塊板由 `world.guardians.reset()` 跟著歸零（`onReset` 真的呼叫得到，靜態掃描守著那一行）。

> **v1.2 · P23 逐條過的結果**（進程外顯 ＋ 今日三事 ＋ 成就整理）：
> A1–3 等級穿在旅人自己的披肩上（不是一個外掛的介面元件）、今日三事用世界的說法
> （「走一趟」「找一處」「重寫成 S」）、**沒有新的會說話的角色**；
> B4 **沒有新增互動層**（今日三事住在圖鑑那一頁、開關住在設定頁）、B5 **沒有新增任何鍵**、
> B6 三件事就是三行，一次讀得完、B7 **沒有失敗態**：沒做完不會有任何事發生（§3.8）；
> C8 **不教** → `daily.js` / `ui/daily.js` 零 `techniqueId`、零 `source`、零連結；
> C9 語料的字元集**一個字都沒有變**（新檔案用的漢字全部已經在子集裡），
> `npm run fonts` 重跑後 6 個 woff2 位元組相同、總量仍是 1535.5 KB；
> C10 玩家看得到的每一句都掃過，另加一份**禁字表**（過期／逾期／期限／倒數／截止／
> 連續／錯過／失敗／懲罰／限時；「任務」只准出現在「不是任務」那一句裡）——
> rubric 掃原始碼與畫出來的 HTML 兩面，e2e 再掃一次真的畫面；
> D11–14 光點用暖金（成就熱點那一階）、夜色一格沒動、
> 多一格那一下整圈亮一次 —— **借的是慶祝那條曲線**（同一種鐘形、同一個 1.25 秒），
> 不新增動畫檔、**不新增音效 cue**；格子變少（重置）不亮，那一刻不該像在慶祝；
> `prefers-reduced-motion` 不受影響（光點本身不位移，只有亮度在動）；
> E15 **光源 +0**（自發光材質）、角色多一個 `InstancedMesh`（draw call +1、材質 +1、幾何 +1、
> 滿級三角 +792），**世界端的數字一個都沒動**（`audit:perf` 高／低畫質逐值不變）、
> E16 一份幾何一份材質、99 個實例、位置建構時算好、E17 每幀只改一個整數與一個材質亮度，
> 零配置（換日那一支**刻意不放在每幀迴圈裡** —— 它會 new 一個 Date 與一個字串）；
> F18–21 **碰撞體 +0**：角色不在 `collectSolids()` 掃的那棵樹底下，所以這件事是結構上成立的；
> G22–24 `daily` 一欄純加法、`normalize()` 驗形（日期不合形就整筆當成還沒挑過）、
> 不寫 `bestGrades`、不給 XP、不影響解鎖（靜態掃描 `refreshUnlocks()`／`gateSatisfied()`
> ＋ 零 XP 探針 ＋ 逐項快照）、重置清得乾淨；
> **另加一條這一格自己的驗收**：關掉之後的整份存檔與「從來沒有這個功能」的那一份逐值相同；
> H25–29 rubric 新增一整節（先紅後綠：15 種破壞法逐一驗過會紅），
> e2e 新增「升等時光點真的多一格」與「關掉之後那一區不再出現」，
> 兩段都用**輪詢**不用固定 sleep。

---

*最後更新：v1.2 · P23（**進程外顯**：等級穿在披肩上，一圈 33 格 × 三圈 ＝ 99 §6.1；
**今日三事** §3.8：提議不是任務、本地時間換日、可關且關掉後與沒有這個功能逐值相同、用語禁字表；
**成就對齊** §3.9：`hiddenAchievement()` 直接呼叫 P22 的 `shrineOpen()`，
加上 `achievementDone`／`p23Aligned` 兩個旗標保證不倒退；存檔新增 `daily` §5.4）
／ v1.2 · P18（第 ⑨ 層互動 **守門者** §1.5／§3.2／§4.15：
一個帶著看得見的 system prompt 站在門邊的人、15 條離線分支、選項式作答、
沒有失敗態、進度只累積；`guard` 介面與離線預設實作；存檔新增 `guardians` §5.4；§6.1 預算實測更新）
／ v1.2 · P16c（第 ⑧ 層互動 **守夜人** §1.5／§3.2／§4.14：
12 位站著不動的人、選項式對話、四種情報；擺位規則與「按得到他」的量法；
存檔新增 `watchmen` 與 `struggles` §5.4；§6.1 預算實測更新）
／ 課程 v2 · Phase J3（**拆掉 D2 相容層** §5.3b：`primarySkillId` 是教學的正典、
`teaches` 只剩收集、`primaryTechniqueId` 只剩祖先與時代註記；§5.4 的開機回填；§6.1 預算實測更新）
／ 課程 v2 · Phase J2（**12 座應用關（試煉）** §3.3d：第二幕整幕不存在、
動態 rubric 與門檻公式、絕不軟鎖、鷹架撤除到底；**大師層印記** §3.3e：
無筆之印／默寫之印／一區純手／分歧之證，全部選配、永不擋路、作弊面釘死在「範例」；
§5.4 新增四個純加法存檔欄位；§1.4 護欄崗的地標讓到邊緣）
／ 課程 v2 · Phase J1（第十二片土地 **分歧之廳** §1.4：高原東側的建物、
零實體光源的 **兩面的柱**、當時全場唯一一道硬門檻（2026-08-03 已鬆綁成軟門檻，見 §1.4）、
「加建的地界不得吃掉別人的橋」這條硬規則；新題型 **拆碑 `reverse`** §3.3b 第 13–15 條；
§6.1 預算實測更新）
／ 課程 v2 · Phase I（第十一片土地 **觀象臺** §1.4：正東偏北的高地、
零實體光源的 **朝天的鏡**、不接在任何一區後面的一條橋；新增 §3.3c **多模態的誠實界線**
（不引進媒體資產、零外部請求、素材是抄寫人寫下來的文字）；§6.1 預算實測更新）
／ 課程 v2 · Phase H（第十片土地 **減法之庭** §1.4：高原北緣的加建、零實體光源的
**空的基座**、整張地圖上最平的一片土地；新題型 **轉鈕 `sim`** §3.3b 第 10–12 條：
離線輸出樣本 `sim-samples.json` §5.3、三檔都轉過才開放刻印、旋鈕不參與評分；§6.1 預算實測更新）
／ 課程 v2 · Phase F（第七片土地 **契約鍛冶場** ＋ 加建的 **護欄崗** §1.4：
正西的鍛台與工具溝、東北外緣的哨所、兩座零實體光源的地標、加建的頸口與它的閘門、
派工檯的稱呼可換皮、§6.1 的預算實測更新）／ 課程 v2 · Phase E（第六片土地 **量器坊** §1.4：正南的鑄場台階、地標刻度之柱、
第一道**知識式軟門檻**、第一個**沒有配樂音檔**的區域 §6.5；效能預算改為實測值 §6.1；
新上線區域的名稱與主色住在 `regions-v2.json` §5.3；石座 62 → 75）。
前一次：Phase 27（新增作答文法 §3.3b：三種題型 `choice` / `order` / `workshop`，
共用手掌印結尾、同一支離線引擎、不會失敗、純鍵盤走得完；石座 26 → 27）。
前一次：Phase 25（新增第五層互動「動得了的器物」§4.6、三個高度階 §4.7、
面向排名 §3.2、飄空物件的下緣規則 §6.1）。
這份文件跟著世界一起長 —— 新增了會影響「以後怎麼做」的規則，就回來補一條。*
